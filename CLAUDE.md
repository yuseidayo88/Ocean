# OneFound

一人社長のための AI カンパニー SaaS。統括AI（Executive）が AI社員を編成し、
ユーザー（社長）はゴールを示し、採用し、判断だけを行う。

※ GitHub リポジトリ名は歴史的経緯で `Ocean` のまま。プロダクト名は **OneFound**。

## 進め方（厳守）

- **進行計画は `docs/PLAN.md`**（元のマスタープロンプトの Phase 0〜10 ＋ Phase 11 課金）。
  **Phase 10 まで完了。Phase 11（課金）は骨格まで** — 決済（Stripe の鍵）と
  公開（`CLOUDFLARE_API_TOKEN`）が外部待ち。**ダミーデータは撤去し、ゼロの状態から
  始まる**（2026-08-24）。**Case B / D の統括AI接続も済み**（同日）。次は公開の前提（鍵）
- **宿題10件は全部やる。後回しにしない**（→ `docs/PLAN.md`）
  - A（構造が増える・**コード前に設計**）: Discovery Mode / 既存事業の取り込み /
    質問 / はじめての画面（空状態を含む）→ **画面5枚 ＋ データモデル追記を1ラウンド**
  - B（器の振る舞い・**Phase 4 で**）: 統括AIの3状態 / 右ペインの3状態 / エラー画面 / 会話履歴の分離
  - C（中身）: ロスター定義文 → Phase 7、多言語 → Phase 3 で骨だけ
  - **Phase 4（ダミーデータで全画面）が本当のUI/UXレビューの場**。静止画で磨き込みすぎない
  - ~~いまの設計は原文の Case A にしか対応できていない~~ → **Case A / B / D が通る**（2026-08-24・下の「入口の接続」）

- フェーズ制。**各フェーズの終わりで必ず停止し報告する**。
  報告は「何をしたか / 変更点 / 確認できるもの / 次フェーズの予定 / 判断が必要なこと」。
- **ユーザーの明示的な承認なしに次のフェーズへ進まない。**

## 現在地

- Phase 0（方向性3案）完了 → 案A Mission Control 採用
- Phase 1（画面設計）完了 ＋ **A群の設計ラウンド完了**。23画面。全画面が同じデザイン言語。
  - **入口**: ⓪ はじめての画面 → **あとはチャット1本**（2026-08-24。⓪-a〜⓪-d の
    専用画面は撤去し、候補も診断も**会話の中のカード**になった）
  - **本編**: ② 計画の承認 ③ ホーム（**オフィス / デスク / ワークフロー**の3ビュー）
    ④ Work（会話なし・一目で分かる1枚）⑤ タスク ⑥ 成果物 ⑦ 決定事項 ⑨ メンバー ⑩ 通知 ⑭ チャット
    （① 新しいWork はチャットに、⑧ 採用はメンバーに畳んだ）
  - **板**: ⑪ デザインシステム ⑫ スキル（SKILL.md の管理）
  - **会社**: ⑬ 会社の切り替え ⑬' アカウント（23画面）
- **入口は3つに分かれる**（→ `docs/design/01-data-model.md` 入口）
  - **やりたいことがある** → そのまま書く（composer が主役）
  - **まだ決まっていない** → 条件を構造で集めて候補3つを比較（`discovery_sessions`）。
    候補は**採用しなかったものも残す**（なぜその道を選んだかは、選ばなかった道と並べて意味になる）
  - **すでに事業がある** → サイト・資料・数字を取り込んで診断（`business_profiles`）。
    診断は必ず「次に何をするか（Work）」まで持つ
- **会話はチャットに一本化する**（→ `docs/design/01-data-model.md` チャット）
  - 左レール下段に **チャット履歴**（ChatGPT の Your chats と同じ置き方）。
    ＋で新規、未読は青い点、選択中は面が明るい
  - **Work は会話を持たない。** Work＝フェーズ・タスク・成果物・決定事項の器。
    「統括AIに相談する」でその Work に紐づいたスレッドへ飛ぶ（`chat_threads.work_id`）
  - 理由: 相談は Work をまたぐ / 履歴が1か所にあるほうが探せる / Work 画面が状態に集中できる
  - **`/api/*` はページとは別の関数として配られる**（Vercel も Cloudflare も）。
    メモリのストアはそこからは見えないので、ページ側が書いたスレッドが
    **まるごと存在しないように見える** — 実際「このチャットは見つかりませんでした」だけが出た。
    だから**見えなかったときは `fallback` と言い**、画面は**流さない道**
    （`chatReply` ＝ ページ側の関数）で取り直す。
    **存在しているものを「見つかりません」と言わない。**
    落ちる前に会話を読み直し、**もう返事が入っていれば頼み直さない**（二度払わない）
  - **返事は流れてくる**（`/api/chat` ＋ `lib/chat/stream.ts`。2026-08-24）。
    サーバーアクションは返り値が1個なので、返事が全部できるまで画面は「考えています」のまま —
    モデルが10秒考えれば10秒黙る。**それはチャットとして成立していない**。
    行区切りの JSON（`{"t":…}` / `{"done":true}` / `{"err":…}`）で流し、
    **最初の1文字が届いた瞬間から読める**。カードは往復の終わりにしか揃わないので、
    終わりの合図でスレッドを読み直して本文ごと差し替える。
    流す道が使えない環境では**流さない道に落ちる**（`chatReply`）— 返事が出ないよりいい
  - **道具を呼んだ往復には本文が来ない。** OpenAI 互換のモデルは `tool_calls` を返すとき
    `content` を空にするのがふつうで、「道具を呼ぶときも本文を書け」と頼んでも守られない
    （実際 Luna で **「（返事がありませんでした）」だけが並んだ**）。
    だから足りないときは**道具を渡さずに、もう一度だけ短く書いてもらう**（`prose`）。
    決め打ちのプロバイダも同じ振る舞いにしてある — **本物と同じ道を通さないと、この穴は見つからない**
  - **右ペインの会話も、まったく同じ統括AI**（2026-08-24）。前はここだけ道具を持たない
    別の返事で、同じことを聞いても形が違った。カードは狭い器に押し込まず、
    **「◯◯があります · チャットで開く」の1行**にして会話の画面へ渡す
  - **送るのと返してもらうのを分ける**（`chatSay` / `chatReply`。2026-08-24）。
    書くだけならモデルを待たないので、**入口のボタンを押した瞬間に会話の中にいる**
    （実測 107ms）。返事は開いた画面が取りに行く — 最後の発言が社長のものなら、
    そのスレッドで1回だけ `chatReply`。前は `/start` に立ったまま数秒待たされていた
  - **会話はいつも下に貼り付く。** 開いたときも返ってきたときも、いちばん新しい発言が見えている。
    **1回貼るだけでは足りない** — 書体が届いたりカードの中が伸びたりして、
    描き終わったあとから中身が高くなる（`ResizeObserver` で伸びたら貼り直す。
    ただし**上を読んでいるあいだは動かさない** — 下から 80px 以内にいるときだけ）
- **質問は会話の最後に、説明つきの選択肢リストで出す**
  （参考: Claude の AskUserQuestion / Gemini / Mindtrip）
  - **会話には流さない。入力欄の上にくっついた板として出す**（スクロールで流れない）。
    答えるとそのまま会話が続く
  - **選択肢は「見出し＋1行の説明＋番号キー」**。「¥1,980」だけでは選べない
  - **番号キーは行の左**。推奨に緑の印をつけ、いちばん上に置く
  - **最後の行は自由入力（鉛筆アイコン）で、その行の右端にスキップ**
  - 右上に **‹ N / M ›** と **✕**（複数あるときは順に1つずつ）
  - **答え終わった条件は緑のチェック＋項目名つきのチップ**で見せる
    （質問の選択肢と見間違えないように）
  - **質問は決定事項の台帳に出さない**（事業判断だけ昇格）
- **どの画面からでも統括AIと話せる**（→ `docs/design/08-panes.md`）。
  入力欄に書いて Enter → **右ペインがその会話になって開き、入力欄はその中へ移る**
  - 参考（Mobbin）: ClickUp Brain / Fabric / HoneyBook / HubSpot / Customer.io。
    **右にAIパネルを出すアプリは例外なく入力欄もパネルの中に置いている**（読む目と書く手を離さない）
  - **右ペインの3つめの形は作らない。** パネルの一種（見出し ＋ ✕）。タブにもしない
  - **右は1枚だけ。** 会話を開くと画面側のペインは引っ込み、行を選び直すと会話が引っ込む
  - スレッドは画面が決める（Work の画面ならその Work のスレッド）。見出しの右は ＋ と ⤢
  - **チャット画面では出さない**（そこは会話そのものが主役）
  - **返事はチャットの画面とまったく同じ**（2026-08-24 に繋いだ。道具も同じ）。
    流れてくるところまで一緒で、カードだけは「◯◯があります · チャットで開く」の1行にする
  - 盤面のある画面（オフィス / ワークフロー）は、**器を測って与えられた面いっぱいに描く**
    （`lib/use-size.ts` の `useSize`）。**文字は縮めない。伸び縮みしていいのは絵だけ** —
    ただし**ワークフローだけは例外**。自分で拡大縮小する地図なので、文字も一緒に伸び縮みする
- **入力欄は全画面で同じものを1つ**（`shell.composer`）。中央下部・幅748。
  - **1行にまとめる**（参考: ChatGPT）。`＋ / 書くところ / 宛先 ⌄ / ↑` を横一列。
    **2段に分けない。** 高さ52・角丸26（＝真ん丸の端）。
    書いて2行以上になったときだけ縦に伸ばし、道具は下端に揃える（角丸18に落とす）
  - **中身の上に浮かせる**（`position: absolute`）。中身は画面の下まで使えて、
    下端は黒のグラデーションに溶ける。**重なってよい**（スクロールで続きが見える）
  - ただし**下に貼り付く中身**（レーンの下段・行動の行・盤面のツールバー）は
    `COMPOSER_H = 108px` ぶん逃がす。隠れたままにしない
  - **黒に溶かすのは、中身が入力欄の裏に潜る画面だけ**（`veil`）。
    盤面（ワークフロー）は中身が入力欄の上に収まっていて潜らないので、
    溶かすと背景のドットを切るだけになる → 溶かさない
  - **帯そのものは指を通す**（`pointer-events: none`。2026-08-24）。受けるのは
    入力欄と質問の板だけ。帯は 124px（48 ＋ 52 ＋ 24）あるのに逃がすのは `COMPOSER_H` の 108px なので、
    帯が指を受けると**下端 16px にあるものが押せなくなる**
    （メンバーの全員の歯車／計画の「Work を見る」が実際そうだった）。
    盤面のホイールをふさがない、という元の理由も一緒に満たす
  - **質問の板が乗ると帯は高くなる。** 下に貼り付く行はその実寸ぶん逃げる
    （`Composer` の `onHeight` → `Math.max(COMPOSER_H, h - 16)`。板が無ければ `COMPOSER_H` に戻る）
  - **中身と入力欄のあいだを詰めない。** `COMPOSER_H` は
    入力欄 52 ＋ 窓の下との間 24 ＋ **中身との間 32**。
    前は 112 で入力欄が 94 あったので、中身の終わりと入力欄の上端がちょうど重なって窮屈だった
    （下まで送った状態の実測: **−91px → 39〜74px**）
  - **入力欄が主役の画面だけ中央に置く**（はじめての画面 / 新しいWork）→ `floating=False`
  - **質問の板は入力欄と一体で浮く**（`composer(above=ask(...))`）
  ＋ / **宛先 ⌄** / ↑。**要らないと判断した画面以外は必ず置く**
  - **深さの選びは置かない**（2026-08-25 に外した）。ここにあった `自動 / 浅め / …` は
    自分の中だけで値を持っていて、押しても何も変わらなかった（「自動」という言葉も
    ほかのどこにも無い）。**深さはモデルの中の話**なので、モデルと並べてメンバー画面に置く
  - **宛先＝どの Work の会話に書くか**（2026-08-24）。相手はいつも統括AIなので、
    選ぶのは「どの話の続きか」。**Work の名前を並べ、いちばん下に「新しいチャット」**。
    選ぶとその会話へ移る — 宛先だけ変えて画面に残ると、
    いまどこに書いているのかが画面のどこにも出なくなる
  - **終わった Work は出さない**（もう相談することが無い）。
    **Work が1つも無くても畳まない** — ⌄ が付いているのに開かないのは、社長には壊れて見える。
    会話の途中からでも「新しいチャット」へ抜けられるので、行き先はいつも本物
- **ホームが見せ場**。上部ピルで3ビュー切替。ホームだけ右ペインなしの全幅。**KPIの数字は出さない**（稼働中/タスク/判断待ち/今週の成果の帯はやめた）。
  読む順は **誰がいるか（オフィス）→ 手もとで何が起きているか（デスク）→ この先（ワークフロー）**。
  **放っておけないこと**（判断待ち / 要確認 / 遅れ）は**ビューの外**に置いて、どのタブでも見える
  - **オフィス＝上に「絵とログ」、下に「AI社員」**（2026-08-23 に作り直した）。
    絵だけの1枚では「3つの Work のうち1つが遅れています」が置けず、
    かといって文章で言い直すと絵が要らなくなる。**答えは絵の外に、事実として並べる**
    - **絵は与えられた面いっぱい**（`lib/use-size.ts` の `useSize` で器を測る）。
      入る大きさに縮めるのではなく、**あるだけ使う**。下に空白を残さない
    - **「あなた」は描かない。** 社長は統括AIより上にいる存在で、社員の一部ではない。
      **ツールバーも置かない**（拡大率もカーソルも手も、見れば分かることを言い直している）
    - **軌道 = Work。1本の輪が1つの Work。** 輪そのものが進捗の計器で、
      **真上がはじまり、時計回りに済んだぶんだけ明るい弧**になる。数字は書かない
    - **弧の色 ＝ その区間をやった人。** 色が変わるところが引き継ぎで、それはそのまま
      フェーズの境目でもある。**だから刻みは置かない**（二度言うことになる）。
      変わり目には**時計回りの矢羽根**を立て、先端は**尾を引いて「いま」**を言う
    - **社員は自分がやった区間のまん中に立つ。** 先頭の球の下にだけ、いまのフェーズ名
    - **輪の縦横比は一定にしない。** 内側を平たくしておかないと、内の輪の社員と
      外の輪の社員が同じ角度で重なる（球は縮まないので、輪だけ広げても離れない）
    - **ゆらぎは球の中の層に掛ける。** 外側の `<Link>` に掛けると `transform` が
      上書きされて真ん中合わせが消え、球が輪から半個ぶんずれる
    - **Work 名は輪の外側・左上に、輪ごとに角度を変えて置く**（198° / 225° / 242°）＋
      輪へ伸びる短い線。真下に積むと、どの輪の名前か分からない
    - **判断待ちの Work だけ、先端の少し先に橙の菱形**（この Work の次はあなたの番）。
      赤い点線は予定との差。ガントの ◆ と同じ読み方にする
    - **文字は名前と Work 名とフェーズ名だけ**。状態も進捗も文字では書かない
    - **右＝ログ**（`EVENTS`）。見出しは「ログ」、右上は緑の点＋「稼働中」。
      縦にスクロールし、下端は黒に溶かす。
      引き継ぎもここに出る（「◯◯ から △△ を受け取りました」）
    - **輪は 0% から今の割合まで引かれる**（開いたとき）。進み具合そのものが動きになる。
      位置（%）を時間に写すので、引き継ぎの矢羽根も社員の球も、
      **弧がそこに届いた瞬間に出る**（あとから足したように見えない）。
      `pathLength={1}` で長さを 1 に正規化するので、輪の大きさに関わらず同じ速さで進む
    - **下＝AI社員**（1人1枚、**横に送る**。**1人ぶんの幅は縮めない**）。
      **見出しの行は置かない**（`AI社員 4人 · 稼働 3 / 4` はやめた。
      顔と名前が並んでいれば分かるし、人数も稼働も1枚ずつのカードが言っている）。
      `overflow-x: auto` だけでは縦のホイールが横に効かないので、
      **縦の回転を横の移動に変える**（`lib/use-rail.ts` の `useRail`）。
      端は黒に溶かすが、**本当にまだあるときだけ**出す。送り切ったら親に返す
      統括AIを先頭に分けて置く（AI社員ではないので人数にも入れない）。
      1枚は「名前 / 状態・モデル / いま何をしているか＋経過 /
      工程の行＋`3 / 5 · ページ取得` / 出したもの＋待ち」。
      **深さはここに出さない**（2026-08-25）— 言葉が長く（「いちばん深く」）、
      状態の語と1行に収まらない（実際 `待 / 機` と2行に割れた）。
      **状態の語は縮めない**・モデルが長いときは `…` で切る。深さはメンバー画面で見る。
      **工程の行は、いまやっている1つだけ脈打つ**（実行中のときだけ。
      動いているという事実なので、動きを減らす設定でも止めない）。
      **出したものの器は担当ではなく `produces` で決める**（デスクと同じ規則）
    - **上の帯は置かない**（`きょうの決定` も `稼働 3 / 4` もやめた。答えの一文も置かない —
      遅れも判断待ちも、絵と社員の行が言っている）
    - **輪の上を白い光がまわり続ける**（惑星）。輪が大きいほどゆっくり回るので、3本が同じ拍にならない。
      **色は付けない** — 色は意味にだけ使う（AI社員の色は担当を言うためのもの）。
      **`stroke-dashoffset` で走らせない** — 合成に上がらず、何もしていないのに毎フレーム
      塗り直しになる（`/home` が 8%）。**円を回して器のほうを縦に潰して楕円にする**（transform だけ → 1%）
    - **動いている社員は、統括AIへ自分の色の粒を送る**（割り当ての線の上を流れる）。
      止まっている人の線には何も流れない（演出ではなく、そのままの事実）
    - **絵は行き先を持たない。** 社員の球も Work 名も統括AIも、押して別の画面へ飛ばさない。
      代わりに **絵の球と下の一覧が対**になっていて、どちらかに指が乗ると同じ人が両方で明るくなる。
      絵の側は**星のように光る**（光の輪 ＋ 十字と斜めの光条。光条は瞬く）／行の側は白の膜3%。
      **見えていないあいだは瞬かせない**（16本ぶんの計算が、何もしていないのに毎フレーム走る）
  - **デスク＝縦長レーンを横に並べる**（参考: Emergent / Google AI Studio）。
    稼働中の社員の手もとを一気に見る。1人1レーン、人が増えたら横スクロール
    - レーンは「アバター＋名前＋状態 / **いま何をしているかの日本語1行** /
      **工程の行**（`run_steps` を1行に畳む＋所要時間） / 中身 / タスクと進捗」
    - **中身の器は担当ではなく `produces` で決める**（業種を埋め込まない）。
      調査＝積まれる事実 / 執筆＝1文字ずつ伸びる文章 /
      実装＝行番号つきのコードと＋の差分 / 待ち＝成果物と「見て決める」
    - **本当に動く。** Durable Object から WebSocket で `run_steps` を押し出す。
      演出ではないので、**止まっているときは止まっていると出す**
    - **判断待ちのレーンだけ橙**。待機は点線で沈める。1人を選ぶと全画面
  - **進捗は畳んだ**（2026-08-26。社長の「てかこの画面いる？」）。ビューは3つになった
    - **ここにしか無かったのは日付だけ。** 進み具合も判断待ちも予定との差も、
      **オフィスの輪がすでに言っている**（輪そのものが進捗の計器、赤い点線が予定との差、
      橙の菱形が判断待ち）。進捗＝オフィスから絵を取って日付を足したもの、になっていた
    - **会社ぜんぶのカレンダーは重すぎた。** 日付の出どころは統括AIが計画のときに書いた週数で、
      それは社長が承認したものだが、誰も守っていないし、ずれても引き直していない。
      **1画面ぶんの重みを載せるには弱い**（「遅れ N日」の1語ぶんなら載る）
    - **日付は Work 画面のフェーズの行へ移した**（`lib/exec/work-view.ts` が組む）。
      あの列は最初からあって、ずっと空だった
    - **いちばん価値があったのは答えの1行**（判断待ち / 要確認 / 遅れ）。
      **タブを選ばないと見えない**のがおかしかったので、ビューの外に出して
      ピルの左に置いた（`Waiting`）。**出すのは放っておけないときだけ** —
      無いときは何も置かない（「ありません」を1行ぶん使って言わない）
    - **消したもの**: `Progress.tsx` ／ `HomeData` の `ticks` / `todayX` / `done` ／
      `Work` の `over` / `restDays` / `endDate` / `state` / `crew[].x` / `gate.x` ／
      `Phase` の `x` / `w` / `from` / `to` / `goal` / `done` / `all`。
      **ガントの座標を組む計算がまるごと消えた**（誰も読まなくなったので）
    - 直前（2026-08-26 の朝）に密度と明度を直したばかりだったが、
      **直したら見やすくなった＝要る、ではない**。見やすくなって初めて
      「これ、オフィスと同じことを言っている」が見えた
  - **ワークフロー＝地図**（2026-08-23 に作り直した。参考: 地下鉄の路線図 / Datadog のマップ）。
    **横に区切らない。** 会社ぜんぶの Work を1枚の格子の上に置く
    - **会社の幹が1本、左を縦に通る**（2026-08-25。4案から社長が選んだ形）。
      Work はそこから枝分かれする — 「これは1つの会社の地図だ」が一目で出る。
      幹は格子の上（列0のまん中）に置き、鎖は列1から始まる。
      **Work が1本しか無い会社では引かない**（どこから来てどこへ行く線か言えないので、
      鎖を左端から始めて余白も作らない）
    - **格子に載せる。** 列は 180px おき、段は 96px おき。位置を目分量でずらさない
      （わざと互い違いにすると「整っていない」に見える）
    - **文法は2つだけ** — **鎖の中は横の線**（次のフェーズ）/ **枝は縦の線**（新しい Work・成果物）。
      **直角に曲がる線**（角だけ丸める）。斜めのベジェをやめると、線がどこへ行くのか目で追える。
      同じ親から出る枝は**1本の幹にまとめる**ので交差しない
    - **済んだフェーズが2つ以上続いたら1枚に畳む**（`フェーズ 1〜3 · 完了`）。
      **畳んでも元のフェーズ番号は失わない**。フェーズが増えても鎖の幅が伸びきらない
    - **ノード＝左3pxの色帯 ＋ タイトル ＋ サブ1行**。サブは「フェーズ N · 状態」。
      **中にアイコンの四角を置かない**。ヘッダ帯も状態ピルも置かない。角丸14で、
      フェーズ/成果物/判断を同じ器で表す。**ポートの円は置かない**（線が端から出れば分かる）
    - **色帯の読み方は3段** — 済＝暗い / いま＝明るい / これから＝点線 /
      **あなたの番＝橙**。**完了を緑にしない**（緑は社員の「実行中」で使っている）
    - **社員はいまのフェーズのノードの右に置く**（⊕ で足すものではない）。
      **オフィスと同じ粒子の球**（`Orb`）。ここだけ別の丸を描かない。
      40px 未満は同心リングと芯だけになって球に見えないので、**球に合わせてノードを大きくした**
      （176×60・列 198・段 104）。**枠からはみ出させない** — 球がいるノードは
      そのぶん右の余白を広げるので、文字が球の下に潜らない
    - **押しても盤面から出ない。** ノード・Work 名・チップのどれを押しても、
      **その鎖だけが残ってほかが沈む**（0.26）。8本の鎖が1枚に載っているので
      「これはどの Work か」は本当に分からなくなる。ここで答えるのが筋で、
      別の画面に飛ばすのは答えていない。選んでいる1件は URL に持つ（`?view=flow&of=w-lp`）
    - **外し方は3つ** — 同じものをもう一度押す / 盤面の空きを押す / Esc。
      ミニマップも同じように沈む（盤面と同じデータから描いているので自動でそうなる）
    - **線の上のラベルは素の小さい文字**（ピルにしない）。「新しい Work」＝枝分かれ
    - **動かし方は Figma と同じ**（2026-08-23）。**道具は置かない** —
      拡大率も手のアイコンも出さず、**指の動きだけで通じる形**にする
      - **二本指・ホイール＝移動** / **⌘（Ctrl）＋ホイール・ピンチ＝指の下を軸に拡大縮小**
      - **拡大縮小は行き先を追いかける。** ホイールの1目盛りは飛び飛びなので、そのまま当てると
        段でカクつく。行き先だけ先に決めて、毎フレーム残りを一定の割合で詰める
        （`1 - exp(-dt/42)`。時間で詰めるので画面の速さが変わっても手ざわりが同じ）。
        **つまんで動かすときは追いかけない** — 指と1対1でないと嘘になる
      - **動かしているあいだ React を1回も通さない。** transform も背景も地図の枠も
        `ref` から DOM に直接書く。中身を組み直さなくても（`Scene` は memo）、
        差分を取るぶんだけ毎コマ遅れて「ぎこちない」になる。state は指を離したときだけ合わせる
      - 1目盛りは 1.19 倍（`exp(-deltaY/700)`）。**大きく飛ばすと、間を詰めても段に見える**
      - **空きをつまんで移動**（`grab` → `grabbing`）。**動かさずに離したら「押した」**
        （選択が外れる）。4px 動いたらつまみ、それ未満は押した扱い
      - **ダブルクリック／⇧1 ＝全体に合わせる** / **⇧0 ＝等倍**。
        全体に合わせるとき、**入るなら等倍より拡大しない**（粗くしない）
      - **右下の地図をつまんでも動く**（そこが真ん中に来る）。
        **地図の範囲は中身だけから測る** — 見えている範囲まで入れて測ると、
        端まで送るたびに地図そのものの縮尺が変わって「勝手に拡大された」ように見える
      - **中身は画面から出さない。** 器より小さいときは器の中で動ける／
        器より大きいときは器が中身の中で動ける。**そのうえで器の 45% ぶん余分に送れる**
        （ぴったりで止めると窮屈で、地図を隅に寄せて見る、ができない）。
        押しても中身は半分残るので迷子にはならない。
        **「入るなら動かさない」は行き過ぎだった** — 送りも地図も死んでしまう
      - **全体に合わせるときは器いっぱいまで大きくする**（等倍で止めない）。
        合わせる箱は**中身の外周**（ノード・Work 名・線のラベル）から測る。
        格子の左端 56px を箱に含めると、そのぶん左に寄って右が空く
      - この盤面だけは**文字も一緒に伸び縮みする**。地図なので、
        「文字は縮めない」より「絵と文字の位置が変わらない」ほうが正しい
      - ドットの背景も一緒に動いて伸び縮みする（どれだけ動いたかが目で分かる）
    - 背景はドットグリッド。**ツールバーは置かない**（カーソルも手も拡大率も要らない）
    - **盤面は中身の領域いっぱいで、入力欄の下まで伸ばす。** ドットの背景が途中で切れず、
      入力欄はその上に浮く。ただし**いちばん下の段まで `COMPOSER_H` の上に収める** —
      盤面が下まで伸びることと、中身が隠れたままになることは別
    - **線（SVG）とノード（HTML）は同じ寸法で描く。** SVG を `slice` で拡げると
      線だけ 1.1 倍になって、ノードと噛み合わなくなる
    - **右下のミニマップは盤面と同じデータから描く**（飾りの棒を並べない）。
      範囲も**中身から測る**ので、Work が増えて盤面からはみ出しても地図はそのまま合う。
      枠は**いま実際に見えている範囲**（入力欄に隠れているぶんは入れない）
    - **下に詳細パネルは置かない**（盤面だけ）
    - **操作方法の説明文は置かない**（見れば分かる形にする。デザイン優先）
- **色は最低限**（ゴチャゴチャさせない）
  - 意味の色は Google 系の濃い色。**面は濃く、文字は明るく**
    青 #1A73E8 / 緑 #1E8E3E / 橙 #E37400 / 赤 #D93025
    文字 #669DF6 / #5BB974 / #FDD663 / #F28B82
  - **AI社員の色はオフィスの絵だけ**。表・リスト・ピルには出さない
    シアン #2AA9BF / パープル #9A5CD0 / インディゴ #5C6BC0 / グリーン #34A853
  - 青は**1ペインに1つ**
- **メンバー＝どんなAI社員がいて、何を頼めるか**（表はやめた → `design/pages/README.md`）
  - 表は列で並ぶので「いま何をしているか」が主役になり、デスク・オフィス・成果物と重なっていた。
    メンバーだけが持てるのは **この会社は何ができて、どう働かせるか**
  - **1行が3段** — 名前（＋英語名・状態）/ 何をするか（約束1行）/ できること（タグ）
  - **「できること」のラベルは置かない。** タグが動詞で書いてあるので見れば分かる。
    行に出すのは3つまで、あとは灰色の `+2`。**読むだけで ＋ は置かない**
    （何ができるかは社員の定義と読ませた SKILL.md で決まる）
  - **1行めは約束、3段めはできること。二度言わない**
  - **統括AIをいちばん上に固定。** AI社員ではないので `EMPLOYEES` に入れない（採用も解雇もできない）。
    モデル・深さ・設定は社員と同じに持つ
  - **在籍のあとに「まだいない」人を暗く並べる**（2026-08-24）。行の形は同じまま、
    違うのは2つだけ — 全体を 0.58 に沈める／右はモデルと歯車ではなく `採用する`。
    押せば右ペインで**約束と守ることを読んでから**採れる（`who='candidate'`。器は `SettingsPane` のまま）。
    候補＝**ロスターの定義 − いまの在籍**。突き合わせる前に `slugOf()` で別名を寄せる —
    寄せないと `revenue-strategist` の戦略担当が、在籍と候補の**両方に並ぶ**
  - **モデルと深さは行の右に縦に積む。別々の操作で、どちらも枠を持たない**
    （面が出るのは指が乗っているあいだ 3% と押した瞬間 5% だけ）
  - **歯車は名前の行に上揃え**（26px の当たりを 24px の行の中心に）
  - **要確認 は文字の右に書類のアイコン**。押すとその成果物へ飛ぶ。
    橙の「見て決める」ボタンは置かない（行の中でいちばん強い面になる）
  - 列は 30（アバター・ヘアラインの左）/ 79（名前・約束・タグ・全員の行）/ 1150（すべての右端）
- **設定は全部、右ペインで開く。画面ごと移動しない**（→ `docs/design/08-panes.md`）
  - **器は1つ**（`SettingsPane`）。社員 / 統括AI / 全員に効くこと の3つを同じ形で開く。
    右は1枚しかないので、器を分けると同じものを3つ作ることになる
  - 違うのは4つだけ — **統括AIは止められない**（会社に1人）/
    **まだいない人は読むだけ**（約束・守ること ＋ 採用する。設定するものがまだ無い）/
    **全員に効くことは人ではない**のでモデルも深さも一時停止も持たない /
    スキルは「この社員の」と「会社ぜんぶの」に分かれる（`employee_id` が null なら共通）
  - `?open=exec` / `?open=all` / `?open=<社員id>`。**開いている1件は URL に持つ**
- **メンバーの右ペイン＝「AI社員の設定」**（→ `docs/design/03-agent-schema.md`）
  - **スキル ＝ SKILL.md のファイル管理**（参考: Base44 の Knowledge files）。
    **⬆ 読み込む / ＋ 新しく書く**、行ごとに **有効トグル・⬇ダウンロード・✏編集・🗑削除**。
    **行の先頭にアイコンは置かない**（ここにはスキルしか並ばない）。
    有効かどうかは**青のトグル**で示す（「有効」と文字で書かない）。
    **追加はセクション見出しの右上**（スキル ＋追加 / ルール ＋追加）。下に点線の行を置かない
    点線のドロップ領域に `.md` / `.zip` を落として**何個でも足せる**。
    「この社員のスキル」と「会社ぜんぶのスキル」に分かれる（`employee_id` が null なら会社共通）
  - **ルール** 日本語で1行ずつ追記・削除。定義側の Critical Rules は消せない
  - スキル＝**必要なときだけ**読む手順書 / ルール＝**毎回**効く制約。
    Claude Code の Skills と CLAUDE.md の関係と同じ
  - **モデル**：`ModelInline`。**社長が選ぶ**（2026-08-25）— Claude 3枚（Opus 5 /
    Sonnet 5 / Haiku 4.5）と OpenAI 3枚（GPT-5.6 Sol / Terra / Luna）。
    一覧は `lib/ai/catalog.ts` の1枚だけ（画面も実行も原価もここを見る）。
    見出しで会社を分け、右に**1語だけ**（速い / ふだん / じっくり）。**値段は出さない**。
    **Thinking 版を一覧に並べない**（それは深さの仕事。`gpt-5.6-luna-pro` は Luna の
    thinking 版なので載せない）。**「エコ」という言い方はしない**
  - **選んだものは本当にその往復に届く**（`RunInput.model`）。社員はタスクの実行、
    統括AIは計画・判断・会話。`runs.model` にも残るので、あとから何で走ったか分かる
  - **思考の深さ**：粒とつまみだけ（`EffortInline`。参考: Claude Code のエフォート）。
    **thinking の量**を決める。いちばん左が「考えずに答える」、右へ行くほど深く考える。
    **モデルは変わらない** — `tierFor(effort)` にモデルを選ばせない
  - **段はモデルが決める。** 受ける段はモデルごとに違う（Claude に `none` は無い／
    **Haiku 4.5 は深さを受けない**）。**受けない段を出さない** — 選んだ瞬間に
    その往復ごと上流で弾かれる。1段も無いモデルでは**つまみごと出さない**
  - **深さの言葉と段は1対1**（`EFFORT_WORD`）。前は段が5つ・言葉が6つで、
    つまみの位置がどの段を指すのか誰にも言えなかった
  - **統括AIの深さが効くのは計画と判断のとき。会話の返事はいつも速く返す**
    （社長が「遅い」と言ったのはここ。相談のたびに深く考えると、会話として成立しない）
  - **まだ選んでいない人は既定の姿を出す**（`DEFAULT_PREF`）。実行もその既定で走るので、
    画面と実物が食い違わない（「自動」のような、どこにも無い言葉を出さない）
  - **モデルと深さを同じプルダウンに入れない。** 別々の操作にして縦に積む。
    横に並べると対等な2つに見えて、「深さはモデルの中の話」という入れ子が消える
  - **保存ボタンを置かない。** トグルもスライダーも**切り替えたその場で効く**
    （参考: Linear / Notion の設定）。「一時停止」は保存ではないので、設定の最後の行に置く
  - **一時停止は本物**（2026-08-25）。止めているあいだ、その社員の**新しいタスクは起こされない**
    （走っている最中のものは最後までやる — 途中で切ると書きかけの成果物が宙に浮く）。
    一覧の状態も「一時停止」に変わる（止めたことを画面が隠さない）。
    **`employees.status` には持たせない** — あの列は実行が running / idle と書き換えるので、
    止めた印が次の実行で消える。設定なので `agent_prefs`（0025）に置く
  - **道具（capabilities）は社長に触らせない**。必要なら統括AIが提案し、採用と同じ確認を挟む
- **計画の承認は「承認するのに要る材料」だけ**（参考: AWS Amplify / Workable の Review）
  - **計画は表ではなく図。** 10週の軸に4フェーズを帯で置く（`pbar`）。
    **帯の中に文字を書かない**（左の列が名前とねらいを言っている）。担当と週数は右の列
  - **「あなたに聞くこと」は ◆ として軸の上に立てる。** 橙の菱形＋その場に小さいラベル
    （`価格の方向性` / `MVPの線引き`）。ワークフローの地図と同じ読み方。
    見出しの右に `およそ 10週 · あなたに聞くのは ◆ の2回`
  - **採用する社員は粒子のアバターで見せる**（26px＋名前）。「3体」と書くだけにしない
  - 中央: 統括AIの一言 → **計画（図）** → **承認すると起きること** → **作るもの**（6件）→
    `承認して始める` `直したい`
  - **カードの入れ子をやめる。** ヘアラインだけ。フェーズの色帯（AI社員の色）もリストから外す
  - **右ペインは「この計画の根拠」**。**時間の使い方**（10週の内訳バー＋「確かめることに半分」）/
    なぜこの順番か / 前提にしていること / 見送った案。
    以前はロードマップを右に置いていたが、中央の表と同じことを二度言っていた
  - **行動の行は入力欄に隠さない**（`COMPOSER_H` ぶん上に置く）
- **採用は日本語で「どんなAIか」が分かる形**。大きい日本語名＋1行の説明＋できることタグ＋
  この Work での担当。英語名は副次的に小さく
- **トークンはふだんの画面に出さない**。左レールの残高、進捗の消費グラフ、採用の想定トークン、
  計画のフェーズ別見積り — 全部やめた。出すのは請求・プラン画面と、枠に当たって止まったときだけ
- **操作説明のコピーを置かない**。「ドラッグで移動できます」の類は書かず、デザインで分からせる
- **右上に日付や時刻を出さない**。OS が出しているものを二度言わない
- **左レールは行き先だけ**（ホーム / 通知 / Work / タスク / 成果物 / メンバー / 決定事項 の7つ）。
  **採用はレールに置かない** — メンバー画面からの操作。絞り込みや操作をレールに並べない
  - **採用ページ（`/hire`）は畳んだ**（2026-08-24）。採用はメンバー画面の中で終わる —
    在籍のあとに**まだいない人を暗く並べ、右に `採用する`**。押せばその場で採れる。
    見出しの右の `＋ 採用する` は置かない（同じ一覧を2画面で持たない）
  - **レール上部＝プロダクトの名前（OneFound）と、このレールを閉じる印だけ。**
    **窓の印（丸3つ）は置かない** — ここは OS の窓ではないので、真似をしない。
    開け閉めは**器の中にある**
    （ChatGPT / Linear と同じ）。**閉じたら左の端に何も残さない。**
    戻り道はトップバー左端（右ペインと同じ作法）
  - **会社名はパンくずの根**（`会社名 / タスク`）。
    いま見ているものは全部この会社のものなので、レールの上ではなく**行き先の親**として置く。
    レールを閉じても消えないのも利点
  - **会社は1つだけ**（2026-08-24 の判断 → `docs/PLAN.md` 見送りの台帳）。
    切り替える先も増やす道も無いので、**押せる顔をしない** — 素の文字にする。
    前は ⌄ ＋ 一覧 ＋「会社を追加」を出していたが、どれも押しても何も起きなかった。
    「押せる以上、複数社は本物にする」の**裏返しで、本物にしないなら押させない**
    - 入れるときは `memberships` ＋ `current_account_id()` の書き換えを認証と一緒に。
      **ポリシー28本は無変更**のまま入る設計（`account_id = private.current_account_id()` の一形）
    - そのとき **トライアル枠は人に1回。2社目からは有料**（会社を作り直すだけで枠が湧かないように）
  - **左下のアバター＝わたし**（`Y あなた ⌄`）。設定 / 請求 / ログアウト。
    会社の話と自分の話を同じメニューに混ぜない（上は「いまどの会社にいるか」、下は「わたし」）
  - **絵と役目を合わせる。** 左向きの板アイコンは左レール、右向きは右ペイン。
    左を操作する絵で右を動かさない
- **言葉は短く、全画面で同じ語を使う**（長い言い回しはそれだけでAIっぽい）
  - 状態は **判断待ち / 要確認 / 実行中 / 待機 / 完了 / 承認済** の6語だけ。
    **この6語の外に新しい言い方を作らない。**「あなた待ち」「レビュー待ち」「読み込み済み」は却下。
    2つまとめて言いたいときも `判断待ちが 1件、要確認が 1件` と並べる（まとめる語を発明しない）
  - **通知の種類は状態とは別の語彙**（判断待ち / 要確認 / **エラー**）。
    状態は「タスクや Work がいまどうなっているか」、通知の種類は「何が起きたか」。
    エラーで止まったら **エラー** と書く（`止まっている` のような言い方を発明しない）
  - 遅れているかどうかは **順調 / 遅れ N日** の2語。赤は遅れ
  - **判断待ち** = あなたが決める　/　**要確認** = あなたが成果物を見る。この2つだけが橙
  - **AI社員の名前は「◯◯担当」の4文字に揃える** — 調査担当 / 戦略担当 / 企画担当 /
    開発担当 / 執筆担当 / 品質担当 / 分析担当。
    **ダミーの在籍は4人**（調査・戦略・企画・開発）。執筆担当は統括AIの採用提案、
    品質担当と分析担当は採用ページの候補。**全画面でこの名簿を守る**
    フェーズ名（調査・戦略・プロダクト・ローンチ）と見分けるために「担当」を付ける。
    表・レーン・オフィスの図で幅が揃い、担当の列が狭くて済む
- **仕事の入れ物は2つ**（→ `docs/design/06-work-and-scope.md`）
  - **フェーズ** = 進行中 Work の一区切り　/　**Work** = 終わりのあるまとまった仕事（承認が要る）
  - **用事は廃止した**（→ 06 の判断ログ）。小さい頼みごとは**いまある Work の中のタスク**になる。
    合う Work が1つも無いなら、小さくてもそれは Work（「ロゴを作りたい」は3条件を満たす）。
    やめた理由は、覚える言葉が増えること／全画面に例外が付くこと／
    `tasks.work_id` を nullable にしてまで守る価値が無かったこと
  - **すべてのタスクは Work に属する**（`tasks.work_id` NOT NULL）
  - Work の粒度は中身では決まらない。**その先に何を期待しているか**で決まる（LPは終着点にも通過点にも）
  - 判定条件3つ（終わりが言える / 単独で価値がある / 長すぎない）。全部そろえば Work
  - 統括AIが先に判定して**2択で提案**。「これは Work ですか？」とは聞かない。
    タスクで済むなら聞かず、どの Work に入れたかだけ言って実行する
  - **入れ子にしない**（Work→フェーズ→タスクの3段固定）
  - 間違えても**昇格・降格**で移せる（タスク→Work、フェーズ⇄Work）
- **Phase 3（実装の土台）完了** — Next.js 16 + OpenNext で Cloudflare Workers（workerd）の上で動く。
  Supabase プロジェクト `onefound`（ap-northeast-1）に 27表 ＋ RLS。**スキーマ由来の警告は 0件**
  （2026-08-25 現在、残っている1件は `Leaked password protection` ＝ Supabase の設定・Pro プラン。
  **コードで直せるものではないので、直したふりをしない** → `docs/RUNNING.md`）。
  `fast / standard / deep` の provider adapter 入り。
  **通り道は OpenRouter**（2026-08 に方針を変えた → `docs/design/05-tech-and-cost.md` 判断ログ）。
  直つなぎも残してあるので、`TIER_TABLE` の `vendor` を書き換えれば戻る
  - 不変条件はデータベース側で守る（→ `docs/RUNNING.md` の表）。
    RLS の裏方は `private` スキーマ。PostgREST に出さない
  - **会社は最初から分離済み**。全ポリシーが `account_id = private.current_account_id()` の一形なので、
    複数社に開くとき**ポリシーは1行も書き換えない**（→ Phase 11）
  - 残り: マジックリンクを実際に踏むところだけ（ユーザーの受信箱が要る）
- **タスク・通知・メンバーの3枚は作り直し済み**（2026-08-23 · 設計 → `design/pages/README.md`）。
  「表示している内容は違うのに、表示の仕方が似たりよったり」だったのを、
  それぞれの画面が答えるべき問いに合わせて器を変えた。**歯車のアイコンも作り直した** —
  丸＋放射線8本は 15px では明るさに見えるので、歯6枚・谷の深い閉じた輪郭にした
- **Phase 4（ダミーデータで全画面）実装済み** — 静止画23枚が動く Next.js の画面になった。
  - ~~`lib/dummy/index.ts` が唯一のデータの出どころ~~ → **ダミーデータは撤去した**（2026-08-24・下の「ゼロ状態」）。
    画面が読むのは store だけ。型と UI の定数は `lib/view/model.ts` に残る
  - `components/shell/`（レール・トップバー・入力欄・右ペイン・質問の板・スライダー）
    `components/home/`（オフィス / デスク / ワークフロー）`components/ui/`（アイコン・粒子アバター）
  - **粒子アバター（Orb）は TSX に移植済み。** 40px 未満は球をやめて同心リングと芯だけ
  - **B群の宿題**: 統括AIの3状態（`ExecStatus`）/ 右ペインの3状態（`PaneEmpty` `PaneLoading` `PaneError`）/
    エラー画面（`app/error.tsx` `not-found.tsx`）/ 会話履歴の分離（レール下段＋Work は会話を持たない）
  - `npm run dev:demo` / `npm run start:demo` / `wrangler dev --env preview` でログインを通さず全画面が触れる。
    **`.env*` には書かない** — OpenNext が既定値としてビルド成果物に焼き込み、本番にも付いていく
  - **押しても何も起きない。** 書き込みはどこにも届かない（Phase 5 以降）
  - **会話の中身はスレッドごとに持つ**（`CHATS`）。3本が同じ会話だと履歴の意味がない。
    質問（`ask`）が無いスレッドもある — 板が出ないのが正しい状態
  - **開いた状態と閉じた状態を機械で確かめている**。`?open=` を URL に持っているので回せる。
    1408 / 1440 / 1920 の3幅 × 20画面 × 開閉で、切れる文字・画面外の文字・消える文字がゼロ
  - **中で送るところは縦（`.sy`）と横（`.sx`）を分ける。** 片方は必ず止める —
    ログは縦にだけ、社員の列は横にだけ動いてほしい。`overflow: auto` だけだと両方動く
  - **器を測るのは `lib/use-size.ts` の `useSize`**（`ResizeObserver`）。最初の1描画は 0 で、
    サーバーと同じものが出る（ずれた形が出てから直る、が起きない）
- **Phase 5（Work の作成）実装済み** — ゴールを書くと、統括AIが計画まで引く
  - `lib/exec/` — 憲法（`constitution.ts`）／ 道具（`tools.ts`）／ 1回まわす（`run.ts`）。
    **返事の文章は使わない。** 画面に出すものは全部 `tool_use` から取る
  - 道具は5つ — `decide_container`（入れ物の判定）`ask_end`（終わりが言えないとき）
    `ask`（1度に1〜4問。**回数は決めない**）`propose_hires` `draft_plan`。**1往復で全部受け取る**
  - **ロードマップは最後まで引くが、タスクは直近のフェーズだけ**（`first_phase_tasks`）。
    先を固定すると、前の結果で引き直せなくなる
  - `lib/store/` — 書き込み先。本番は Supabase（`works` は `plan_review`、
    フェーズは `planned`、タスクは `queued`。**状態を先に進めない** — それは Phase 6）。
    出られない環境ではメモリ
  - **鍵が無いときは決め打ちのプロバイダ**（`lib/ai/fake.ts`）。本物と同じ道具・同じ道を通すので、
    パースとDB書き込みの穴はこれで見つかる。**画面に「これは仮の計画です」と必ず出す**
  - 計画の承認の画面は**1つ**（`PlanView`）。ダミーでも本物でも同じ画面が出る
  - 入口: `/start` と `/work/new` の入力欄が統括AIに繋がった（Case A）。
    Case B（`/discovery`）と Case D（`/import`）も接続済み（2026-08-24 → 下の「入口の接続」）
- **Phase 6（計画の承認）実装済み** — 承認を押すと状態が本当に変わる
  - `approveWork` / `reviseWork` / `getWork`（`app/actions/work.ts`）。
    承認: works → `active`、最初のフェーズ → `active`、提案した社員を採用、
    そのフェーズのタスクに担当を割り当てる。**タスクは `queued` のまま**
    （走らせるのは Phase 7。ここで `running` にすると、動いていないのに動いていることになる）
  - **Work 画面が本物を読む**（`lib/exec/work-view.ts`）。`PlanView` と同じ作法で、
    読む形は1つ（`WorkView`）・アダプタが2つ（`fromDummy` / `fromLive`）。
    **無いものは無いと出す** — 成果物も決定も日付もまだ無いので、節ごと空状態にする。
    進捗は 0 のまま（`queued` を進捗に化けさせない）
  - **計画の根拠のペインもダミーを捨てた。** 前は本物の計画でも
    「韓国の日本語学習者 約12万人」が出ていた。統括AIが言っていない節は出さない
  - **本番でだけ落ちる穴を4つ塞いだ。** この環境にはセッションが無いので、
    **`authenticated` の役で本物のDBに1本通して**見つけた
    （最後に例外を投げてロールバックするので、1行も残らない）
    - **全表の `account_id` に既定値が無かった** → `0007_account_default.sql` で
      22表に `default private.current_account_id()`。**ポリシーは1行も書き換えていない**
    - **`questions.thread_id` が NOT NULL** → Work のスレッドを先に作る（`threadFor`）。
      Work は会話を持たないが、相談の行き先は持つ
    - **`audit_events` に insert の権限が無かった**（裏方の表なので正しい）→
      `works` の引き金にした（`0008_works_audit.sql`）。
      **`public` に SECURITY DEFINER の関数を置く案は撤回した** — `rpc` から叩けて
      リンターの警告が1件増える。**セキュリティ警告0件は守る**。
      引き金なら、アプリが呼び忘れることも嘘の数を書くこともできない
    - **同じ定義の社員を Work ごとに採用していた** → 定義 id で引き当てて使い回す
  - **まだ繋がっていない**: 承認した Work は URL で開けるが、
    ホームのビューと左レールの一覧は**まだダミー**（Phase 7 で実行と一緒に繋ぐ）
- **Phase 0〜6 の棚卸し（2026-08-23）** — 通しで見直して17件直した
  - **`npm run lint` が壊れていた**（Next 16 で `next lint` が消えていた）。
    6,000行が一度も検査されていなかった → `eslint.config.mjs` を置いた。
    見るのは3つだけ（未使用 / hooks の依存 / a11y）。**通らない規則を並べない**
  - **質問の板は飾りだった。** 選択肢も ‹ › も自由入力もスキップも、押しても何も起きず、
    `answerQuestion` は誰も呼んでいなかった。統括AIの `ask` は DB に書かれ、読み戻され、
    `fromDraft` で落ちていた → **口を4つ**（`onPick` / `onFree` / `onSkip` / `onMove`）
    付けて計画画面に載せた。**答えは板が覚えない**（親が持つ）ので、読み直しても残る
  - **深さでモデルを切り替えていた**（`tierFromEffort`）。決めごとと逆のものが、
    使われないまま残っていた → 消して、`RunInput` に `effort` を通した
  - **並びが不定だった。** `questions` と `tasks` は1本の insert 文で入るので
    `created_at` が全行同着になり、`answer(work, index)` が別の質問に書き込みうる
    → `seq`（0009）
  - **`ownerHint` を集めて捨てていた** → `tasks.owner_hint`（0010）に残し、承認のとき名前で引き当てる
  - **生の DB エラー文が画面に出ていた** → `sayError`。中身はログへ
  - **`stopReason` を誰も見ていなかった** → 枠に当たったのか断られたのかを出す。
    `maxTokens` も 4,000 → 16,000（道具5つを1往復で書かせるので足りない）
  - **色が3か所にあった。** 22ファイルが同じ値をベタ書きしていた
    → `lib/design/tokens.ts` 一本化（生の色 470→65、CSS変数 22→5）。
    設計書に無かった灰色にも名前を付けた（`SUNK` `WELL` `SEAM` `RULE` `EDGE` `FAINT` `MUTE`）。
    **値は寄せていない** — 近い2つを1つにするかは、いつでも決められる
  - **フェーズの `review` がDBに無かった**（設計にはあった）→ 0011。Phase 9 の差し戻しが乗る場所
  - 死んだコード（`ModelPick` `EffortPick` `EffortSlider` `NOTICE_GROUPS` `PaneNote`）を消し、
    `say5` の「Phase 5 から」を実際のフェーズに直し、ログイン画面の球を本物の `Orb` にした
- **ロードマップを引く力を上げた（2026-08-26）** — 社長の指示
  「目的伝えた際にAIがロードマップを完璧に作れるようにして欲しい」。**4つ直した**
  - **理由が1行も書かれていなかった。** 計画の右ペインは「なぜこの順番か / 前提にしている
    こと / 見送った案」を出す作りなのに、`draft_plan` はそのどれも出させておらず、
    画面には**どの Work でも同じ3行**の決まり文句が出ていた（しかも1行は仕組みの説明）。
    **社長は根拠ゼロのロードマップを承認していた** → `why` / `assumes` / `dropped` /
    `time_note` を道具に足し、憲法にも「なぜこの順番かを必ず書く」を入れた。
    **統括AIが言っていない節は出さない**は変えない（空なら節ごと消える）
  - **引くときに会社のことを知らなかった。** `draftWork` に渡していたのは
    名簿 ＋ 条件 ＋ ゴールだけで、**承認済みの成果物も決めたことも渡していなかった**
    （AI社員の実行には渡していたのに）。3本目の Work でも1本目の結果を知らずに引いていた
  - **引いたあとの検査が2つだけだった**（空でないか / 担当が名簿にいるか）。
    憲法には「**着かない計画は、引き直します**」と書いてあるのに引き直させる仕掛けが無かった →
    `lib/exec/plan-check.ts`。**図（`draw_workflow`）の validate → 描き直しと同じ作法**で、
    辻褄（関門の行き先・成果物のフェーズ・週数の合計・理由の有無）を見て、
    **指したところだけ**渡して**1回だけ**引き直させる。**元より悪くしない**
    （引き直しが空なら元を使う）
  - **いちばん頭の良くないモデルが引いていた。** deep ＝ Luna（知能指数 52.3 で 5.6 一家の最下位）。
    **Luna Pro は単価がまったく同じ**（$0.2 / $1.2）で「同じ素体を `reasoning.mode = pro` で
    出したもの」→ **タダで上がるので上げた**。チャットの返事と AI社員の仕事は Luna のまま
  - **一覧の単価が3枚まちがっていた**（Sonnet 5 / Sol / Terra。最大3倍高く記帳していた）。
    OpenRouter の一覧を実際に引いて直した — **原価の記帳がずれると、残高がずれる**
  - **「Thinking 版を一覧に並べない」に例外を1つ作った**（Luna Pro）。
    あれは深さの設定ではなく**同じ値段の別の出し方**で、深さのつまみからは届かない。
    並べないと社長は一生選べない
  - **決め打ちのプロバイダは1回目をわざと壊す** — 週数を足すと12週で、全体の10週と食い違う。
    本物のモデルもここをよく外す（フェーズごとに書いたあと、合計を数え直さない）。
    行儀よく書くと、引き直しが動いているか永久に分からない
- **通り道は OpenRouter、モデルは GPT-5.6 Luna**
  （→ `docs/design/05-tech-and-cost.md` 判断ログ）
  - `lib/ai/openrouter.ts`。OpenAI 互換なので `openai` の SDK に行き先だけ差し替える
  - **直つなぎは残す。** `TIER_TABLE` の `vendor` を書き換えれば戻る
  - **モデルは3階層とも `openai/gpt-5.6-luna`**（2026-08-24 に社長が決めた。理由は速さ）。
    **階層の設計を捨てたのではない** — 「深さ＝thinking の量。モデルは変わらない」を
    素直に表した形で、違いは `reasoning_effort` で付く（fast/standard は low、計画は high）
  - **無料のテストモデル（Ox Alpha）は既定から外した。** 前は「本番以外は自動でこれ」で、
    表を書き換えても本番以外には効かず、**社長が選んだモデルで動いていなかった**
    （Ox Alpha は実測 p50 5秒・p90 35秒・p99 96秒 — 遅さの正体はこれ）。
    ただで回したいときだけ `OPENROUTER_FREE_TEST=1`
  - **引き受けた宿題**: 委託先の開示（`provider` の指定でルーティングを固定する。
    Phase 11 までに決める）／ **統括AIの計画（deep）も Luna になった** — 速さと安さのモデルなので
    計画の質は落ちうる。質が要るなら `openai/gpt-5.6-luna-pro`（同じ単価）に deep だけ差し替える
  - **この環境から `openrouter.ai` に出られない**（アプリの実行時）。slug は MCP で確かめた
    （実在・tools 対応・105万コンテキスト）が、キャッシュの透過と `usage` の中身は
    **実キーで確かめていない**。鍵が入ったら最初にそこを見る
- **Phase 7（モデル実行基盤）実装済み** — AI社員が1タスクを最後まで走る
  - `lib/run/worker.ts` — 社員の定義（`lib/roster/` の7人）を頭に載せ、道具4つで1往復。
    **1タスク=1往復**（途中の道具は全部「書き残す」系なので、往復する理由が無い。
    Web調査のような「読む」道具が入るときに初めてループが要る）
  - **ポンプ方式** — Work 画面を開いているあいだ、2.5秒ごとに読み直し、
    走っているタスクが無ければ次の queued を1つ起こす。**見ていないところで
    料金だけ増える、が起きない**。DO / キュー / Cron は Cloudflare に出るときに足す
  - **進捗は導出値のまま** — `run_steps.progress`（社員の自己申告）→ 引き金 →
    `tasks.progress`（0012）。アプリからは今も直接書けない
  - 成果物の本文は `deliverables.body`（R2 に出られるまでの置き場）。
    Work 画面の成果物を押すと右ペインで本文が読める。タスクを押すと歩みが流れる
  - 判断に当たったら `ask_decision` で止まる（task→needs_decision ＋ decisions ＋
    判断待ち通知）。**答える側は Phase 9**
  - 検査は `tools/check/run.mjs`（承認 → 自動実行 → 成果物、を1本で）
- **Phase 8（調査ワークフロー）実装済み** — 成果物 → レビュー → 承認 / 差し戻しの輪が回る
  - **統括AIのレビュー** — 成果物ができたら fast の目で1度見て、通知に一言添える
    （「どこを見ればいいか」。判定ではない。鍵が無い環境では黙って飛ばす — 偽のレビューを作らない）
  - **差し戻しは言葉で** — 成果物のペインに「直してほしい」。書いた指摘がそのまま
    直しタスク（`◯◯ を直す` / intent＝社長の指摘）になり、同じ担当に積まれてポンプが走らせる。
    review まで進んでいたフェーズは active に戻る
  - フェーズの全タスクが done → `review` ＋ 判断待ち通知（次に進める側は Phase 9）
  - 成果物画面が本物を読む（先頭に混ぜる。サムネイルは**実際の書き出し**。
    タブで開くと本文と承認/差し戻しの口）
  - Web検索は OpenRouter の web プラグイン（`OPENROUTER_WEB=1`）。**既定オフ** —
    従量課金なので、無料のテストを黙って有料にしない
- **Phase 9（判断と受け渡し）実装済み** — 決めたことが次の実行に必ず入る
  - **判断のUI** — 判断待ちのタスクを開くと右ペインに選択肢（決定事項の台帳でも
    その場で決められる）。決めると decisions → decided、タスクは queued に戻る
  - **受け渡し** — 次の実行の依頼文に「決めたこと（社長の決定。これに沿う）」が必ず入り、
    **読了は decision_refs に残る**（二重に読んでも1行）。
    E2E は「決定が無いと fake が完走しない」形にして、渡ったこと自体を検査
  - **フェーズの承認** — review になると Work 画面に行動の帯。押すと統括AIが
    前の成果物と決定を見て**次のフェーズのタスクを引いてから**進む
    （`lib/exec/next.ts`。先を固定しなかった計画の、引き直しの本体）。
    最後のフェーズなら Work → done
  - **見つけた穴: decisions が消せた。** 追記のみの引き金は UPDATE しか見ておらず、
    DELETE が素通りだった → `0013` で revoke（探針で消せることを実証してから塞いだ）
- **Phase 10（社員を増やす）実装済み** — 採用が本当に効く
  - 採用ページとメンバー画面の「採用する」が employees に書く。
    **候補の id ではなくロスターの定義で採る**（`lib/roster/`）。同じ定義は使い回す —
    二度押しても、Work の承認と重なっても、調査担当が2人にならない
  - メンバー画面にダミーの4人のあと**本物の在籍**が並ぶ（約束・できることは定義文から）
  - 担当ごとの実行は Phase 7 から効いている（owner_hint 引き当て・定義文が頭に載る）
  - ホームのビューの実データ接続は「ゼロ状態」で済ませた（↓）
- **Phase 11（課金）は骨格まで** — 記帳と上限は動く。決済と公開は鍵待ち
  - **実行の原価は必ず記帳される** — 引き金 `run_ledger`（0014）。runs が閉じると
    `token_ledger` に負の1行。アプリは台帳に書けない（残高の偽造を防ぐ。探針で実証）
  - **残高が尽きたら Work ごと paused に落ちる**（設計の言葉どおり「paused = 予算上限」）。
    ポーリングのたびに通知が積もらない。エラー通知は「枠に当たって止まりました」
  - **タダの実行は 0 で記帳する**（`billedCostUsd`）— 表のモデルで走ったときだけ定価で数える。
    無料のテストモデルに定価を記帳すると、タダの実行がトライアル残高を減らす
  - **請求とプラン（/billing）** — トークンの数字を出していいのはこの画面だけ。
    残高・台帳・プラン。デモ（メモリ）では「上限なし」と正直に出す
  - **まだ**: Stripe（鍵が無い）／ CF 公開（`CLOUDFLARE_API_TOKEN`）／ 複数社（memberships）
- **再点検を2回した（2026-08-24）** — 1回目は**同時操作の穴**: 実行の二重起動
  （`startRun` を atomic claim に）／ 差し戻し・フェーズ承認の二度押し（review のものだけ動く）／
  朝の報告と採用の一意性（0015 の一意 index。探針で実証）／ 失敗した実行の記帳（0にしない）。
  2回目は**回復性**: サーバーが途中で入れ替わると running が残ってポンプが永久に止まる →
  `reclaimStalled`（10分で失効。1回だけ積み直し、二度目は blocked ＋ エラー通知）／
  FK の覆い index 31本（0016。ポンプが2.5秒ごとに読む表）
- **ゼロ状態（2026-08-24）** — **ダミーデータを消して、空の会社から始まる**ようにした
  - `lib/dummy/` は削除。**画面はぜんぶ store を読む**（読みの口 → `lib/store/types.ts` の
    「ゼロ状態」節: listWorks / listNotes / listThreads / listSkills / companyName / recentSteps）。
    デモ（メモリ）も本番（Supabase）も空から始まり、`/start` にゴールを書くと埋まっていく
  - **ホームのビューが実データ**になった。live → 絵の形の変換は `lib/live/home.ts` が唯一の変換点。
    週数（`plan_draft`）から x/w、タスクから弧と進捗、needs_decision から ◆。
    **無いものは無いと出す** — 週数の無い計画に日付を書かない、進捗 0 の輪に弧を引かない。
    Work が1つも無ければ `/home` は `/start` へ（空の盤面を見せない）
  - **空状態は全画面に**。本物の文字はダミーより長いので、通知の左列は
    **切らずに折り返す**（機械の検査が「…で切れ」を数えるのはそのため）
  - **チャットの返事が本物になった** — `sendChat` が fast の1往復で統括AIの返事まで書く
    （文脈: 憲法＋会社のいま＋スレッドの直近10発言）。鍵の無い環境は**「仮の返事」と名乗って**返す
  - **スキルも本物** — `agent_skills` に読み込み・トグル・削除が効く（SKILL.md のドロップで保存される）
  - 採用の候補＝**ロスターの定義 − いまの在籍**（ダミーの候補カードをやめた）。
    2026-08-24 に採用ページごとメンバー画面へ畳んだ（上の「メンバー」）
  - 検査も作り直した — layout.mjs は**空の会社の17画面**、run.mjs は E2E 13項目のあとに
    **埋まった状態の9画面**のレイアウトを測る（ダミーを消したので、ここでしか測れない）
- **標準スキルと学び（2026-08-24）** — スキルが本当に仕事に効くようになった
  - **標準スキル（builtin）はユーザーに見せる**（元々の機能。何を読んで働いたかが見えないと
    成果物の質を判断できない）。行に「標準」タグ。**切れるが消せない**（ロスターの
    Critical Rules と同じ思想）。1枚も無ければ `lib/roster/skills.ts` の3枚を播く
    （調査のまとめ方 / 価格の決め方 / 日本語の書き方。播種の一意性は 0017 の index）
  - **実行がスキルを読む** — 1タスク=1往復なので「途中で取りに行く」はできない。
    有効な会社スキルから最大2枚だけ依頼文に載せ、載せたら `used_count` を進める（読んだ印）
  - **学び（learned）** — 社員は実行の終わりに `note_learning`（任意）で1行書き残せる。
    1人1枚（learnings.md・上限30行）に溜まり、**次の実行の依頼文に最新10行**が載る。
    設定ペインの「学び」欄で**見える・消せる**。**ルールには自動で書かない** —
    ルールは毎回効く制約なので、勝手に増えると社長が知らないうちに社員が変わる。
    昇格はいつか社長の操作で（タスク→Work の昇格と同じ語彙）
  - ついでに見つけた双子の穴: **メモリ版の承認が採用していなかった**（crew が合成 id で、
    在籍と食い違う — 学びが「書いた人」と「見る人」で別人になる）→ supabase 版と同じく
    定義で引き当てて staff に入れる
- **会社の記憶と版（2026-08-24）** — 会社が覚え、社員が学び、成果物に版がついた
  - 実行の依頼文に**会社で承認された成果物の索引**（最新5件・タイトルだけ）が載る。
    チャットの文脈に**決めたこと**（最新5件）が載る。二度調べ直さない
  - **差し戻しの指摘は担当の学びに残る**（`社長からの差し戻し（◯◯）: …`）。
    直しタスクは1回で消えるが、指摘は次の仕事でも効く
  - **成果物の版** — 同じ Work で同じタイトルは同じ lineage の新しい版（v2〜だけ表示。
    旧版は superseded で一覧から隠す）。直しタスクには「タイトルはそのまま」と指示する。
    器（lineage_id / version / superseded）は 0001 のスキーマが最初から持っていた
  - **runs.model** — どのモデルで走ったかを記録（0018）。ガバナンスの土台。
    委託先の開示（provider 固定）は鍵が入ってから
  - **⌘K は会社への口** — 検索に当たらなくても、いちばん下の「統括AIに頼む」で
    そのまま会話が開く（種類は「頼む」。検索の結果と取り違えない）
  - 機能候補12件の判断は `docs/PLAN.md` の「見送りの台帳」。
    **外に出る道具（メール・公開・支払い）は Approval 必須**の原則も PLAN の守るルールに固定
- **朝の報告** — 器（Shell）が開いたとき1回だけ `morning()` を呼び、
  **その日はじめて開いた朝に、きのうの動きを1通**にする（`morningBrief`。日付で重複を止める）。
  動きが無かった朝は黙る（空の報告は報告ではない）。聞かれる前に会社のほうから言う —
  チャットボット（Grok 等）との違いはここ
- **入口の接続（2026-08-24）** — Case B / D が統括AIに繋がった（`lib/exec/entry.ts`・道具6つ・deep 1往復）
  - ~~`/discovery` `/import` `/diagnosis` の専用画面~~ → **チャットに一本化した**
    （2026-08-24 の作り直し。下の「チャットが入口」）。以下は移す前の記録
  - **Case B** — `/discovery` は条件を**構造で**集める（`set_conditions` が言われたことを写し、
    足りなければ質問の板）。**2つそろったら候補3つ**（`propose_candidates`。全部そろうまで待たない —
    足りない分は仮に置いて summary にそう書かせる）。fit は3スコア・推し1つ・**推さない2つには理由**。
    `/discovery/result` で比較 → 採用でゴールを組んで Case A と同じ道（計画→承認）へ。
    **候補は消さない**（不変条件9。出し直しても前の束は残り、画面が読むのは最新の束だけ）
  - **Case D** — `/import` は URL（中身はまだ読めない＝**待機**のまま。読めないものを読めたと言わない）、
    文字ファイル（.md .txt .csv は**その場で読んで** done）、手書きの数字（入力欄 → doc）を保存。
    診断は `describe_business` / `report_facts`（**測れていない数字は missing で出す** — それ自体が診断）/
    `report_diagnosis`（**1件ごとに提案 Work 必須** — 問題を並べて終わりにしない）。
    `/diagnosis` から1件ずつ Work に。空の診断画面は見せない（診断が無ければ `/import` へ戻す）
  - 0019（diagnoses.facts / is_real ×2表）・0020（candidates.why）。ポリシーは無変更・警告0のまま
  - FakeProvider が同じ道具で決め打ちを返し、画面は「これは仮の◯◯です」と名乗る。
    E2E 7項目（板→候補→計画 / 取り込み→診断→計画）は fake で完走する
  - **敵対レビューで18件（反証0件）。全部直した**（→ `docs/PLAN.md` 入口の再点検）
    - **不変条件9の抜け道**: 候補を revoke しても**親の探索が消せた**（cascade）→ 0021。0013 と同型
    - **条件を変えるが跳ね返っていた** → `?edit=1` で留まる（候補を出し直す前提が切れていた）
    - **切れた1往復が無言の空振り** → `checkStop`（max_tokens / content_filter）＋ 16000。
      道具が1つも来なかった往復もエラーにする（押しても何も起きない、を作らない）
    - **同時に足すと事業が複数できた**（ドロップ2枚 = profile 2つ）→ 直列化 ＋ **catch**
      （catch が無いと、1回の失敗で列が永久に詰まって以後の取り込みが黙って消える）
    - **`?s=` / `?p=` を信じない** — 消えていたら作り直す（deep 1往復ぶん払ってから捨てない）
    - **診断→Work は1回だけ**（`linkFinding`。候補の `adopted_work_id` と同じ守り）
    - **NaN を止める**（`週NaN時間` がゴール文に焼き込まれる）／ **`ask` を正規化**
      （options が落ちた質問は板で落ちる。Phase 5 の道も同じ穴だったので一緒に直した）
    - デザイン言語5件 — 診断の重さは**明るさで言う**（赤橙は 停止 / あなたの番 の色）／
      **0か100しか取らない進捗バーは進捗ではない**（右の状態の語と二度言い）／
      操作説明コピー2本 ／ `条件 N / 5`（5は項目数で目標ではない。本文と食い違う）／
      条件の語は `lib/live/conditions.ts` 1か所（隣り合う2画面で言い換えていた）
- **チャットが入口（2026-08-24）** — **会話は1か所。入口も相談もチャットで起きる**
  - **入口の3つとも、新しいチャットを1本作る**（`/start` の入力欄・「まだ決まっていない」・
    「すでに事業がある」）。**別の画面に飛ばさない** — `/discovery` `/discovery/result`
    `/import` `/diagnosis` `/work/new` は撤去した（会話が2か所にあると、あとから探せない）
  - **統括AIは道具を持つ**（`lib/exec/chat.ts`・8つ）。返事は文章が主役で、
    そこに**カード**が付く — 質問 / 候補3つ / 診断 / Work を作る確認
  - **カードは id しか持たない。** 中身は描くときに store から読むので、
    「もう採用した」「もう Work にした」が古いまま残らない
  - **動くのはいちばん新しいカードだけ。** 会話が先に進んだら、古いカードは読むだけになる
    （参考: ChatGPT / Claude）。選択肢を押すと「質問 → 答え」の形で会話に入り、そのまま続く
  - **1チャット = 1 Work**（`chat_threads.work_id`）。**Work は勝手に作らない** —
    `propose_work` は提案で、社長が「この Work を作る」を押したときだけできる。
    要らない話（雑談・調べもの）には提案しない。二度目は `linkThread` が最後に止める
  - スレッドが**集めた条件と取り込んだ事業を覚える**（0022 の `discovery_id` / `profile_id`）。
    会話の途中で条件を足せる
  - 見出しは16文字（左レールで切れない長さ）。入口から始めたものは
    「何をやるか決める」「事業を見てもらう」と短く名付ける
  - **右ペインは出さない**（チャットは会話そのものが主役 — 元からの決めごとどおり）
- **触ってみて出た6件を直した（2026-08-24）** — 社長が実際に押して見つけたもの
  - **入口のボタンが「押しても何も起きない」に見えた** → 送るのと返してもらうのを分けた
    （`chatSay` / `chatReply`）。押した瞬間に会話へ入り、そこで「考えています」が出る（107ms）
  - **会話のいちばん下が入力欄に隠れていた** → `COMPOSER_H` ぶん逃がし、
    **伸びたら貼り直す**（上を読んでいるあいだは動かさない）
  - **宛先の「新しいチャット」が開かなかった** → Work が無くても畳まない
  - **通知が0のときだけ別の画面になっていた** → 2列のまま言葉だけ変える
  - **メンバーに「まだいない人」を暗く並べた** → 採用ページ（`/hire`）は畳んだ。
    ついでに **別名で在籍と候補が二重に出ていた**（`revenue-strategist`）を `slugOf` で寄せた
  - **入力欄の帯が指を吸っていた**（124px あるのに逃がすのは 108px）→ 帯は指を通す。
    計画の行動の行は**板の実寸ぶん**逃げる（`onHeight`）
  - **レールの「すべて見る」が死んでいた** → ⌘K を開く（会話も並ぶ）
  - 検査も直した — 消えた画面の URL を捨て、**Work と会話の id は画面から拾う**
    （決め打ちの `w-japanese` が 404 を測って「通った」ことにしていた）。
    E2E に「会話は下に貼り付いていて、入力欄に隠れない」を足した
- **チャットをまともにする（2026-08-24）** — 社長が実キーで触って「まともにチャットすらできない」
  - **原因は「道具を呼ぶ往復に本文が来ない」**（→ 上の「会話はチャットに一本化する」）。
    OpenAI 互換のモデルは `tool_calls` を返すとき `content` を空にする。
    決め打ちのプロバイダは**本文も一緒に返していた**ので、この穴はデモでは出ない。
    決め打ちのほうを本物に合わせた（道具の往復では黙る）ので、E2E がこの道を通る
  - **返事が流れてくるようになった**（`/api/chat`）。10秒黙っていた画面が、1文字目から動く
  - **右ペインも同じ統括AI**（`sendChat` は消した）。カードは「◯◯があります · チャットで開く」
  - **本文はマークダウンの3つだけ受ける**（見出し `#` / 箇条書き `-` / 強調 `**`）。
    そのまま出すと記号が地の文に混ざる
  - **倒れたら理由と「もう一度」**（黙って終わらせない）
  - **モデル名の嘘を消した** — メンバーの行は `GPT-5.6 Luna`（実際に走っているモデル）
  - **＋ が本当に資料を読む**（`/import` を畳んでから、材料の入口が無くなっていた）。
    `.md .txt .csv .json` はその場で読んで会話に渡す（6,000字まで）。
    読めない形式は**読めないと言う**。ついでに「Phase 8 から」のような
    **もう過ぎたフェーズを約束する文**を消し、スキルの ＋追加 は `/skills` へ繋いだ
- **実キーで触って出た8件（2026-08-24）** — 統括AIが「ただのチャット」になっていた
  - **カードが出なくなっていた。** 前の直しで「道具は必要なときだけ」と緩めたら、
    質問も候補も出さずに文章だけ返すようになった。**逆に振り直した** —
    「社長に何かを尋ねるときは**必ず ask を使う**。本文で質問を並べない」を憲法級の決めごとにし、
    「何ができたら終わりですか？」も ask にする。
    さらに**入口の一言で始まった往復は道具を必ず1つ使わせる**（`toolChoice: 'required'`。
    OpenRouter / OpenAI / Anthropic の3つとも同じ意味で渡す）
  - **知らないことを聞いたまま提案していた** → **条件が3つそろうまで候補を出さない**。
    序盤ほど**押すだけで答えられるやさしい質問をたくさん**（1問1行・選択肢3〜4個）。
    「もういいから出して」と言われたときだけ、足りない分を仮に置いて出す
  - **「何ができたら終わりですか」が赤い1行だった**（`ask_end` は**選択肢まで持っている**のに
    本文だけをエラーの色で出して捨てていた）→ **選んで答えられる形**にした（`EndAsk`）。
    赤は止まっている印。質問に赤は使わない
  - **候補は3つとも選べる。** 推している1つだけが色と面を持っていて、残りが読み物に見えていた →
    **行そのものが選ぶもの**（指が乗れば光る）。推しの印は左の緑帯とタグだけ。
    3つとも同じ押しやすさにする
  - **選択肢に自由入力の行を入れた**（鉛筆＋その場で書く）。前は「下の入力欄に書いて」と
    書いてあるだけで、答えの続きなのに置き場所が離れていた
  - **答えた条件が1行につながって読めなかった** → 1組を2行にして組と組を空け、
    吹き出しを `pre-wrap` にした
  - **考えているあいだ「〇〇しています」を出し続ける**（参考: Claude）。
    道具の名前が引数より先に届くので、それを日本語1語に写して流す
    （`tool_begin` → `{"s":…}`）。**起きている事実だけ**を書く
  - **モデル名は聞かれたら答える。** 憲法に「聞かれたら隠さず答える」を足し、
    **統括AI自身に自分のモデル名を渡した**（知らなければ答えようがなく、規則に逃げるしかない）
  - **Work がまだ無い会社では、会話が左レールに出てこなかった**（`/start` にいるあいだ
    丸ごと「まだありません」にしていた）→ 会社が空かどうかと、会話があるかどうかは別のこと
- **実キーで触って出た8件（2026-08-24）** — 統括AIが「ただのチャット」になっていた
  - **カードが出なくなっていた。** 前の直しで「道具は必要なときだけ」と緩めたら、
    質問も候補も出さずに文章だけ返すようになった。**逆に振り直した** —
    「社長に何かを尋ねるときは**必ず ask を使う**。本文で質問を並べない」を憲法級の決めごとにし、
    さらに**入口の一言で始まった往復は道具を必ず1つ使わせる**（`toolChoice: 'required'`）
  - **答えたあとに止まっていた。** 条件を書き留めただけで往復が終わり、
    社長が「3案まだですか」と催促してやっと候補が出た → **止まらない仕掛け**
    （`lib/exec/reply.ts`）。道具は使ったのに**カードが1枚も出なかった**往復は、
    そのまま**もう1回だけ**走らせる（条件が足りなければ質問、そろっていれば候補）。
    決め打ちのプロバイダも本物と同じく「条件だけ書いて終わる」ようにしたので、E2E がこの道を通る
  - **考えている印が入力欄の裏に隠れていた** → **流れない場所**（入力欄のすぐ上）に貼る。
    考えているあいだ、ずっと見える
  - **質問カードの最後の1問だけ形が違った**（答えたのに選択肢が開いたまま残る）→
    送ったら**ぜんぶ同じチップ**になる（`答えました N / N`）
  - **候補は3つとも同じ見た目に。** 推している1件だけ緑の面と帯を持っていて、
    1つだけボタンのように見えていた → 面の色をやめ、印は**「おすすめ」の文字だけ**。
    3本のバーは**5つの刻み**にして、数えて読めるようにした
  - **Work は作る前に一度聞く**（`この案で Work を作りますか`）＋
    **作っているあいだの姿**（`「◯◯」の計画を引いています · 30秒ほどかかります`）
  - **空の計画を作らない** — フェーズ0・タスク0の計画案が実際にできて、
    「0フェーズで進めます。まず「」から —。」という壊れた画面になった。
    中身が空なら Work にせず、引き直せることを言う
  - **モデル名は聞かれたら答える。** 憲法に「聞かれたら隠さず答える」を足し、
    **統括AI自身に自分のモデル名を渡した**（知らなければ答えようがなく、規則に逃げるしかない）。
    ただし**通り道は言わない** — 聞かれているのはモデル名
- **読み込みを速くする（2026-08-24）** — 「全体的に、チャットを開くときとか遅い」
  - **往復の数を数えてから直す**（`tools/check/net.mjs`）。感想では直せない
  - **`prefetch` の付けすぎが原因**（↑「行き先は、指が乗ったときに取る」）。**21本 → 3本**
  - **器の中身は画面と一緒に届く** — レール（会話・未読・人数）と会社名は
    サーバー側（`app/(app)/layout.tsx`）で読んで渡す。前はクライアントが2往復していた
  - **会話もサーバーで組んでから渡す**（`app/(app)/chat/[id]/page.tsx` ＋ `components/chat/ChatView.tsx`）。
    開く → 器が立つ → もう1往復 → やっと会話、をやめた
  - **入力欄の宛先は、開いたときだけ取りに行く**（全画面で1本ぶん減る）
  - **朝の報告はその日いちど**（`sessionStorage`）。開くたびに往復していた
  - **`getThread` の2本を同時に投げる**。遠いDBでは、順に待つと往復が2回ぶん積み上がる
  - **Vercel は東京（`hnd1`）に置く**（`vercel.json`）。Supabase は ap-northeast-1 なので、
    米国リージョンから読むと1クエリごとに往復が乗る
- **候補が具体的になる・Work が本当に立つ（2026-08-24）** — 社長が実キーで通しで触った
  - **分野を先に聞く**（`Conditions.interests`）。前は時間・お金・避けたいことだけ聞いて
    候補を出していたので、「小さな実用品の販売所」「受注型テンプレート制作」のように
    **何に関する事業か誰にも分からない案**が出た。
    **分野が空のあいだは候補を出さない**（`canPropose`）。分野は選択肢＋自由入力で聞き、
    候補の名前は「学び・教えるのオンライン講座」のように**誰に何を**が分かる形にする
  - **完了の定義は候補が持つ**（`ending`。0023）。前は採用したあとに `ask_end` で
    聞き返していたので、社長には「選んだのに、また同じことを聞かれた」に見えた。
    いまは**候補を出すときに一緒に書かせて、選ぶ前に読める**。
    `ask_end` は**終点そのものが無い依頼**（「売上を伸ばしたい」）だけの最後の手段
  - **空の計画で終わらせない。** 1往復で道具を5つ書かせるのは速いモデルには重く、
    `draft_plan` が空で返っていた（「0フェーズで進めます」）。
    **空なら計画だけをもう一度頼む**（道具1つ・`toolChoice: 'required'`）。
    名前の無いフェーズ・題の無いタスクは通さない
- **チャットの完全構築（2026-08-24）** — 「全て含めて、完璧に構築して」
  - **会話の方針を作り直した**（`lib/exec/chat.ts` の GUIDE）
    - **分野を最初に選択肢で聞かない** — 分野は無数にあり、一覧から選ばせるのは無理がある。
      1巡目は**誰でも押すだけで答えられること**（時間 / お金 / 避けたいこと / 得意・経験）。
      2巡目で方向を聞くときは、**1巡目の答えから導いた、その人に合わせた選択肢**＋自由入力
    - **案を持って来た人の案を捨てない** — 絞れていれば確かめて propose_work、
      方向が複数あり得るなら**その案のバリエーション3つ**を propose_candidates で
  - **続きが必ず出る**（`lib/exec/reply.ts`）。探索中の統括AIの番は**必ずカードで終わる** —
    ①答え（`質問 → 答え`）が条件に写っていなければ**写す往復**（record）
    ②カードが無ければ**候補か質問**。push の往復は**その道具しか渡さない**
    （「必ず使え」と書くだけでは守られない。道具が1つなら出力は必ずその形になる）。
    探索の器は**入口で先に作る**（`chatStart`）— 前は最初の往復が条件を書いたときにしか
    できず、文章だけの返事のあと仕掛けが丸ごと眠っていた
  - **思考ログ**: モデルが思考を開示するとき（OpenRouter の reasoning delta）は
    `think` チャンクで流し、状態の帯に**実際の思考の断片**を1行出す（`{"th":…}`）。
    開示しないモデル（OpenAI 系）は「〇〇しています」（道具名から）だけ — 作り話をしない
  - **保存先が無いのに、あるふりをしない**（`storeWarning`）。Supabase 未設定のまま配ると
    書き込みは関数の一時メモリに入り、**関数が入れ替わるたびに消える**
    （会話の id が `t-1` ならこの状態 — 実際 Vercel がそうだった）。
    レール左下に赤1行「保存先が設定されていません」を出す。デモ（DEMO_MODE）は出さない
- **モデルと深さを社員ごとに選ぶ（2026-08-25）** — メンバー画面の飾りが本物になった
  - **一覧は1枚**（`lib/ai/catalog.ts`）— Claude 3枚 ＋ OpenAI 3枚。モデルごとに
    **受ける深さの段**と単価を持つ。画面のプルダウンも、実行に渡す名前も、原価の記帳も、ここだけを見る
  - **保存先は `agent_prefs`**（0024。`employee_id` が null なら統括AI ＝ `agent_skills` と同じ書き方）。
    **使われていなかった `employee_settings` は落とした**（0行・参照0か所。`model_fixed` が
    階層名に縛られていて、いま作るものとは別の設計だった）
  - **選んだものが本当に届く**（`RunInput.model` → 3つの通り道すべて）。
    社員の実行は担当の設定、統括AIの計画・判断・会話は統括AIの設定。`runs.model` にも残る
  - **原価もモデルの単価で数える**（`billedCostUsd(tier, in, out, model)`）。
    単価を知らないモデル（無料のテスト・env の差し替え）は 0 のまま
  - **オフィスのカードが正直になった** — 前は全員 `自動 · 深さ 2` と出ていた（どこにも無い言葉）
  - **入力欄の「自動」は外した**（押しても何も変わらなかった。深さはモデルの隣に置く）
  - **通り道での綴りは Luna だけが実測。** 残り5枚は各社の名前から組んだもの
    （この環境から `openrouter.ai` に出られない）。鍵が入ったら最初に一覧と突き合わせる
- **押しても効かないものを片づけた（2026-08-25）** — `dead.mjs` で13画面を1つずつ押して、
  残っていた「死」は**空のときの送信ボタンだけ**（＝押せなくて正しいもの。生 296 / 死 13）
  - **一時停止が本物になった**（↑ メンバーの右ペイン）
  - **「直したい」は説明ではなく入力欄へ連れていく**（`Composer` の `focusAt`）。
    「下の入力欄に書いてください」は操作説明のコピーだった
  - **トグルが自分で値を持っていた** — 保存に失敗して親が戻したときや、読み直して
    値が変わったときに**つまみだけ古いまま**残った。真実は親が持つ（`Toggle` は映すだけ）
  - **請求の画面にだけ入力欄が無かった** — 逃がす余白（`COMPOSER_H`）は取ってあるのに
    入力欄が無く、下に 108px の空きが残っていた
  - **右ペインの会話が下に貼り付いていなかった** — 画面のほうだけ直っていた。
    1つの決まりを2か所で使う（`lib/use-stick.ts`）
  - **同じ学びを二度書かない** — 似た仕事で同じことに気づくので、放っておくと
    30行が同じ1行で埋まる（実際、デモで3行とも同じ文になった）
- **入口の1枚を作り直した（2026-08-25）** — Google でも入れるようにし、画面を組み直した
  - 参考（Mobbin）: Modal / Cursor / TIDAL / Hex / Resend。**どれも同じ形**だった —
    小さな顔 → 名前 → **外の入り口（Google）を上に** → `または` → メール → 送る。
    早い道を上に置き、遅い道（受信箱を開きに行く）を下に置く
  - **青は1ペインに1つ。** 青いのは「リンクを送る」だけで、Google は枠だけのボタン。
    これは妥協ではなく、**Google 自身の暗い画面向けの作法**（暗い面 ＋ 枠 ＋ 4色の G）と同じ形。
    4色の G は**色も形も変えない**（この画面でここだけが会社の色から外れる）
  - **前回の入り方を覚える**（参考: Modal の "You last logged in with Google"）。
    ブラウザの中だけに持ち、押したボタンの下に1行だけ出す
  - **黙って入口に戻さない。** 前は `/auth/callback` が理由を捨てていたので、
    リンクの期限切れも、有効になっていない入り口も、画面では同じ「何も起きない」だった。
    短い合図（`?e=`）に畳んで渡し、入口が日本語1行にする（上流の英語はそのまま出さない）
  - **送ったあとに戻り道を置く**（`別のメールで送り直す`）。前は打ち間違えると詰んでいた
  - 入力欄は `outline` を消しているので、**枠のほうが応える**（focus で青）
- **決まるまでの道を1本にする（2026-08-25）** — 聞く → 候補 → 選ぶ → 計画。
  行き止まりと嘘を抜いた（→ `docs/design/01-data-model.md` 決まるまでの道）
  - **3つとも違うときの道を置く。** 候補のカードのいちばん下に「どれも違う — 条件から見直す」。
    前は下の入力欄に社長が自分で書き直すしかなかった。押すと会話に戻り、
    **何が違うかを1問だけ**聞いてから、**違う軸で**出し直す（相手 / 売り方 / 分野そのもの）。
    すぐ3つ並べ替えても、社長には同じものに見える
  - **答えは答えであって、新しいゴールではない。** 「どこが違いますか」に答えた往復から
    `propose_work` が返ってきて、**質問文がそのまま Work の題**になった。
    探索の途中（まだ Work が無い候補の道）は、答えの往復からの提案を取らない（`noGoalFromAnswer`）
  - **吹き出しに出るのは社長が決めたことだけ**（`startWork(goal, ctx)`）。
    前は候補のねらいも集めた条件も全部つないで1つのゴールにしていたので、
    「◯◯を立ち上げたい 終わり: … 背景: … 分野: … 使える時間: …」が
    **社長が書いた言葉として**画面に出ていた。条件は統括AIにだけ渡し、
    そのとき「使える時間を超える週を作らない / やりたくないことを含む道は引かない」まで言う
  - **選んだ道は、いちばん最初の「決めたこと」**（`addDecided`。`question='どの道で進めるか'`）。
    **選ばなかった2つも理由つきで残す** — なぜその道かは、選ばなかった道と並べて意味になる。
    前はどの案を採ったかが `candidates.adopted_work_id` にしか無く、画面のどこにも出なかった。
    この決定はそのあとの実行の依頼文にも載る（Phase 9 の受け渡し）
  - **ロードマップの引き方を憲法に足した** — 終わりの形から逆に引く ／
    最初のフェーズは**承認したその日から動ける**ものに ／ 社長が使える時間の中で回る形に
- **入口を3つの姿に分けた（2026-08-25）** — 新規登録 / ログイン / パスワード再設定。
  それまでは1つしか無く、**はじめての人と2回目の人が同じ画面**にいた
  - **入り方は2つ**（Google か、メールとパスワード）。**メールのリンクは畳んだ** —
    「忘れたときに届くメール」が再設定で足りるので、**メールで入る道が2本**あった
  - 姿は `?mode=signup` / `?mode=forgot` で **URL に持つ**（リンクを渡せる）。
    切り替えは `history.replaceState` なので**サーバーには行かない**
  - **忘れたときの姿だけ Google を出さない** — Google で入っている人にパスワードは無い。
    出すと道を間違わせる
  - **パスワードの決まりは `lib/auth/password.ts` の1か所**（10文字・大文字・小文字・
    数字・記号）。**満たしたところから緑になり、満たすまで送るボタンは灰色**。
    **記号として数える文字は Supabase と同じ並び**にしてある —
    片方だけ厳しいと、画面で断られたものが API では通る
  - **画面の決まりはUXであって、セキュリティそのものではない。** 匿名鍵はブラウザに
    配られているので、この画面を通さずに認証API を叩けば弱いパスワードでも作れる。
    **本当に止めるのは Supabase の設定**（Minimum password length ＋ Required characters）
    → `docs/RUNNING.md`。**画面だけ直して「守った」と言わない**
  - **「そのメールは登録されていません」とは言わない** — どのメールが登録済みかを
    外から数えられてしまう。ログインが失敗したときはいつも同じ1行
  - **再設定は2回打たせる**（`/auth/reset`）。見えない文字を1回で決めさせると、
    打ち間違えたまま締め出される（次に入れるのは、また再設定のメールを送ってから）。
    **リンクを踏んでいなければ正直に断る**（白い画面にしない）
  - **`?next=` を信じない**（`/auth/callback`）。そのまま渡すと `//evil.example` を
    入れられて、**このアプリの名前で他所へ送れる**。`/` で始まる素の path だけ通す
  - **入っている人に入口を見せない** — `/login` は `/home` へ。
    ただし `/auth/*` は素通しのまま（**再設定はセッションを持ったまま開く**画面）
  - **確認メールが要るかどうかは Supabase が決める。** 要らない設定ならその場で会社に入り、
    要るなら「メールを送りました」。**画面が勝手に決めない**（`/api/health` の `signup` で見える）
- **実キーで最初の会社が動いた（2026-08-25）** — 社長が Google で入り、探索から Work まで作った。
  そこで**最初の実行が2本とも失敗した**。原因は3つとも「名簿が繋がっていなかった」こと
  - **統括AIに名簿を渡していなかった。** 道具の説明には「agency-agents のカタログ ID」と
    書いてあったが、**その一覧をモデルは一度も見ていない**。だから `propose_hires` は
    `hires: []` を返し、担当には「商品設計担当」「デザイン制作担当」という
    **この会社に居ない名前**が書かれた → `rosterBlock`（`lib/roster`）を計画と
    フェーズ送りの依頼文に必ず載せる
  - **誰も採用しないまま承認できた。** `hires` が空なら誰も採らず、タスクは
    `assignee_type='user'`（＝社長がやる）のまま残った → **`hires` だけを見ない**
    （`crewFor`）。タスクの担当名からも採り、名簿に無い名前は落とす。
    1人も残らなければ調査担当にする
  - **担当のいないタスクをポンプが走らせた。** 誰の頭も載らないままモデルを呼び、
    成果物を書かずに終わって blocked になる — **お金だけ減る** →
    `nextQueued` は社員のいないタスクを拾わない（双子とも）
  - **画面が嘘をつく道も塞いだ** — 担当名は**控えに入る前に**名簿へ寄せる（`draftWork`）。
    承認のときだけ寄せると、計画の画面が「商品設計担当」と言い、実際は調査担当が動く
  - ついでに見つけた**前からの穴**: 差し戻しの直しタスクが**担当なしで生まれていた**
    （Work 画面が `taskId` を `DelActions` に渡していなかった）。
    元のタスクが辿れないときは、その Work で動いている誰かに落とす
  - **決め打ちのプロバイダを本番と同じ壊れ方にした** — 小さい Work の計画は
    `hires: []` ＋ 居ない担当名を返す。**行儀よく書くと、この穴は検査に永久に出てこない**
- **会話ログは次の返事が出るところに置く（2026-08-25）** — 社長の指示。
  前は入力欄のすぐ上に貼っていた（隠れない代わりに、返事とは違う場所にあった）。
  いまは**返事が始まる場所**にそのまま出て、本文が流れ出すとその下に続く
  （右ペインと同じ形。会話は `useStick` で下に貼り付いているので隠れない）
- **ワークフロー図が成果物になる（2026-08-25）** — 社長の指示で
  **archify**（https://github.com/tt-a1i/archify · MIT）を入れた（→ `docs/design/11-diagram.md`）
  - **そのままは持ち込めない。** archify は `archify.mjs validate → deliver → visual-check`
    という **Node の CLI が前提**で、AI社員は**1タスク＝1往復・道具4つ**。シェルもファイルも無い
  - **持ち込んだのは「形」と「作法」** — 器（`schema_version` / `diagram_type` / `meta` /
    `lanes` / `phases` / `mainPath` / `nodes` / `edges`）は**名前ごと同じ**。
    検証は OneFound の中で回す（`lib/diagram/check.ts` の9つ）
  - **落としたもの**: `brand` / `visual_preset` / `animation` / `cards` / `views`（飾り）／
    `legend`（**凡例を置かない**）／ `groups`（レーンとフェーズで足りる）／
    `route` / `via` / `channelX`…（**格子が位置を決める**ので要らない）
  - **変えたもの**: `node.type`。archify は `frontend / backend / database`（構成の語）だが、
    一人社長のワークフローでは意味を持たない → `work / decision / deliverable / wait / end`。
    **色が付くのは2つだけ** — 判断＝橙（あなたが決める）／ 終わり＝緑（到達）
  - **絵が壊れる診断と、読みにくいだけの診断を分ける。** 壊れるほうは出さずに、
    **指したところだけ**直してもらう（道具を1つに絞って1回だけ）。それでも壊れていれば
    **通らなかったと正直に言う**。読みにくいだけなら出して、**直らなかったことを成果物に残す**
    （archify の「未解決の診断は正直に報告する」）
  - **描き方はホームの地図とまったく同じ語彙**（格子・直角の線・左3pxの色帯・ドット背景）。
    **同じ会社の中で2つの図法を作らない**。違うのは1つだけ — **これは動かさない**
    （成果物は読むもので、探すものではない。拡大縮小もミニマップも置かない）
  - **文字は潰さない。** 440px のペインに収めようとして 0.4倍になり読めなくなった →
    **縮めるのは 0.8 まで**、あとは**横に送る**（`.sx`）
  - **一覧の書き出しは主線**（`previewOf`）。JSON をそのまま出すと記号の山になる
  - 作法は標準スキル `workflow-diagram.md`（社長にも見える・切れるが消せない）
  - **決め打ちのプロバイダは1回目をわざと壊す** — 線の先に居ないノードを指す。
    本物と同じ壊れ方を通さないと、直す仕掛けが動いているか分からない
- **ホームのワークフローに archify を当てた（2026-08-25）** — **残したのは2つ、外したのは2つ**
  （→ `docs/design/11-diagram.md`）。いちばん効いたのは作法を当てたことではなく、
  **作法を物差しにして、元の実装が自分のルールを破っているのが見つかったこと**
  - **残した① 枝は最寄りの主線ノードから。** チップの列を `Math.max(nowSeq, 1)` で
    決めていたので**畳んだあとの列とずれ**、しかも線が**2点しかなく斜めに出ていた**
    （「直角に曲がる線」は archify より前から決まっている）→ **畳みを組み立て側
    （`lib/live/flow.ts`）に移して列を1か所で決め**、チップは親の真下、線は4点の直角。
    `MapPhase.span` で**畳んでも元のフェーズ番号を失わない**
  - **残した② 通らない盤面は描かない。** 成果物の図と同じ9つの検査に掛ける
  - **外した① 主線を1本にする**（放っておけない順に並べ替え、太く引く）。
    ①**地図の行が動く** ②「いま何が放っておけないか」は**進捗・タスク・通知**が
    もう言っている ③線を太くしただけでは**ほとんど見えない**
  - **外した② ノードは12まで。** これは**1枚の図**の作法。地図は12個に収まらないから
    **拡大縮小とミニマップ**を持っている
  - **見た目は4案つくって社長が選んだ**（いまの形 / 帯 / 縦に流す / **幹から枝分かれ**）。
    選ばれたのは**幹**。`?fx=` の足場は消し、**幹だけを残した**
  - 姿を作って初めて出た2つも直した（どの案でも正しい）— **社員の球の座標を
    組み立て側から渡す**（前は描く側が置き直していて、格子が変わると別のノードに載った）／
    **`bounds` に線も入れる**（幹のように節の外を通る線が箱から出て切れていた）
  - **`labels`（「新しい Work」の枝ラベル）を消した。** `MapWork.from`（枝の系譜）は
    まだ誰も入れていないので、描く側も空を回していた（死んだコードは消す）。
    系譜が入るときに、枝の文法ごと戻す
  - **skill は「当てるもの」ではなく「照らすもの」。** 図（成果物）の側は
    作法がそのまま効く — あちらは1枚の図だから
- **見ていなくても会社が進む（2026-08-25）** — 社長が「1時間ごと・上限つき」を選んだ
  - **ポンプを Work 画面から器（Shell）へ移した。** 前は Work 画面にしかポンプが無く、
    ホームやチャットを見ているあいだ、動いている Work は**止まっていた**。
    「チャットボットは聞かれるまで黙っている。**会社は先に言う**」と言っている以上、
    見ている画面によって進んだり止まったりするのはおかしい
    - **動いているときは3秒、静かなときは15秒**（何も起きていない会社を3秒ごとに叩かない）。
      **裏タブは何もしない**（裏タブの数だけ速く進む、が起きない）
    - **Work 画面は読み直すだけ**にした。同じ会社に2つのポンプを立てると、
      上限を測る場所が2か所になる（取り合い自体は atomic claim が捌く）
  - **閉じているあいだは Cron**（`/api/cron`・毎時0分・`vercel.json` の `crons`）。
    進め方は画面とまったく同じ `pumpCompany` — **2つ目の実行系を作らない**
    - 誰として読み書きするかが問題だった。Cron に cookie は無く、RLS は
      `auth.uid()` を見るのでどの行も返らない。**取れた道は3つ** —
      ① service role の鍵（漏れたら全社が出る。持たないと決めてある）
      ② 専用の Postgres ロール（**書き込みの既定値 `account_id` が埋まらない**。
      往復ごとに口座を渡す仕組みが要るが PostgREST では渡せない）
      ③ **ふつうにログインする** ← これ
    - **runner ＝ 会社にもう1人いる利用者**（`lib/run/runner.ts` ＋ `runAs`）。
      **特別扱いがどこにも無い**のが効いている — ポリシー28本も、既定値22表も、
      store のコードも1行も変わらない。止めたければその利用者を消せば止まる
    - **鍵が3つそろわないうちは 503**（`CRON_SECRET` / `RUNNER_EMAIL` / `RUNNER_PASSWORD`）。
      **動いているふりをしない**。そろっているかは `/api/health` の `cron` で見える
      → 入れかたは `docs/RUNNING.md`
  - **1日の上限**（`lib/run/budget.ts`・`DAILY_CAP_CENTS`・既定 200セント＝20万トークン）。
    見ていないあいだも進むようにした以上、**目を離しているうちにどこまで使うか**が
    決まっていないと社長は閉じられない
    - **残高とは別の線。** 残高が尽きた → Work ごと `paused`（設計の言葉どおり「予算上限」）／
      きょうのぶんが尽きた → **止まるだけ。あすまた動く**（Work の状態は変えない）
    - **列を足さず `token_ledger` の実績から数える**（不変条件2 と同じ考え方）。
      見積もりを持たないので、ずれようがない。1日は**日本時間**で数える（朝の報告と同じ）
    - 通知は**1日1通**（`notifications_cap_daily`・0026）。ポンプは数秒ごとに来るので、
      ここが通知を積む口になってはいけない
    - **きょう使ったぶんと上限は `/billing` に出す**（トークンの数字を出していい唯一の画面）
- **通しで見直して5件直した（2026-08-25）** — 社長の指示「完成度を高めてください」
  - **AI社員が前のフェーズの成果物を1文字も見ずに始めていた。** 渡す成果物を
    「同じフェーズ」に絞っていたので、戦略フェーズの担当に調査の中身が**どこからも届かない**
    （承認済みの索引はタイトルしか渡さない）。**この Work の中はぜんぶ地続き**なので、
    済んだタスクの成果物を新しい順に3件渡す。**差し戻されたものは渡さない**
    （直っていないものを土台にさせない）
  - **引き直しに、空の計画の守りが無かった。** 立てるときは止めていたのに（「0フェーズで進めます」）、
    **「直したい」の往復が空で返ると、ちゃんとしていた計画が置き換わった**。
    直しは元より悪くならない、が要る
  - **Work が立ったことが台帳に1行も残っていなかった**（0008 は `after update` だけ）。
    承認と引き直しは残るのに、**いつ何のために立った Work なのか**が辿れない → 0027 で
    `work.created` ＋ `work.paused` / `work.done`（止めたのは仕組みなので actor は system）。
    見ていないあいだに動くようになったので、台帳だけで読めることの重みが変わった
  - **ホームが止まった写真だった。** 会社は動いているのに1回読んで固まる。8秒ごとに読み直し、
    **中身が本当に変わったときだけ**入れ替える（変わっていないのに state を差し替えると、
    輪も粒も毎回組み直しになる）。裏タブでは読まない
  - **「セキュリティ警告 0件」が実際は1件だった。** `Leaked password protection`
    （＝ Supabase の設定・Pro プラン）。**コードで直せないものを「直した」と言わない** —
    スキーマ由来は0件、と書き分けた
- **聞く回数を先に決めない（2026-08-25）** — 社長の指示
  「あなたに聞くのは2回とか決めなくていいよ、AI が判断して適切な提案ができるまでしていい」
  - **数の上限を全部外した。** 憲法の「1度に聞くのは最大2〜4問」、計画の「関門は多くても2つ」、
    道具の説明（`ask` / `draft_plan`）、チャットの方針、引き直しの頼み文 — 5か所にあった
    「2」という数字は、**どれも根拠が無かった**（画面がそう決めていたわけでもない）
  - 残したのは**1度に出す数**だけ（1〜4問）。板は1問ずつ出るので、多すぎると読めない。
    **回数のほうは決めない** — 「適切な提案ができるまで、必要なだけ聞いてよい。
    **分かったふりで先へ進むほうが失礼**」を憲法に書いた
  - **◆ は「社長でないと決められないところ」にだけ立てる。0 でもいい**。
    そのかわり、**◆ を置かなかったフェーズは会社が自分で進む**（`pumpWork` →
    `planGates` ＋ `closePhaseIfDone(workId, gates)`）
    - 置かなかったところで止まるのは、**統括AIの判断を無かったことにする**のと同じ。
      しかも見ていないあいだは、そこで**次の1時間がまるごと待ち**になる
    - だから「一応聞いておく」で ◆ を増やすと、そのぶん会社は遅くなる — 道具の説明にそう書いた
    - 通知も分ける — ◆ があるフェーズは **判断待ち**「決めて、次に進めてください」、
      無いフェーズは **要確認**「次に進みます」。**勝手に進むのに「進めてください」と言わない**
    - 計画の画面も「あなたが決めるのは ◆ の N か所。**ほかは会社が進めます**」。
      ◆ が1つも無ければ「あなたが決めるところはありません。会社が最後まで進めます」。
      **止まらないことを不意打ちにしない**
- **成果物ができたら、社長が見るまで進まない（2026-08-25）** — 社長の言い直し
  「完全自動で動くというよりかは、**成果物ができたら確認してもらって、
  それで進めていいのか確認してもらう**」
  - **社長を待つのは2つ** — 計画の **◆**（社長でないと決められないところ）と、
    **まだ見ていない成果物**。`closePhaseIfDone` が毎回両方を測る
    （→ `lib/store/types.ts` の `PhaseGate`）
  - **承認そのものが「進んでいい」の合図**。押されるまで次のフェーズは始まらない。
    見せる前に次へ行くと、直したいところがあっても**後戻り**になる
  - **閉じた直後だけでなく毎回測り直す。** 社長が最後の1件を承認した瞬間に
    `ready` が立ち、ポンプ（3〜15秒）が次のフェーズを引く。
    だから**押すのは1回**（承認だけ）— 「承認 → さらに進むを押す」にしない
  - **ポンプは、起こすタスクが無くても関所を見る**（`pumpWork` の idle の道）。
    ここを見ないと、承認したのに次が始まらない
  - **引けなかったら、何度も引き直さない。** deep の1往復は安くないので、
    「引けませんでした」の1通がそのまま**もう試した印**になる（`noticed(key)`）。
    そのあとは Work 画面の帯から社長が押す
  - 通知の言い方は3つ（`gateNote`）— 未確認の成果物があれば **要確認**
    「成果物 N件 を見て、次に進めてください」／ ◆ なら **判断待ち**「決めて、次に進めてください」／
    どちらも無ければ **要確認**「次に進みます」。**成果物のほうが先に来る**
    （見ていないうちに「決めろ」とは言わない）
- **出す形は社長の手もとで使える形に（2026-08-25）** — 社長の指示
  「文章だけでなく、LPやPDFや様々な出力できるようにしたい、CSVとかもそうだし」
  （→ `docs/design/12-outputs.md`）
  - **形は1か所**（`lib/deliver/format.ts`）。`report / doc / copy / table / csv / page / code / diagram`。
    見るのは4か所 — 道具・画面・持ち出し・一覧の書き出し
  - **記号のまま出さない。** 前は `<pre>` に流していたので `## 見出し` も `|---|---|` も
    そのまま並んでいた → `lib/deliver/md.ts` で読んで、見出し・箇条書き・表に組む
    （**ウェイトは 400 のまま。強調は明るさで言う**）
  - **ページ（LP）はアプリの中で走らせない。** `sandbox=""` ＋ `srcdoc` に閉じ込める —
    別のオリジンになるので、こちらの cookie も DOM も見えない
  - **PDF は刷って作る。** サーバーで PDF を組む道具は入れない（workerd に載らない）。
    **新しい窓は開かない** — 見えない iframe に1枚組んで刷ってもらう。
    **紙は白い**（画面の黒をそのまま刷らない）
  - **決め打ちのプロバイダも同じ形で返す。** markdown だけ返していると、
    「csv なのに markdown を書く」という本番の壊れ方が検査に永久に出てこない
- **つないだ道具（MCP・2026-08-25）** — 社長の指示「将来的にはMCP接続もできるように
  したいけど、MCP構築時間かかりますか？できそうならしてください」→ **半日で入った**
  （→ `docs/design/13-mcp.md`）
  - **向きは2つあって、作ったのは client のほう** — **AI社員が、社長がふだん使っている
    ものをそのまま読む・書く**。server の側（社長の Claude が OneFound を読む）は、
    鍵の配り方が別の設計なので分けた
  - **相手ごとの実装を持たない。** MCP は道具の側が自分で名乗るので、
    こちらは**名前を付け替えて並べるだけ**（`lib/mcp/company.ts`）
  - **ライブラリは入れない。** Streamable HTTP は JSON-RPC を POST するだけで、
    返事は JSON か SSE。`fetch` で足りる
  - **本番は https だけ。** localhost は**デモと検査**のためだけ（`DEMO_MODE`）。
    **`NODE_ENV` では分けられない** — `next start` は検査のときも production なので、
    そこで切ると「検査のためだけに本番の判定を緩める」ことになる
  - **書ける道具は、社長が許した相手のぶんだけ。** 既定は読むだけ。
    相手の `readOnlyHint` は参考にするが**鵜呑みにしない**（外に出る道具は Approval 必須の一形）
  - **道具の名前に日本語を入れない。**「テストの在庫」は寄せると `______` になり、
    `mcp________list_items` という読めない名前ができる（実際そうなった）→
    英数字が残らなければ**並び順の番号**にして、どこの道具かは**説明のほう**で言う
  - **1タスク＝1往復の、初めての例外。** MCP は「読む」道具なので、
    読んだ結果を見てから書くことになる。**つないでいない会社では1往復のまま** —
    払う理由が無いのに往復しない。上限は3往復（`MCP_ROUNDS`）
  - **結果は「社長の発言」として渡す。** 器（`Msg`）は role と文字しか持たないので、
    道具の往復を本物の形で入れると3つの通り道ぜんぶを直すことになる。
    **宙に浮いた tool_call を作らない**この形のほうが安全でもある
  - **鍵は行って戻らない。** 型（`McpServer`）に `token` を置かない。
    DB の列の権限で止める形は、引くために `public` の SECURITY DEFINER が要り、
    **それだけで警告が1件増える** → **スキーマ由来の警告0件を優先した**
  - **話す相手はこの repo の中に置く**（`tools/mcp-test/server.mjs`）。
    **他所のサーバーの調子で検査が赤くなるのは検査ではない**
  - 画面はスキルとまったく同じ置き方 — `/tools`（つなぐ・切る・許す・外す）と、
    メンバーの「全員に効くこと」（一覧と入/切だけ）。**左レールには出さない**
  - まだ: **OAuth**（いまは鍵を貼るだけ）／ 社員ごとに道具を分ける ／ server の側
- **チャット → Work → 完了まで、通しで歩いて直した（2026-08-25）** — 社長の指示
  「完璧に作り上げてください。チャットしてから work 作ってそれを完了させるための流れまでも」。
  **本当に最後まで走らせて、終わった画面を見る**まで見つからないものばかりだった
  （`tools/check/_walk.mjs`）
  - **後のフェーズの仕事を、全部 調査担当がやっていた。** 計画のとき提案されるのは
    たいてい**最初のフェーズの担当だけ**。フェーズ3で「企画担当」と書かれても
    会社に居ないので、`advancePhase` が**先頭の社員に落として**いた —
    実測で MVPの要件も LPの構成も申込の流れも調査担当。画面には担当名が出るので、
    社長には見分けがつかない → **要る人がいなければ、その場で採用する**
    （名簿にある名前だけ。`hireEmployee` は定義ごとに1人なので二度採らない）
  - **担当を書かない道を塞いだ。** `draft_phase_tasks` の `owner_hint` は任意だったので、
    書かれなかったタスクが先頭の社員に落ちた。**必須にした** — 名簿は依頼文に載っているので、
    選べないはずがない（まだ居ない担当を選んでよい。そのとき採用される）
  - **終わった Work が「進行中」「フェーズ 1 / 4」と出ていた。** 状態の語は6つだけなので
    **完了**と出す。フェーズは、active が無ければ**最後まで来ている**とみなす
    （100% なのに1番目、という食い違いが出ていた）
  - **「成果物がすべて揃っています」は確かめていなかった。** 実際は9件とも 要確認 のまま
    （社長は1つも見ていない）→ **事実にする** — `この Work は終わりました — 成果物 9件` ＋
    `まだ見ていないものが 9件`。**確かめていないことを言い切らない**
  - **終わりこそ、いちばん言うべきところ。** 前は `status='done'` ＋「Work が終わりました」の
    1行だけ＝記録であって報告ではない → **通知に中身**（成果物N件・決めたことM件・
    まだ見ていないK件）＋ **その Work を始めた会話に統括AIの報告**（できたもの・決めたこと）。
    **言葉は `lib/exec/finish.ts` の1か所**（双子が同じ文を書く）。**モデルは呼ばない** —
    事実だけなので組み立てで足りる（朝の報告と同じ）
  - **ゴール行に「終わり:」「見込み:」が混ざっていた。** 計画の吹き出しでは直したのに、
    チャットから作る道（`chatMakeWork`）が3行を1つに繋いだままで、Work 画面に
    「◯◯をやりたい 終わり: … 見込み: およそ10週」が**社長の言葉として**出ていた →
    `startWork(goal, ctx)` の ctx へ移す
  - **「作るもの」のフェーズを画面が推測していた。** `Math.floor(i / per) + 1` で
    順番から割り当てていたので、フェーズ1のタスクが作るものが「フェーズ2」と出た →
    **`draft_plan` の deliverables に phase を書かせる**（古い控えも読める）。
    書いていなければ**空にする**（でっち上げない）
  - **オフィスのログが常に「稼働中」だった** → 実行中の社員がいるときだけ。いなければ「待機」
  - **もう作ってある会話が「この Work を作る」に戻っていた。** 作ったことがカードの中の
    state にしか無く、開き直すと消えた（押しても二度は作られないが、**画面が嘘をつく**）→
    スレッドが持っている Work を親から渡す（**カードは id しか持たない**の原則どおり）
- **会話の考え方を一本に締め直した（2026-08-25）** — 社長の指示「chat の考え方とかも固めて最適化して欲しい」
  - **元になっている1つの考えを、いちばん上に書いた。** 社長がここに書くのは
    「やりたいこと」か「困っていること」で、**どちらもそのままでは仕事にならない**。
    会話の仕事は、それを**押せる形**（質問 / 候補3つ / 診断 / Work の提案）に変えること。
    だから**統括AIの番は、次に押せるものか、答えそのもので終わる** —
    **文章だけで終わっていいのは、社長が尋ねたときだけ**
  - **いまどこにいるかを毎回そのまま言う**（`stageLine`）。前は状態の断片
    （条件・候補を出したか・材料）だけを並べていたので、モデルは往復のたびに
    **どの道のどの段かを推測し直して**いた。推測は外れる — 条件を書き留めただけで止まる、
    同じことを聞き直す、が実際に起きた。段が分かれば次の一手は1つに決まる。
    探索中は**まだ聞けていない条件の名前まで**出す（`分野・方向 / 使える時間 …`）
  - **答えが届いた往復は、1回で写して次の一手まで出す**（`needCard` を答えの往復にも広げた）。
    道具は複数呼べるので往復を分ける理由がない — 前は**1つの発言にモデルを3回**
    呼んでいた（写す往復＋出す往復＋保険）。続きの輪は保険として残す
  - **社長の最初の一言は、いつも history の先頭に残す。** 直近10件だけだと、
    探索が長引いたときに**始まりが消える** — 条件は state が持っているが、
    「そもそも何と言って始めたか」はどこにも残らなかった
  - **統括AIが「社長を待っているもの」を知っている**（`snapshot` に判断待ちと要確認の件数）。
    前は Work の一覧と決めたことだけだったので、「何かやることある？」に答えられなかった。
    聞かれていなくても一言添える — 放っておくと会社が止まるところだから
  - **同じことを二度書かない。** ask の回数の決まりが2か所にあったのを1つに。
    「候補を出していいか」の判断（`canPropose`）も**会話の考え方の一部**なので chat.ts へ移した
- **使われるための3つを入れた（2026-08-25）** — 社長の指示「もっと需要のあるもの、
  他のものに負けないものに」。**足りない機能を数えるのではなく、
  「これが無いと使えない」を3つ**選んだ
  - **社長が Work を止められる。** 見ていないあいだも動く会社にしたのに、
    **止める手がどこにも無かった**（`pauseWork` は残高が尽きたとき仕組みが呼ぶだけ）。
    気が変わった Work にお金を使い続けられるのは、社長には怖い →
    Work のペインの最後にトグル（社員の一時停止と同じ作法・保存ボタンは置かない）。
    止めているあいだ**新しいタスクは起きない**（`activeWorks` は active だけ）。
    走っている最中のものは最後までやる — 途中で切ると書きかけの成果物が宙に浮く
  - **作ったものを持ち出せる**（`DelTake`。コピーと ⬇）。ここまで、調べさせて書かせたものは
    **アプリの中にしか無かった** — 一人社長は調査結果を人に見せ、価格表を計算に使い、
    LPの構成をそのまま書き出す。**持ち出せない成果物は、無いのと同じ**。
    スキル（SKILL.md）には最初から ⬇ が付いていたのに、いちばん大事なものに無かった。
    図は `.json`（archify の器のまま。別の道具に渡せる）、文章は `.md`
  - **はじめての画面に、押せる例を3つ。** 入力欄が1つあるだけの画面で、
    **やりたいことが言葉になっている人しか先へ進めなかった**。
    説明のコピーではなく**提案** — 押すとその言葉で会話が始まり、統括AIが聞き返す。
    押しただけでは Work にならないので、覗くのは安全。作る / 決める / 調べる を1つずつ
    （3つなのは1行に収めるため。4つだと1つだけ次の行に落ちて並びが崩れる）
- **待たないで、先に作って動き出す（2026-08-26）** — 社長の指示
  「**受注が来てから制作っていうようにしたくない**。例えばlp制作事業だとすると、
  まずはマーケティングのための自分のサイト、ポートフォリオを制作してから、公開して
  そこから営業やマーケティングをする」「**まず必要な社員全員採用して**、調査社員が調査完了したら
  その結果をユーザーに投げて**わからない部分は統括AIがユーザーに質問投げて
  他のAIが全員動き出す**」。**4つとも「待つのをやめる」話**だった
  - **① 他人が動かないと終わらないフェーズを作らない。**
    「受注が3件入っている」「問い合わせが来ている」は、AI社員には終わらせられない —
    しかも**フェーズは全タスクが done で初めて閉じる**ので、置くと会社がそこで**本当に止まる**
    （事業の助言としてまずいだけでなく、仕掛けとして詰む）。
    憲法に「フェーズはAI社員が自分で終わらせられることだけ」を足し、
    `checkPlan` に `waiting` の検査を入れた（受注・問い合わせ・応募・反響・集客・商談…）。
    売る仕事は **見せられるものを作る → 公開する → そこから来る** の順に引く。
    受注はフェーズの**あと**に来るもので、フェーズではない
  - **② 要る人は最初に全員そろえる。** `PlanPhase` に `owner` を必須で足した
    （名簿の「◯◯担当」）。前は最初のフェーズの担当しか提案されず、
    あとのフェーズの人は `advancePhase` になってから採られていた —
    画面には「担当は未定」と出て、会社が後から組み上がるように見えた。
    `crewFor` にフェーズの担当も渡すので、**承認したその場で全員そろう**。
    名簿に無い名前は**そのフェーズを回す人**に寄せる（先頭の社員ではない）
  - **③ 起こせる人は全員起こす**（`nextQueued` が配列を返す・ポンプは `allSettled`）。
    前は「走っているタスクが1つでもあれば null」だったので、
    **4人採用しても動くのは常に1人**。オフィスの絵に4人いるのに1人しか働いていなかった。
    いまは**走っている人のぶんだけ飛ばして、残りを全部**起こす
    （同じ人に2本は持たせない）。取り合いは `startRun` の atomic な置き換えが捌く。
    **上限は束の前に1度だけ測る** — 1ティックぶんは超えうるが、
    上限は「その日はここまで」の線であって1トークン単位の栓ではない
  - **④ 分からないところは統括AIが聞く。そのタスクだけが待つ。**
    `draft_phase_tasks` にタスクごとの `ask`（質問・なぜ・選択肢）を足した。
    統括AIは前のフェーズの成果物を読んでから次のタスクを引くので、
    **そこで「これは社長にしか決められない」と分かったものに ◆ を立てられる**。
    そのタスクは `needs_decision` で待ち（`markDecision` — AI社員が実行中に詰まったときと同じ道）、
    **ほかのタスクは queued のまま動き出す**。
    計画の ◆（`gates`）は**フェーズの終わりの関門**、こちらは**タスクの入口の関門**
  - **デスクのレーンが `.sx` を名乗っていなかった**（`overflowX` だけ）。
    ついでに検査も直した — **`.sx` は「横に送る」という設計側の宣言**なので、
    その中身を「画面外」として数えない（`.clip` と同じ扱い）。
    数えていると、**社員がひとり増えるたびに検査が赤くなる**
  - **決め打ちのプロバイダは、本番と同じ壊れ方をする** — ローンチのゴールを
    「受注が3件入っている」で1回目に返す（担当も「営業担当」＝名簿に無い）。
    行儀よく書くと、`checkPlan` → 引き直しが動いているか永久に分からない
  - **⑤ 承認してから動き出すまで、実測で14秒なにも起きなかった。** 検査を直している途中で、
    行を0.5秒ごとに書き出して初めて見えた（「押しても効かない」ではなく「押した15秒後に効く」は、
    通しの検査では気づけない）。原因は2つとも**器の側**だった
    - **ポンプの静かなときの間隔（15秒）を、社長が押した直後にも待っていた** →
      `lib/pump.ts` の `wakePump()`。画面が移ったとき（承認・採用のあとはたいてい移る）と、
      移らない操作（判断に答える / 成果物を見る / フェーズを進める / 止めた Work を動かす）で起こす。
      **2つ目のポンプは立てない** — 起こすだけで、走らせるのは器の1本のまま
    - **サーバーアクションは同じ画面のぶんが順番待ちになる。** ポンプは1往復ぶん返らないので、
      そのあいだ `getWork` も `homeData` も**後ろで待たされていた** —
      動いているのに画面は「待機」のまま固まり、走り終わってからいきなり「要確認」に飛ぶ。
      **動いているところを見せる**のがこの製品の売りなので、これは直さないといけない →
      `app/api/pump/route.ts`（**中身は同じ `pumpCompany`。変えたのは呼び方だけ**）。
      道筋は順番待ちに入らないので、走っているあいだも読み直しが通る
    - 直した後の実測: **0.5秒で 15% → 3秒で 75% → 5秒で 要確認**（2人が並んで動くのが見える）
- **会社が育つ（2026-08-26）** — 社長の指示「**hermes agent を全社員に導入したい**」。
  元は **NousResearch/hermes-agent（MIT）**「The agent that grows with you」の閉じた学習の輪
  （→ `docs/design/14-learning.md`）
  - **そのままは持ち込めない。** Hermes は Python 3.11 ＋ Node ＋ ripgrep ＋ ffmpeg ＋ Git ＋
    `~/.hermes/` ＋ CLI とゲートウェイの2プロセスが前提で、**Cloudflare Workers には載らない**。
    archify のときとまったく同じ形なので、**持ち込んだのは「輪」だけ**（コードは1行も使っていない）。
    スキルの書式（agentskills.io）は**この環境から読めなかった** — たぶん互換だが、突き合わせていない
  - **⓪ 輪より先に、土台が無かった。** 統括AIには94行の憲法があるのに、
    **AI社員には1枚も無かった**（頭に載るのは自分の定義と道具の使い方だけ）。
    「分かったふりで書かない」「確かめていないことを言い切らない」は製品のあちこちに
    書いてあるのに、**社員は一度も読んでいなかった** → `lib/roster/constitution.ts`。
    7人ぜんぶの**定義の前**に載る（そこがプロンプトキャッシュの境目）
    - **短くする。** 社員は standard（速いモデル）で走るので、統括AIと同じ量を載せると
      定義のほうが薄まる。ロスターの「守れない量を書かない」はここにも効く
    - **7人ぶんに同じ行を書き足さない。** 調査担当の「数字には必ず根拠を付ける」と
      戦略担当の「価格・対象は ask_decision」は**憲法に上げて、定義からは消した**
    - **根拠の穴を正直に埋めた** — 調べる道具（Web検索）は既定オフなので、
      「**渡されていないなら、記憶から書いたことに『未確認』と印を付ける**」を1行入れた。
      道具が入るまでは、これが正直な形（**画面だけ直して「守った」と言わない**の一形）
    - **社長にも見える**（メンバー画面 → 全員に効くこと）。標準スキルと同じ考え方。
      ただし**切れない**（会社の土台なので、Critical Rules と同じ）
  - **① 社員が手順書を書く・使いながら直す**（`write_skill` / `improve_skill`）。
    学びは1行の申し送りで**次の実行に必ず載る**、スキルは**必要なときだけ読まれる**ので量を書ける。
    **渡した手順書だけ直せる**（当てずっぽうの id で他の行に届かない）／
    **1タスクで1枚まで**（書き散らかされると、通す側も読む側も溺れる）／
    ファイル名は**英数字が残らなければ番号**（MCP の `______` と同じ事故を起こさない）／
    書いたものは**会社ぜんぶのもの**（会社が学ぶのであって、席が学ぶのではない）
    - ついでに塞いだ穴 — 実行は**会社ぜんぶのスキルしか読んでいなかった**ので、
      設定ペインで社員に付けたスキルは一度も読まれていなかった
  - **② 通すのは統括AI**（社長が3案から選んだ）。Hermes は社員が自分で自分を書き換えるが、
    ここには「**勝手に増えると、社長が知らないうちに社員が変わる**」という先の判断がある。
    間に1枚だけ挟んで、**社長の手は増やさない**（やることは4つのまま）
    - `lib/exec/skills.ts`。**1往復で待っているものを全部見る**（1枚ずつだと3枚書かれた日に3回払う）。
      基準は3つ — 次に読めるか / この1回の結果ではないか / もうある手順書と重ならないか。**迷ったら落とす**
    - **鍵が無い環境でも通す**（飛ばすと下書きが永久に溜まって輪が回らない）。
      ただし**仮の審査だと名乗る**
    - **落としたものは理由つきで残す** — `/skills` の下に暗く並び、**トグル1つで戻せる**
      （「戻す」ボタンを別に置かない）。**直しを落としたときは印を残さない** —
      戻せるものが無いのに「落としました」と出ると、手順書そのものが落ちたように読める
  - **③ 記憶の手入れ**（`lib/exec/memory.ts`）。上限にあふれたとき、
    落ちるのは「古いもの」であって「もう要らないもの」ではない → 満杯に近づいたら**畳んで濃くする**。
    **捨てるのではなく、まとめる。** 溜まったときだけ呼ぶ／**元より悪くしない**（畳めていなければ元のまま）
  - **④ 思い出す**（`lib/exec/recall.ts`）。前は承認済み成果物の**タイトルしか**渡していなかったので、
    **別の Work で調べた中身はどこからも届いていなかった**（憲法に「二度調べません」と書いてあるのに）
    - **日本語で効く探し方を選んだ** — `to_tsvector` は語を切れない／pgroonga は専用の演算子が要り、
      PostgREST から使うには `public` に関数が要って**それだけで警告が1件増える** →
      **`ilike` ＋ pg_trgm の GIN**（0030）。**関数を置かないので警告は0件のまま**
    - 探す先は **作ったもの / 決めたこと / 会話** の3つ。**ひらがなを捨てて言葉を拾う**（`termsOf`）—
      形態素解析を積むより**ずれ方が読める**。「会社」「今回」のような、どこにでも当たる言葉は捨てる
    - **往復は増やさない。** 読む道具として渡すと 1タスク=1往復 が崩れるので、
      **こちらが先に引いて依頼文に載せる**
  - **⑥ 育つと壊れるところを、先に直した**（社長の言い直し「AI社員が hermes みたいに
    学習していって欲しい」で読み直した）
    - **「いつ読むか」を誰も読んでいなかった。** 道具の説明には「ここが合っていないと
      誰も読めない」と書いて `when` を書かせているのに、選ぶ側は **`name` の字の重なり**
      だけを数えていた（「の」「を」「方」が当たるだけで上位に来る）。
      **社員が手順書を書けるようになった以上、棚が増えるほど当てずっぽうは悪くなる** →
      思い出すのと同じ `termsOf` に揃え、**`desc` も見て**、
      **フェーズ名とねらいも手がかりに入れる**（「調査のまとめ方」は、タスク名ではなく
      フェーズが言っている）。**1枚も当たらなければ載せない**（関係ない手順書は高いだけ）
    - **差し戻しが、手順書に返っていなかった。** 指摘は担当の**学び**にだけ残り、
      **その仕事の土台になった手順書は直らないまま**だった（「使いながら良くなる」が半分）。
      直しのタスクには**同じ手順書が載る**ので、依頼文に一言添えるだけで輪が閉じた —
      **覚えておく場所を増やさずに済んだ**
  - **⑤ 社長のこと**（`founderNotes`）。学びが「社員が仕事から覚えたこと」なら、
    こちらは**会社が社長から覚えたこと**。**モデルを呼ばない** — 判断に答えた／差し戻した、という
    **起きた事実**をそのまま1行にする（朝の報告と同じ）。読むのは計画・会話・実行の3か所
    - **表を増やさない** — 置き場は学びと同じ `agent_skills`（`source='learned'`）で、
      **`employee_id` が null のほうが社長のぶん**（スキル・設定と同じ書き方）
    - **見える・消せる**（メンバー画面 → 全員に効くこと → 社長のこと）。
      見えないところで会社が社長像を作らない
- **候補を「実際に需要があって、1人でできる仕事」にする（2026-08-26）** — 社長の指示
  「**実際に需要があって個人1人でもできるような仕事、ゴールの提案をして欲しい**」
  - **その2つが、どちらも入っていなかった。** `fit` は
    speed（速さ）/ cost（安さ）/ strength（得意との相性）で、
    **誰かが欲しがっているかは一度も問われていない**し、**1人で回せるか**も無かった
    （strength は工数ではない）。だから**速くて安くて得意に合うもの**を探すことになり、
    「小さな実用品の販売所」のような**抽象的で当たり障りのない案**に寄っていた
  - **軸を入れ替えた** — **需要 / 1人で回せる / 最初の1件まで**。
    cost は落とした（**お金がかかることは「1人で回せない」に吸収される**）。
    並び順も「得意との相性の高い順」から**需要の高い順**へ
  - **抽象に逃げられなくした** — 候補は `who`（誰が買うのか）と
    `first_one`（最初の1人をどこで見つけるか）が**必須**。
    「個人」「中小企業」「困っている人」は書いたことにならない、と道具の説明に書いた。
    `hours_per_week` も書かせて、**社長の使える時間と突き合わせる**
  - **確かめていないことを、候補の顔に出す**（`unsure`）。統括AIは **Web を見ていない**ので、
    需要は記憶から言っているだけ。**黙って自信ありげに出すほうが危ない** —
    AI社員の憲法の「未確認と印を付ける」と同じ作法
  - **需要は、作ったものを公開したときに確かめる**（憲法 ＋ `draft_plan` ＋ `checkPlan`）。
    **一度この順番を間違えた** — 最初は「最初のフェーズで外に出して確かめる（相手に直接聞く /
    先行の申し込みを取る）」と書いたが、社長の指示は逆だった
    （「**マーケティングを最後の方にして欲しい。最初に制作、準備をきちんとする感じで進めたい**」。
    前の LP の例と合わせて**2回言われている**）→ **順番は「作る → 整える → 公開する →
    そこから広げる」**に直した
    - **`market-first`**: 集客・営業・宣伝・SNS運用を**最初のフェーズにしない**。
      準備ができていないうちに広げても、来た人に渡すものがない
    - **`publish`**: それでも**「公開する」フェーズは必ず入れる**（作って終わりにしない）。
      出して初めて、欲しがる人がいるかどうかが分かる。
      **すでに売れている事業には要らない**ので `unproven` のときだけ見る
    - どちらも **fatal にしない**（言葉で見分けるので取りこぼすし、
      もう売れている事業なら広げる仕事から始めるのが正しいこともある）
    - **リスクは言った上で、社長の順番で通した** — 作り切ってから誰も欲しがらないと
      分かるのがいちばん高くつく。それでも**社長が2回言ったことは社長の判断**
  - **`PlanLimits` を誰も埋めていなかった。** 型は前からあるのに `checkPlan(plan)` を
    引数なしで呼んでいたので、「使える時間の中で回る形にする」の検査は**一度も動いていなかった**
    → `draftWork(goal, ctx, limits)` で通した
  - 0031（`who` / `first_one` / `unsure` / `hours_per_week`）。`fit` は jsonb なので列は無変更
- 次: 公開の前提（鍵）— Stripe / `CLOUDFLARE_API_TOKEN` / 実キーでの1周 ／
  **Google の入り口**（Supabase の Provider 設定 → `docs/RUNNING.md`）
- **早いほど得**: 画面を作らずに1タスクだけ API で走らせて成果物を見る（半日）。
  **APIキーが要る**（この環境には未設定）
- タスクなどの「追加」ボタンは置かない。**タスクは統括AIとの会話から作られる**
- 成果物: `design/canvas/*.dc.html` + `design/canvas/canvas.json` / `docs/design/*.md` / `docs/PLAN.md`
- **動くプレビュー**: https://onefound-yuseidayo88-3854s-projects.vercel.app
  ゼロの状態から始まる（ダミーは無い）。**本番の行き先は Cloudflare Workers**
  （判断ログ → `docs/design/05-tech-and-cost.md`）
- **Cloudflare は Worker 2本**（本番 `onefound` / レビュー `onefound-preview`）。
  ビルドと workerd 上での全画面の動作は確認済み。**残りはデプロイだけで、
  `CLOUDFLARE_API_TOKEN` が要る**（この環境からは `api.cloudflare.com` に出られない）
  → `docs/RUNNING.md`
- プレビュー: 画面 https://claude.ai/code/artifact/10950880-aa8e-430f-9bc9-48cec22bd920
  / 設計 https://claude.ai/code/artifact/9f7ac75e-3b7e-4662-a483-a3420b795125
  / 計画 https://claude.ai/code/artifact/b271ddac-42df-443f-8a7f-a431547ce4e5

## デザイン言語（確定事項）

参照: Radius Browser / ChatGPT Web。詳細は `design/canvas/DesignSystem.dc.html`。

- **3ペイン**: 左＝ナビ＋**チャット履歴**、中＝その画面の主役、右＝下の2つのどちらか。
  **チャット画面だけ右ペインなしの2ペイン**（ChatGPT と同じ）。ホームも右ペインなしの全幅
- **行き先は先に決める**（→ `docs/design/09-navigation.md`）。押せる顔をしていて
  **何も起きない**ものを置かない。ただし**行き先を持たなくていい**ものもある —
  押した結果がその画面の中で起きるなら（ワークフローの鎖を選ぶ、一覧の行を開く）、
  別の画面に飛ばすほうがむしろ答えていない。
  **開いている1件は URL に持つ**（`?open=<id>` / `?of=<id>`）ので、
  別の画面から「その1件」へ直接飛べるし、トップバーの ‹ › が本当に効く
- **右ペインは閉じた状態から始まる。** 一覧の行やカードを押すと、その1件のぶんだけ開く。
  **✕ で閉じたら右の端に何も残さない**（開き直すつまみを置かない）。
  戻り道は**トップバー右端の板アイコン。開いているあいだは出さない**
  （ペインの中に ✕ があるので、2つ置くと同じことを2回言うことになる）
- **右ペインは2つの形しかない**（→ `docs/design/08-panes.md`）
  - **パネル**（既定）— 選んだ1行の詳細と、画面そのものの付き添い。
    素の見出し ＋ ✕ だけ。**タブの器も ＋ も置かない**。行を選び直すと中身が入れ替わる
  - **タブ** — **持ち出して読み比べる「文書」だけ**。いまは **成果物** と **スキル（SKILL.md）** の2つ。
    ✕ で閉じて ＋ で足せる。**中身も一緒に入れ替わる**（`useTabs` / `?open=a,b&at=1`）。
    並びは動かさない・同じものを2枚開かない・✕ はそのタブだけ閉じる
  - 見分け方は「**選び直したら入れ替わってほしいか、残っていてほしいか**」。
    全部をタブの見た目にすると、撤去したはずの「ブラウザの真似」が小さく戻ってくる
  - 1つの中の行き先（概要 / 履歴 / 資料）はタブではなく**選ぶ列**。ペインの中に置く
- **背景は純黒 #000000**。明るい面は左レール #141414 のみ。カードは #0E0E0E、境界 #232323
- **書体**: `system-ui` / Noto Sans JP。**ウェイトは 400 のみ**（500以上を使わない）。
  UI 13px、本文 15px、ログは等幅12px。`-webkit-font-smoothing: antialiased`
- **角丸**: 行・ボタン 8 / カード・ペイン 12 / 入力欄 14〜24 / アバターと送信は円
- **文字を減らす**（参考: Midday / Fey / Linear）
  - **ラベル（小）→ 数字（大）→ 図形**。数字の下に説明文を置かない
  - **「タイトル＋補足の説明文」を反射で作らない。**補足は、それが無いと分からないときだけ
  - **数で言えるものは図形にする** — 進捗はバー、フェーズは分割バー、人数は粒子の点、
    凡例は色の点。文章で言い直さない
  - **同じことを2回書かない。**ラベルと値で言えているなら補足は消す
  - 見出しは**名詞1〜2語**。「いま何がどこまで進んでいるか」のような文にしない
- **枠で囲わない**（AIっぽさの正体は「全部が枠付きカード」。参考: Linear の設定画面 / 21st の Card 定義）
  - **面と枠を持てるのは「押せるもの・選べるもの」だけ** — カード、ボタン、入力欄、選択中の行、ペインの境界
  - **セクションは見出し＋中身だけ。**面も枠も置かず、**28〜46pxの余白**で区切る
  - **並ぶものはヘアラインだけで区切る。**外枠は付けない（表・リスト・工程）
  - **入れ子は1段まで。**カードの中にもう1枚の枠付きの箱を置かない
  - **アイコンを四角で囲わない。**行の先頭のアイコンは裸で置く。
    面と枠を持てるのは、アバターと成果物のサムネイルだけ
  - **「アイコンの四角 ＋ タイトル ＋ その下にサブタイトル」を既定にしない。**
    中身が何であっても同じ形になるので、どの画面も似てくる（＝AI感の正体のひとつ）。
    何が主役かで器を選ぶ（参考: Cloudflare / Grok / Whop の Analytics、Tailscale の Machines）
    - **数が主役** → アイコンを消し、**ラベル（小）→ 数字（大）→ 形**。
      内訳は棒にして、文章で言い直さない。色をつけるのは放っておけない数だけ
    - **名前が主役** → 二段に積まず、**事実を右に列として並べる**（表と同じ読み方にする）
    - **中身が主役** → サムネイルに**実際の書き出し**を出す（灰色の棒を置かない）
    - **人が主役** → 丸を許すのはアバターのときだけ
  - **同じ器を縦や横に並べない。**候補・通知・レーンはカードにせず、
    ヘアラインで区切った行や列にする。強調したい1つだけ左に色帯と薄い面を敷く
  - **状態のピルは「例外」だけ。**判断待ち・要確認・レビューにしか付けない。
    承認済・稼働中・完了は、ふつうの状態なので点＋素の文字で足りる。
    同じピルが2つ以上並んだら、それはもう情報ではなく模様（参考: Modal / Supabase / Tailscale）
  - **凡例（legend）を置かない。**凡例が要るなら、形のほうが間違っている。
    数えた件数を出すのは、放っておけないもの（判断待ち・要確認）だけ
  - **偽の階層を作らない。**「統括AI / タスク」のようなパンくずは、
    本物の親子（Work→その Work、メンバー→その社員、チャット→そのスレッド）があるときだけ。
    それ以外は画面の名前ひとつ（参考: Tailscale の Machines）
  - **ブラウザの真似をしない。**戻る・進む・再読込と `onefound://` のアドレス欄は撤去した。
    ここはブラウザではない
  - **同じことを1画面で二度言わない。**帯と見出しで同じ名前、ボタンと入力欄で同じ導線、
    バーと数字で同じ進捗 — どれかを消す
  - **一括選択のチェックボックスを置かない。**頼まれていない機能で表を埋めない
  - **下の余白を放置しない。**中身が上に寄って下が空くのは、器が大きすぎるのではなく
    次にやることを置いていないから（参考: Tailscale の「Add devices」ブロック）
  - 迷ったら外す。線を1本減らすほうが、足すより良くなる
- **触れたときの反応**（→ `app/globals.css` の「触れたときの反応」）
  - **色は増やさない。明るさだけで言う。** ホバーは白の薄い膜（3%）、押した瞬間は 5%
  - **ずれない。** 幅も高さも変えない。内側の影と色だけを動かす（枠を足して1px ずれる、をやらない）
  - 面が出るのは**指が乗っているあいだだけ**なので、「面と枠を持てるのは押せるものだけ」は破れていない
  - 5種類しかない — `.row`（一覧の1行）`.btn`（枠だけ・文字だけ）`.solid`（青・橙の面）
    `.icob`（アイコンだけ）`.card`（成果物・選択肢）。`.lnk` は文字が明るくなるだけ
  - **押せないものを押せる顔にしない。** 入力欄が空のときの送信ボタンは灰色で `cursor` も変えない
  - キーボードで回れる（`:focus-visible` に青の輪）。入力欄は器のほうが応えるので二重に囲わない
    - 押せる行は `<button>` にできない（中に `<Link>` が入る）ので `pressable()` を付ける
      （→ `lib/a11y.ts`）。`role="button"` ＋ `tabIndex` ＋ Enter / Space が揃う
    - **Esc で閉じる** — 右ペインも会社の切り替えも同じ作法
  - **動きを減らす設定の人には飾りの動きだけ止める。** 回る印と脈は「いま動いている」という事実なので残す
- **動きは1つの言葉で**（→ `docs/design/10-motion.md`）。`EASE = .32s cubic-bezier(.33,1,.68,1)`。
  速く始まってゆっくり止まる。**画面ごとにイージングを変えない**
  - **出だしを強くしすぎない。** 1フレーム目で4割進む曲線だと「滑った」ではなく「跳んだ」に見える
  - 動くのは — 左レール（幅0↔260）/ トップバーの戻り道（幅0、消さない）/ 右ペイン（`panein`
    `paneout`）/ 入力欄（伸びる・左右に動く）/ 画面の入れ替え（`template.tsx` ＋ `.page`）/
    ペインの中身（`.swap`）/ 一覧の選択（160ms）/ 質問の板（`.rise`）/ 浮き板（`.pop`）
  - **画面の入れ替えで透明にしない。** `opacity: 0` から始めると次の画面ができるまで中央が空に見える。
    例外は**ワークフローの盤面**（開くと**フェーズ1から順に、左から右へ**現れる。
    列ごとに 58ms ずらす。進み具合の帯は 0 から今の割合まで満ちる）。
    出たあとの濃さは `lit` が決めるので、`animation-fill-mode` は `backwards`
  - **URL の `?…` を書き換えるだけの操作でサーバーに行かない。**
    `router.replace()` は同じ画面でも RSC を取りに行く（ネットワーク越しなら数百 ms）。
    `useOpen` / `useTabs` / `useParam` は `history.replaceState` を使う。**通信ゼロ**
  - 左レールは消さずに**幅を 0 に**（閉じているあいだは `inert`）。
    右ペインは `panein` / `paneout`（閉じるときは畳み終わってから親に消してもらう）
  - **出入りは transition ではなく animation。** transition だと React の再描画を待って
    3フレーム止まって見える
  - **何もしていないときに CPU を使わない。** SVG の中で `<animateTransform>` を回すと
    その下の粒を毎フレーム計算し直す → 層ごとに `<svg>` を分けて CSS で回す。
    `left` ではなく `transform` で動かす。`/home` は 31% → 5%
  - **瞬きは組にして掛ける。** 22個の粒に1つずつ animation を掛けると、
    何もしていないのに22個ぶんのスタイル再計算が毎フレーム走る（`/home` が 6.5%、
    うち 5.6% がこれだった）。**組ごとに速さと遅れを変えれば、目にはばらばらに見える** → 2%。
    `will-change` を足しても効かない（合成に上がらない）
  - **行き先は、指が乗ったときに取る**（`components/ui/Go.tsx`）。Next の既定は
    「毎回サーバーで作る画面なら先に取らない」なので、素の `<Link>` だと押してから取りに行く。
    かといって**全部のリンクに `prefetch` を付けたら、逆に遅くなった**（2026-08-24。
    レールの行き先7つ＋会話6本が画面に入っただけで先に取られ、**1画面ひらくたびに
    10〜16画面ぶんのサーバー描画**が走っていた。実測 21本 → 3本）。
    `hover` / `focus` / `touchstart` で取れば、押すまでの間に間に合う
  - **認証で毎回 Supabase に行かない。** `getUser()` は毎回認証サーバーへ往復する。
    middleware は先読みの1本1本にも走るので、1画面で往復が数十回になる → `getClaims()`
  - **Cloudflare は遅くない。** workerd と Node の差は1回 7〜10ms（実測）
  - **機械で確かめる** → `tools/check/`（レイアウト / キーボード / レール / CPU / 動き）
- **器の最小幅は 1440px**（左レール 260 ＋ 盤面 1148 ＋ 余白 32 → `SHELL_MIN`）。
  **これより狭い窓では中身を縮めず、窓のほうを横に滑らせる**（→ `docs/design/08-panes.md`）。
  画面ごとのブレークポイントは作らない。1本の規則で全画面が同じ振る舞いになるほうがいい
  - 置く前は、1120px で右ペインを開くと `/tasks` のタイトル列が 0px まで潰れて中身が消えていた
- **リンクだから青、にしない。** `a { color: inherit }`。色は意味にだけ使う
- **色は意味だけに使う**: 青＝次に押すもの（1ペインに1つ）、緑＝済・推奨、橙＝判断待ち、赤＝停止。
  値は「現在地」の確定値（面 #1A73E8 / #1E8E3E / #E37400 / #D93025、
  文字 #669DF6 / #5BB974 / #FDD663 / #F28B82）
- **AI社員は色で識別**（アイコンでの分類はしない）。アバターは**粒子で描く**
  （→ `docs/design/03-agent-schema.md`）
  - 点の緯度リングでできた**球** ＋ **芯の格子**（モアレ）＋ 外へ伸びる**スポーク** ＋ **散り** ＋ **芯**
  - 層ごとに違う速さでゆっくり回る。社員ごとに変わるのは**色だけ**、形は同じ言語
  - **40px未満は球をやめて同心リングと芯だけ**にする（表のアイコンで潰れない）
  - **待機中は 0.4 倍に沈めて回転を止める**。統括AIだけ白 #D2D2D2
- 読ませずに分かるようにする: 比較は棒グラフ、進捗はバー、状態は行左3pxの色帯
- **画面ごとに器を変える**（全部を同じ縦積みカードにしない。似た画面は失敗）
  - **タスク**＝**いつやるかで束ねる**（参考: Todoist Upcoming / Attio Tasks / Evernote Tasks）。
    表はやめた — 順番は正しくても「いつまでに何をすればいいか」を目で追う必要があった。
    束は **きょう / あした / 今週のうち**。空の束は出さない。
    **判断待ちは束から出して、いちばん上の帯に置く**（あなたが決めるものだけ面と枠を持つ）。
    **完了は下の1行に畳む**（`?done=1` で開く）。
    **状態はタイトル前のアイコン**（状態の列は置かない）
  - **成果物**＝グリッド（参考: Craft / Frame）。**プレビューは中身を出す**。
    灰色の棒ではなく、実際の書き出し2〜3行を小さく出して見分けられるようにする
  - **通知**＝**読むものではなく片づけるもの**（参考: Linear Inbox / Plane Inbox / Lemni）。
    時系列フィードはやめた — 開く先が決定事項・成果物とばらばらだった。
    **画面そのものが2列**（左＝未処理の積み / 右＝中身と行動）。右ペインは使わない。
    左はいつも1件選ばれている。**この画面から出ずに終わる** — 開いて、決めて、次の未処理へ。
    左右の帯（きのう片づけた / 行動の行）は同じ高さ 56 で、`COMPOSER_H` ぶん上に逃がす。
    **1件も無くても器は変えない**（2026-08-24）— 空のときだけ中央に1枚を出すと、
    通知が来た瞬間に画面の形ごと変わる。2列は置いたまま**言葉だけ変える**。
    空の積みに文字は置かない（上の行の `未処理 0` が言っている）
  - メンバー=3段の行（↑）/ 採用=候補カード / 決定事項=台帳タイムライン /
    **Work=一目で状況が分かる1枚**（参考: Upwork / Squarespace / Linear）—
    上に事実の帯（進捗 / いまのフェーズ / **判断待ち** / 残り / AI社員）、
    その下に **フェーズ全部**（進捗バー・タスク数・日付）→ **いま動いていること**
    （フェーズをまたいで実行中と判断待ちを並べる）→ **成果物**。
    右レールに 最新の状況 / 決めたこと / AI社員。タブに隠さない /
    チャット=2ペインの会話 / ホーム=3ビュー（オフィス / デスク / ワークフロー）

## 実装時に必ず参照

- **AI社員の定義フォーマットと初期ロスター** → `docs/references/agency-agents.md`
  （https://github.com/msitarzewski/agency-agents · MIT · 294体/20カテゴリ）
