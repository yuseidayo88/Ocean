# 進行計画

最初のマスタープロンプトで決めた **Phase 0 〜 10** を、いまの決定に合わせて更新したもの。
フェーズの番号と区切りは原文のまま。中身だけ、Phase 0〜2 で決まったことを反映しています。

## 守るルール

原文の「Claude Code Working Rules」24項目を、いまの言葉でまとめ直したもの。

### 進め方

- **フェーズを飛ばさない。** 各フェーズの終わりで**必ず停止**する
- 停止するときは5つを報告する — **何をしたか / 変更点 / 確認できるもの / 次の予定 / 判断が必要なこと**
- **明示的な承認なしに次へ進まない。** 「ついでに次も作っておきました」は禁止
- 一気に全部作らない。次フェーズの実装を先回りしない
- 大きな仕様変更を、理由を説明せずにしない

### プロダクト

- **特定の業種にハードコードしない**
- **Chat 中心の UI にしない。** 主役は「会社が動いている状態」
- **偽の進捗を出さない。** 画面の状態＝バックエンドの状態
- **重要な情報を会話履歴だけに保存しない。** 決定は構造化して残す
- **成果物を会話から分離する**
- **高度なAI設定（モデル・思考量）は一般ユーザーから隠す**
- AI社員の人数より **Work の完了**を優先する
- セキュリティとプライバシーを速度のために犠牲にしない
- **外に出る道具（メール送信・公開・デプロイ・支払い）は、必ず社長の Approval を挟む**。
  調査・下書きは自動でいい。取り消せない一歩だけ、人が押す

## 全体像

| Phase | 名前 | 状態 | 成果物 |
|---|---|---|---|
| **0** | 方向性（`/design` Product Discovery） | ✅ 完了 | 3案 → **案A Mission Control** 採用 |
| **1** | 画面設計 | ✅ 完了 | 23画面 ＋ デザインシステム（A群の設計ラウンド込み） |
| **2** | 設計（Architecture Review） | ✅ 完了 | `docs/design/` 7文書 |
| **3** | 土台 | ✅ 完了 | Next.js 16 + OpenNext / Supabase 27表 ＋ RLS（**残りはマジックリンクを踏むところだけ**） |
| **4** | 画面の骨組み | ✅ 完了 | ダミーデータで23画面が触れる |
| **5** | Work の作成 | ✅ 完了 | ゴール入力 → 質問 → 採用提案 → 計画（**Case A / B / D が通る** — B / D は 2026-08-24 に接続） |
| **6** | 計画の承認 | ✅ 完了 | 承認 → `active` / 引き直し / Work 画面が本物を読む |
| **7** | モデル実行基盤 | ✅ 完了 | AI社員が1タスクを最後まで走る（ポンプ方式・進捗はDBが導出） |
| **8** | 調査ワークフロー | ✅ 完了 | 成果物 → 統括AIの一言 → 承認 / **差し戻し（言葉がそのまま直しタスクになる）** |
| **9** | 判断と受け渡し | ✅ 完了 | 判断に答える → 決定が次の実行の文脈に入る（decision_refs に読了記録）→ フェーズ承認で次のタスクを引く |
| **10** | 社員を増やす | ✅ 完了 | 採用が本当に効く（定義で採る・使い回す）／ メンバーに本物の在籍が並ぶ |
| **11** | 課金と公開 | 🔶 骨格まで | 台帳への記帳・残高で止まる・プラン画面。**決済（Stripe）と公開（CF）は鍵とトークン待ち** |

**現在地（2026-08-24）**: Phase 0〜10 は完了。Phase 11 は骨格まで
（原価の記帳・残高で止まる・プラン画面。**決済と公開は鍵待ち**）。
横断の宿題（朝の報告・ダミーデータの実用化）と、再点検2回も済み —
1回目は**同時操作の穴**（実行の二重起動 / 二度押しの二重書き込み → atomic claim ＋ 0015 の一意 index）、
2回目は**システムの回復性**（止まった実行の回収 `reclaimStalled` / FK の覆い index 31本 = 0016）。
**入口の Case B / D も統括AIに接続した**（2026-08-24 → 横断の表「入口の接続」）。

残っているもの（すべて外部待ち）:

| 残り | 待っているもの |
|---|---|
| Stripe（決済） | APIキー |
| Cloudflare 公開 | `CLOUDFLARE_API_TOKEN`（ビルドと workerd 上の動作は確認済み） |
| 本物のモデルで1周 | Vercel 上での確認（この開発環境からアプリが openrouter.ai に出られない。**モデルは GPT-5.6 Luna**） |
| Web取得（URL の中身を読む） | OpenRouter の鍵（web プラグインは従量課金なので既定オフ）。**取り込んだ URL は名前だけ材料になる**と画面にも書いてある |
| ~~複数社（memberships）~~ | **見送り**（2026-08-24・会社は1つだけ → 下の見送りの台帳） |

Phase 8 が最初の山場です — **通りました**（成果物 → レビュー → 承認 / 差し戻しの輪が回る）。
7日トライアルで見せるのはここです。

Phase 3 に入る前の**追加の設計ラウンド**（下記「Phase 3 に入る前にやること」）は実施済みです。

---

## Phase 3 — 土台

**目的**: 中身は空でいいので、デプロイされて動くアプリを1つ作る。

| | |
|---|---|
| やること | Cloudflare Workers に Next.js を載せる（OpenNext）／ Supabase の Postgres と Auth ／ スキーマと RLS ／ マイグレーション ／ 多言語の土台 ／ エラーの扱い |
| **0番目** | **使う SDK が Workers 上で動くことの確認**。ここで詰まると全部止まるので最初にやる |
| 完了条件 | ログインできて、自分のアカウントの空のホームが見える |
| 参照 | [01 データモデル](./design/01-data-model.md) / [05 技術構成](./design/05-tech-and-cost.md) |

## Phase 4 — 画面の骨組み

**目的**: ダミーデータで、23画面を実際に触れるようにする。AI はまだ繋がない。

| | |
|---|---|
| やること | 左レール＋チャット履歴 ／ ホーム4ビュー（オフィス/デスク/進捗/ワークフロー）／ 入口3画面 ／ Work ／ タスク ／ 成果物 ／ メンバー ／ 決定事項 ／ 通知 ／ 右ペインのタブ |
| 完了条件 | ブラウザで全部見られて、キャンバスの設計と一致している |
| ここが本番 | **本当のUI/UXレビューはここ。** 静止画では分からないこと（余白・動き・実データの長さ・空の見え方）を、動くものの上で直す |
| 注意 | **ここで偽の進捗を入れない。** ダミーデータであることが分かる形にする |

## Phase 5 — Work の作成

**目的**: ゴールを言うと、計画とメンバー案が出てくるところまで。

| | |
|---|---|
| やること | ゴール入力 ／ **フェーズ / Work の振り分け** ／ 確認の質問（入力欄の上の板）／ **Discovery Mode**（何をやるか決まっていない人）／ **既存事業の取り込みと診断** ／ 社員の推薦 ／ 採用 ／ ロードマップ生成 ／ 最初のタスク |
| 完了条件 | 4つの入口が全部通る — 決まっている人（Case A）／ 決まっていない人（Case B）／ 新規事業（Case C）／ 既存事業（Case D） |
| 参照 | [06 Work の粒度と、用事](./design/06-work-and-scope.md) / [07 統括AIの憲法](./design/07-executive-constitution.md) |
| 原文の要件 | **ロードマップ全体は作るが、後半フェーズの詳細タスクを最初から固定しない。** 直近フェーズだけ詳細化し、前フェーズの結果と判断を踏まえて再計画できる形にする |

## Phase 6 — 計画の承認

**目的**: 承認を押すと、状態が本当に変わる。

| | |
|---|---|
| やること | 計画を直す ／ 統括AIに聞く ／ 承認して開始 ／ 状態遷移 |
| 完了条件 | 承認したら Work が `active`、フェーズが `active`、タスクが `queued` になる |
| 参照 | [04 状態遷移](./design/04-state-machines.md) |
| やったこと | `approveWork` / `reviseWork` / `getWork`（`app/actions/work.ts`）／ Store の `approve` `revise` `getWork`（Supabase・メモリの両方）／ **Work 画面が本物を読む**（`lib/exec/work-view.ts` の `WorkView` ＋ `fromDummy` / `fromLive`）／ `works.plan_draft` |
| **タスクは `queued` のまま** | 走らせるのは Phase 7。ここで `running` にすると、動いていないのに動いていることになる |
| ここで見つけた本番の穴 | ① 全表の `account_id` に既定値が無く、Phase 5 の insert は本番でだけ NOT NULL 違反になっていた（`0007_account_default.sql`）② `questions.thread_id` が NOT NULL なので、Work のスレッドを先に作らないと質問が1問も置けなかった ③ 同じ定義の社員を Work ごとに採用していた（調査担当が何人もできる）|
| まだ繋がっていない | 承認した Work は**URL で開ける**が、ホーム（オフィス / デスク / 進捗 / ワークフロー）と左レールの一覧は**まだダミー**。Phase 7 で実行と一緒に繋ぐ |

### Phase 6 のあとの棚卸し（2026-08-23）

Phase 0〜6 を通しで見直して、**17件**直しました。**`npm run lint` が壊れていた**
（Next 16 で `next lint` が消えていた）ので、6,000行が一度も検査されていない状態でした。

| | 直したもの |
|---|---|
| **機能の穴** | **質問の板が答えを受け取らなかった**（選択肢・‹ ›・自由入力・スキップの4つとも飾りで、`answerQuestion` は誰も呼んでいなかった）／ lint が壊れていた ／ `say5` の案内が全部「Phase 5 から」のまま ／ 質問とタスクの並びが不定 ／ 生の DB エラー文が画面に出ていた ／ `ownerHint` を集めて捨てていた ／ `stopReason` を誰も見ていなかった |
| **設計の食い違い** | 深さ→モデル（`tierFromEffort`）が決めごとと逆 → `effort` を `RunInput` に通した ／ Haiku のモデルIDに日付が付いていた ／ フェーズの `review` がDBに無かった |
| **片づけ** | 色が3か所（22ファイルのベタ書き → `tokens.ts` 一本化。生の色 470→65）／ 死んだコード（`ModelPick` `EffortPick` `EffortSlider` `NOTICE_GROUPS` `PaneNote`）／ i18n が自分のルールを破っていた ／ 27表/28表・21画面/23画面 の食い違い |

## Phase 7 — モデル実行基盤

> 原文では「OpenRouter Agent Runtime」。**Anthropic と OpenAI に直接つなぐ**方針に変えたので名前を変えています。

| | |
|---|---|
| やること | `AgentRunner`（Durable Object の中で1ターンずつ回す）／ `ModelProvider`（2社を吸収）／ 階層（`deep` / `standard` / `fast`）／ 社員の Skills と Rules を文脈に入れる ／ 実行ログとトークン計上 |
| 完了条件 | 1タスクが最後まで走り、`run_steps` が画面にリアルタイムで流れる |
| 参照 | [02 実行モデル](./design/02-executive-model.md) / [05 技術構成](./design/05-tech-and-cost.md) |
| やったこと | `lib/run/`（道具4つ: log_step / write_deliverable / ask_decision / finish）／ `lib/roster/`（7人の定義文）／ Store に実行の口（memory / supabase 同型）／ **ポンプ方式** — Work 画面を開いているあいだ、次の queued が順に走る（DO もキューも無い環境で死なない。見ていない間に料金だけ増えない）／ 進捗は `run_steps` → 引き金 → `tasks.progress` の導出（0012。アプリは直接書けない）／ 成果物の本文は `deliverables.body`（R2 に出られるまでの置き場） |
| DO はまだ | Durable Object / WebSocket 押し出しは Cloudflare に出るとき（`CLOUDFLARE_API_TOKEN` 待ち）。それまでは 2.5秒のポーリング |

## Phase 8 — 調査ワークフロー　← 最初の山場

**目的**: **AI社員が実際に働いて、成果物が1つ出てくる。**

```
調査タスク → 調査担当が実行 → 成果物 → 統括AIのレビュー → あなたのレビュー
```

| | |
|---|---|
| やること | 調査担当1体を最初の実働社員として動かす ／ Web検索・Webフェッチ ／ 成果物の保存（R2）／ レビューと承認 |
| 完了条件 | **登録から2日以内に成果物が1つ出る。** これが7日トライアルの中身 |
| 参照 | [03 AI社員スキーマ](./design/03-agent-schema.md) |
| やったこと | 統括AIのレビュー（成果物に一言。fast の目で1度見る）／ **承認 / 差し戻し** — 差し戻しは社長の言葉がそのまま直しタスクになり、同じ担当に積まれてポンプが走らせる ／ フェーズの全タスク done → `review` ＋ 判断待ち通知 ／ 成果物画面が本物を読む（先頭に混ぜる。サムネイルは実際の書き出し）|
| Web検索 | OpenRouter の web プラグイン対応を入れた（`OPENROUTER_WEB=1`）。**既定オフ** — 検索は従量課金で、無料テストを黙って有料にしないため |
| R2 はまだ | `CLOUDFLARE_API_TOKEN` 待ち。それまで本文は `deliverables.body` |

## Phase 9 — 判断と受け渡し

| | |
|---|---|
| やること | 統括AIの推薦 ／ 判断のUI ／ 承認 ／ **決定事項の構造化保存** ／ 次の社員へ文脈を渡す |
| 完了条件 | 決めた内容が次の社員の依頼文に必ず入り、`decision_refs` に読んだ記録が残る |
| やったこと | 判断のUI（タスクの右ペイン＋決定事項の台帳。**その場で決められる**）／ 決めると decisions → decided・タスクは queued に戻り、**次の実行は「決めたこと」を文脈に持って**走り直す（読了は decision_refs。二重に読んでも1行）／ フェーズ review → **次のフェーズへ進める** — 統括AIが前の成果物と決定を見て次のタスクを2〜4件引いてから進む（先を固定しなかった計画の、引き直しの本体）／ 最後のフェーズなら Work → done |
| 見つけた穴 | **decisions が消せた。** 追記のみの引き金は UPDATE しか見ておらず DELETE が素通りだった → `0013` で revoke（探針で実証してから塞いだ） |

## Phase 10 — 社員を増やす

| | |
|---|---|
| やること | 戦略担当等を追加 ／ Work の内容に応じた動的な分岐 ／ 受け渡しをホームのフロー図に出す |
| 注意 | **業種別にロードマップをハードコードしない。** 必要な職種から順に |
| やったこと | 採用ページとメンバー画面の「採用する」が employees に書く（**候補の id ではなくロスターの定義で採る** → `lib/roster/` の7人）／ 同じ定義は使い回す — 二度押しても2人にならない ／ メンバー画面にダミーの4人のあと本物の在籍が並ぶ（約束・できることは定義文から）／ 担当ごとの実行は Phase 7 から効いている（owner_hint で引き当て、定義文が頭に載る） |
| 分岐について | 誰を薦めるかは統括AIの `propose_hires` / `draft_phase_tasks` がゴールと前の結果から決める。**業種別のハードコードはしていない**（決め打ちの FakeProvider だけが固定） |
| ホームのフロー図 | 実データに接続済み（→ 横断「ゼロ状態」。`lib/live/home.ts`） |

## Phase 11 — 課金と公開（追加）

原文にはありませんが、料金プランを設計した以上、必要です。

| | |
|---|---|
| やること | 7日トライアル ／ 「閲覧のみ」への自動降格 ／ Stripe（月払い・年払い・プラン変更・追加トークン）／ 利用規約とプライバシー（**成果物の権利** と **学習に使わない**の明記） |
| やったこと（骨格） | 実行の原価が必ず台帳に落ちる（引き金 `run_ledger`。アプリは台帳に書けない — 残高の偽造を探針で遮断）／ 残高が尽きたら Work ごと `paused`（通知は積もらない）／ タダの実行は 0 で記帳（`billedCostUsd`）／ 請求とプラン画面 `/billing`（トークンの数字を出していいのはここだけ） |
| まだ | Stripe（鍵が無い）／ CF 公開（`CLOUDFLARE_API_TOKEN`）／ 複数社（memberships） |

## 横断 — 自分から動く会社（Phase 7〜11 のあと）

チャットボット（Grok bot 等）は聞かれるまで黙っている。**会社は先に言う** — ここが対抗軸。

| | |
|---|---|
| やったこと | **朝の報告** — その日はじめて器を開いたとき、きのうの動き（実行・要確認・判断待ち・停止）を統括AIが1通にする（`morningBrief`。日付で重複を止め、動きが無い朝は黙る）／ **ポンプ** — 開いている画面が次の queued を起こす（見ていないところで料金だけ増えない）／ ダミーデータを「実際に使いそうな人」の物語に揃えた（会社名の名札をやめた） |
| 再点検①（同時操作） | 実行の二重起動（タブ2つで同じタスクが二度走る）→ `startRun` を atomic claim に ／ 差し戻し・フェーズ承認の二度押しで二重書き込み → review のものだけ動かす ／ 朝の報告と採用の一意性を 0015 の一意 index でDB側でも守る ／ 失敗した実行も使ったぶんは記帳 ／ 朝の「その日」は社長の側の日付で数える |
| 再点検②（回復性） | サーバーが途中で入れ替わると実行が running のまま固まり**ポンプが永久に止まる** → `reclaimStalled`（10分で失効。はじめてなら積み直してもう一度、二度目は blocked ＋ エラー通知）／ FK の覆い index 31本 ＋ RLS の行ごと再評価1本（0016） |
| 標準スキルと学び | **標準スキルは見せる**（builtin 3枚を播種。切れるが消せない・「標準」タグ）／ 実行が有効なスキルを最大2枚読み、used_count が進む ／ **学び** — 社員が `note_learning` で書き溜め、次の実行の依頼文に載る。設定ペインで見える・消せる。**ルールには自動で書かない** |
| 会社の記憶と版（2026-08-24） | **会社の記憶** — 実行の依頼文に「会社で承認された成果物の索引（最新5件）」、チャットの文脈に「決めたこと（最新5件）」が載る ／ **差し戻しの指摘は担当の学びに残る**（直しタスクは1回で消えるが、指摘は次の仕事でも効く）／ **成果物の版** — 同じ Work で同じタイトルは同じ lineage の新しい版（v2表示・旧版は superseded で隠す。器は 0001 が最初から持っていた）／ **runs.model** — どのモデルで走ったかを記録（0018。ガバナンスの土台）／ **⌘K が会社への口に** — 検索に当たらなくても「統括AIに頼む」で会話が開く |
| ゼロ状態（2026-08-24） | **ダミーデータを撤去し、空の会社から始まる**ようにした。画面はぜんぶ store を読む ／ **ホーム4ビューが実データ**（`lib/live/home.ts` が live → 絵の唯一の変換点。Work が無ければ `/home` は `/start` へ）／ チャットの返事が本物（fast 1往復。鍵が無ければ「仮の返事」と名乗る）／ スキル（SKILL.md）の読み込み・トグル・削除が本当に保存される ／ 検査は「空の17画面」＋「埋まった9画面」の2段に作り直した |
| チャットが入口（2026-08-24） | **会話を1か所にした。** 入口の3つ（ゴールを書く / まだ決まっていない / すでに事業がある）は、どれも**新しいチャットを1本作る**。`/discovery` `/discovery/result` `/import` `/diagnosis` `/work/new` は撤去 ／ 統括AIが道具8つを持ち、返事に**カード**を添える（質問 / 候補3つ / 診断 / Work を作る確認）／ **カードは id しか持たない**ので中身は常に最新 ／ **動くのは最新のカードだけ**（参考: ChatGPT / Claude）／ **1チャット = 1 Work**、しかも**確認を押すまで作らない** ／ スレッドが条件と事業を覚える（0022）／ E2E は入口3つとも会話で通す |
| 入口の再点検（2026-08-24） | 3レンズ（正しさ / デザイン言語 / 整合性）＋反証で18件。**全部そのまま成立**（反証0件）。直したもの: **不変条件9の抜け道**（`discovery_candidates` を revoke しても親 `discovery_sessions` の DELETE が残り cascade で候補ごと消せた → 0021。0013 と同型）／ **条件を変えるが跳ね返る**（候補が出たあと条件を直す道が塞がっていた → `?edit=1`）／ **切れた1往復が無言の空振り**（max_tokens を見ておらず、押しても何も起きない → `checkStop` ＋ 16000）／ **同時に足すと事業が複数できる**（ドロップ2枚で profile が2つ → 直列化）／ **その列が1回の失敗で永久に詰まる**（catch 無し）／ **?s= / ?p= を検証せず deep 1往復を捨てる**（無ければ作り直す）／ **診断→Work が二度立つ**（候補の `adopted_work_id` に当たる記録が無かった → `linkFinding`）／ **NaN が `??` を素通り**（`週NaN時間` がゴール文に焼き込まれる → `Number.isFinite`）／ **`ask` だけ無検証キャスト**（options が落ちると板で落ちる → `toQuestions`。Phase 5 の道も同じだったので一緒に直した）／ デザイン言語5件（診断の重さに赤橙を使っていた・偽の進捗バー・操作説明コピー・`条件 N / 5` の食い違い・隣り合う2画面で条件の語が別） |
| 実キーで触って出た8件（2026-08-24） | 統括AIが「ただのチャット」になっていた。**カードが出ない**（前の直しで「道具は必要なときだけ」と緩めすぎた）→ 「社長に何かを尋ねるときは**必ず ask**。本文で質問を並べない」を強い決めごとにし、**入口の一言の往復は道具を必ず使わせる**（`toolChoice: 'required'` を3つの通り道に足した）／ **知らないまま提案していた** → 条件が3つそろうまで候補を出さない・序盤ほどやさしい質問をたくさん ／ **「何ができたら終わりですか」が赤い1行**（`ask_end` は選択肢を持っているのに捨てていた）→ 選んで答える形（`EndAsk`）／ **候補は3つとも選べる**（行そのものが選ぶもの・指が乗れば光る。推しの印は緑帯とタグだけ）／ **選択肢に自由入力の行**（鉛筆・その場で書く）／ **答えた条件が1行につながっていた** → 2行組＋`pre-wrap` ／ **「〇〇しています」を出し続ける**（`tool_begin` → `{"s":…}`。道具の名前は引数より先に届く）／ **モデル名は聞かれたら答える**（憲法＋統括AI自身に自分のモデル名を渡す）／ **Work が無い会社では会話が左レールに出なかった**（`/start` で丸ごと空にしていた） |
| チャット再構築の敵対レビュー（2026-08-25） | 3レンズ（駆動ループ / 双子ストア / 方針とfakeの忠実さ）×反証2人の workflow で11件。**確定して直したもの**: ①**Supabase の addChat が `refs: null` を挿していた** — 列は `not null default '[]'` なので、**Supabase を設定した瞬間にカード無しの全発言が 23502 で落ちる**（今まで出なかったのは本番がメモリ運用だったから。書きは `card ?? []`、読みは kind を持つ object のときだけカードに）②**絞られた往復の空振りが「empty chat turn」で全体をエラーにしていた** — push 中は空のまま返し、続きの輪に次を出させる ③**record の発火が `→` 1文字**（ふつうの発言まで写す往復に送られた）→ 板の実形 `改行＋→ ` だけ ④**カードが出た往復では答えを写し損ねたまま進んでいた** → カードの有無に関係なく写す ⑤**/api/chat に maxDuration が無かった**（途中で切られると返事が黙って消える）＋ ⑥**stream の「応えた」判定が途中の行でも立っていた** → done/err だけ ⑦続きの輪の各往復に try/catch（何か書けていれば成功、何も書けなければ正直に失敗）⑧リロード/2タブの二重返事 — **最後が社長の発言でなければ返さない** ⑨探索の器が無い探索スレッドは replyTo が作り直す ⑩採用の二重ガード（採用済みならその Work へ）⑪E2E の待ち文字列をカード固有の語に。**残した狭い窓（台帳）**: 同時2本の replyTo / 同時押しの採用は数ミリ秒の窓が残る — 閉じるにはストア側の atomic claim（startRun と同型）が要る。fake は record / push-ask 分岐を E2E で踏んでいない（決め打ちが常に条件を写すため）— 実キーの通し確認が要る |
| チャットの完全構築（2026-08-24） | 「どう会話を始めて何を聞くか」から作り直し。**会話の方針**: 分野を最初に選択肢で聞かない（無数にあるので一覧は無理）— 1巡目はやさしい質問（時間/お金/避けたい/得意）、2巡目の方向は**答えから導いた選択肢＋自由入力**。案を持って来たら**その案のバリエーション**を提案。**続きの保証**: 探索中の統括AIの番は必ずカードで終わる — 写し損ねた答えは record 往復で回収し、push の往復は**道具を1つに絞る**（required でも別の道具に逃げるので、絞るのが唯一確実）。探索の器は入口で先に作る。**思考ログ**: reasoning を開示するモデルは断片を流し見せ（`{"th":…}`）。**保存先の正直さ**: Supabase 未設定のデプロイは書き込みが関数の一時メモリに入り消える（Vercel が実際この状態 — 会話 id `t-1` が証拠）。レールに赤1行で警告し、環境変数の設定を促す |
| 候補が具体的にならない・Work が立たない（2026-08-24） | 実キーで通しで触ったら、**①案が抽象的**（「小さな実用品の販売所」「受注型テンプレート制作」— 何に関する事業か分からない）**②完了を聞き返される**（候補を選んだのに「何ができたら完了ですか」がまた出る）**③Work が作れない**（「計画を引けませんでした（中身が空でした）」）。直したもの: **分野を先に聞く**（`Conditions.interests`。選択肢＋自由入力。**分野が空のあいだは候補を出さない** — `canPropose`。候補の名前は「誰に何を」が分かる形に）／ **完了の定義は候補が持つ**（`ending`。0023。選ぶ前に読める。`ask_end` は終点そのものが無い依頼だけの最後の手段）／ **空の計画で終わらせない**（1往復で道具5つは速いモデルには重い → 空なら計画だけをもう一度、道具1つ・`toolChoice: 'required'` で頼み直す。名前の無いフェーズは通さない） |
| 読み込みが遅い（2026-08-24） | 「全体的に、チャットを開くときとか遅い」。**往復の数を数えた**（`tools/check/net.mjs`）ら、1画面ひらくのに **21本**。原因は `prefetch` の付けすぎ — レールの行き先7つ＋会話6本が画面に入っただけで先読みされ、**1画面ひらくたびに10〜16画面ぶんのサーバー描画**（それぞれ認証を通ってDBを読む）が走っていた。直したもの: **指が乗ったときだけ取る**（hover / focus / touchstart）／ **器の中身は画面と一緒に**（レールと会社名をサーバー側の layout で読む。クライアントの2往復が消える）／ **会話もサーバーで組んでから渡す**（`ChatView`。開く→器→もう1往復、をやめた）／ **入力欄の宛先は開いたときだけ**／ **朝の報告はその日いちど**／ **`getThread` の2本を同時に**／ **Vercel を東京（`hnd1`）に**（Supabase が ap-northeast-1）。**21本 → 3本**（実測） |
| 統括AIが「ただのチャット」になっていた（2026-08-24） | 実キーで触った社長から8件。**①カードが出ない** — 前の直しで「道具は必要なときだけ」と緩めた結果、質問も候補も出さず文章だけ返すようになっていた（本文に箇条書きを並べて「この中から選んでください」まで書いた）。「社長に何かを尋ねるときは**必ず ask**」を憲法級にし、入口の一言で始まった往復は `toolChoice: 'required'` で道具を必ず使わせる。**②答えたあと止まる** — 条件を書き留めただけで往復が終わり、催促してやっと候補が出た。**止まらない仕掛け**（`lib/exec/reply.ts`）: カードが1枚も出なかった往復は、そのままもう1回だけ走らせる（足りなければ質問、そろっていれば候補）。決め打ちのプロバイダも本物と同じ振る舞いにしたので E2E がこの道を通る。**③考えている印が入力欄の裏**→流れない場所に貼る。**④質問の最後の1問だけ形が違う**→送ったら全部チップ。**⑤候補が1つだけボタンに見える**→面の色をやめ「おすすめ」の文字だけ、バーは5つの刻み。**⑥Work を作る前に確認＋作っている姿**。**⑦空の計画を作らない**（フェーズ0の壊れた計画案が実際にできた）。**⑧モデル名は聞かれたら答える**（通り道は言わない） |
| 流す道が別の関数だった（2026-08-24） | 前の直しを入れたら**会話ができなくなった**（「このチャットは見つかりませんでした / もう一度」）。**`/api/*` はページとは別の関数として配られる**（Vercel も Cloudflare も）ので、**メモリのストアがそこからは見えない** — ページ側のサーバーアクションが書いたスレッドが、流す道からは**まるごと存在しないように見えていた**。直したもの: 見えなかったときは `{"fallback":true}` とだけ返し、画面は**流さない道**（`chatReply` ＝ ページ側の関数）で取り直す ／ 落ちる前に会話を読み直し、**もう返事が入っていれば頼み直さない**（線が切れただけのとき、二度払って同じ返事が2つ並ぶ）／ `/chat/new` から始める道を E2E に足した（`/start` を通らないので、返事を頼む相手が変わる） |
| チャットをまともにする（2026-08-24） | 実キーで触った社長から「なんで返事がありませんってなるの / まともにチャットすらできない」。**原因は「道具を呼ぶ往復に本文が来ない」** — OpenAI 互換のモデルは `tool_calls` を返すとき `content` を空にするので、質問や候補のカードを出した往復は本文が空になり、画面に「（返事がありませんでした）」だけが並んでいた。直したもの: **本文の保証**（足りないときは道具を渡さず、もう一度だけ短く書いてもらう＝`prose`）／ **決め打ちのプロバイダを本物に合わせた**（道具の往復では黙る。本物と同じ道を通さないとこの穴は見つからない）／ **返事を流す**（`app/api/chat/route.ts` の行区切り JSON ＋ `lib/chat/stream.ts`。最初の1文字から読める。流せない環境は `chatReply` に落ちる）／ **右ペインも同じ統括AI**（別brainの `sendChat` は削除。カードは「◯◯があります · チャットで開く」の1行で会話へ渡す）／ **本文のマークダウン3つ**（`#` `-` `**`）／ **倒れたら理由と「もう一度」** ／ **モデル名の嘘を消した**（メンバーの行は実際に走っている `GPT-5.6 Luna`。選べない一覧はプルダウンにしない） |
| 触ってみて出た6件（2026-08-24） | 社長が実際に押して見つけたもの。**入口のボタンが「押しても何も起きない」に見えた** → 送るのと返してもらうのを分けた（`chatSay` / `chatReply`）。押した瞬間に会話へ入り、そこで「考えています」が出る（実測 107ms）／ **会話のいちばん下が入力欄に隠れていた** → `COMPOSER_H` ぶん逃がし、**伸びたら貼り直す**（`ResizeObserver`。上を読んでいるあいだは動かさない）／ **宛先の「新しいチャット」が開かなかった** → Work が無くてもプルダウンのまま ／ **通知が0のときだけ別の画面になっていた** → 2列のまま言葉だけ変える ／ **メンバーに「まだいない人」を暗く並べ、右に `採用する`** → 採用ページ（`/hire`）は畳んだ（同じ一覧を2画面で持たない）。ついでに**別名で在籍と候補が二重に出ていた**（`revenue-strategist`）を `slugOf` で寄せた ／ **入力欄の帯が指を吸っていた**（帯は 124px、逃がすのは 108px → 下端 16px が押せない）→ 帯は指を通し、計画の行動の行は**板の実寸ぶん**逃げる（`onHeight`）／ **レールの「すべて見る」が死んでいた** → ⌘K を開く ／ 検査も直した — 消えた画面の URL を捨て、**Work と会話の id は画面から拾う**（決め打ちの `w-japanese` が 404 を測って「通った」ことにしていた）。E2E に「会話は下に貼り付いていて、入力欄に隠れない」を追加 |
| 入口の接続（2026-08-24） | **Case B / D が統括AIに繋がった**（`lib/exec/entry.ts`・道具6つ・deep の1往復）。**Case B** — `/discovery` が条件を構造で集め（`set_conditions`＋質問の板）、2つそろうと候補3つ（`propose_candidates`。fit は3スコア・推し1つ・**選ばなかった理由つき**）→ `/discovery/result` で比較 → 採用すると Case A と同じ道（計画→承認）へ。候補は消さない（不変条件9）／ **Case D** — `/import` が URL・文字ファイル（.md .txt .csv はその場で読む）・手書きの数字を `imported_sources` に保存 → 診断（`describe_business` `report_facts` `report_diagnosis`）→ `/diagnosis` が数字の帯と見つかったこと（**必ず提案 Work つき**）を出し、1件ずつ Work に。**読めていない URL は「名前だけが材料」と正直に出す** ／ 0019（diagnoses.facts / is_real）・0020（candidates.why）／ E2E 7項目を追加（板→候補→計画 / 取り込み→診断→計画） |
| モデルと深さを社員ごとに選ぶ（2026-08-25） | メンバー画面のモデルと深さは**押しても何も残らない飾り**だった（`ModelInline` / `EffortInline` が自分の中だけで値を持っていた）。**一覧を1枚に**（`lib/ai/catalog.ts` — Claude: Opus 5 / Sonnet 5 / Haiku 4.5、OpenAI: GPT-5.6 Sol / Terra / Luna。モデルごとに**受ける深さの段**と単価を持つ。画面も実行も原価もここだけを見る）／ **保存先 `agent_prefs`**（0024。`employee_id` が null なら統括AI。**使われていなかった `employee_settings` は落とした** — 0行・参照0か所・`model_fixed` が階層名に縛られた別設計）／ **選んだものが本当に届く**（`RunInput.model` を3つの通り道に通した。社員の実行は担当の設定、統括AIの計画・判断・会話は統括AIの設定。`runs.model` にも残る）／ **原価もそのモデルの単価で**（`billedCostUsd(tier, in, out, model)`。知らないモデルは 0 のまま）／ **深さの言葉と段を1対1に**（前は段5・言葉6でつまみがどの段か言えなかった。Claude に `none` は無く、**Haiku 4.5 は深さを受けない** → つまみごと出さない）／ **統括AIの深さが効くのは計画と判断のとき**（会話の返事はいつも速く返す）／ **入力欄の「自動」を外した**（押しても何も変わらなかった）／ オフィスのカードの `自動 · 深さ 2` が本物の名前になった |
| 押しても効かないものを片づける（2026-08-25） | `dead.mjs` で13画面の押せるもの309個を1つずつ押した結果、残っていた「死」は**空のときの送信ボタンだけ**（生 296 / 死 13 ＝ 全部これ）。そのうえで見つけた6件を直した: **一時停止が本物に**（`agent_prefs.paused`＝0025。止めているあいだ新しいタスクは起こされない。走っている最中のものは最後までやる。**`employees.status` に持たせない** — 実行が running/idle と書き換えるので止めた印が消える）／ **「直したい」は入力欄へ連れていく**（`Composer` の `focusAt`。操作説明のコピーをやめた）／ **`Toggle` が自分で値を持っていた**（保存に失敗して親が戻しても、つまみだけ古いまま残る）／ **請求の画面にだけ入力欄が無かった**（余白だけ 108px 取ってあった）／ **右ペインの会話が下に貼り付いていなかった**（画面のほうだけ直っていた → `lib/use-stick.ts` に1つの決まりとしてまとめた）／ **同じ学びを二度書かない**（30行が同じ1行で埋まる。実際デモで3行とも同じ文になった） |
| 入口の1枚を作り直す（2026-08-25） | 社長の指示「Google でも入れるようにしたい ／ ログイン画面も Mobbin を参考にデザイン性を上げて ／ いまのデザインシステムに則って」。**Mobbin で実物を5つ見た**（Modal / Cursor / TIDAL / Hex / Resend）— どれも「顔 → 名前 → 外の入り口を上に → `または` → メール」で、早い道が上だった。同じ形にしたうえで、この会社の言語に合わせた: **青は1ペインに1つ**（青いのは「リンクを送る」だけ。Google は枠だけ — これは **Google 自身の暗い画面向けの作法**と同じ形なので妥協ではない）／ 4色の G は色も形も変えない ／ **前回の入り方を覚える**（Modal の "You last logged in with Google"。ブラウザの中だけ）／ **黙って入口に戻さない**（`/auth/callback` が理由を短い合図に畳み、入口が日本語1行にする。上流の英語はそのまま出さない）／ **送ったあとの戻り道**（打ち間違えると詰んでいた）／ 入力欄は枠のほうが応える（`outline` を消しているので）。検査に `/login` と `/login?e=` を足した |
| 決まるまでの道を1本にする（2026-08-25） | 社長の指示「AIがユーザーに質問して、したい事業を確定させて成功までのワークフロー／ロードマップを描くまでの流れを完璧に整えたい」。**聞く → 候補 → 選ぶ → 計画** の1本道から、行き止まりと嘘を抜いた: **①どれも違うときの道が無かった** — 候補3つが全部外れた社長は、下の入力欄に自分で書き直すしかなかった → カードの最後に「どれも違う — 条件から見直す」。押すと会話に戻り、**何が違うかを1問だけ**聞いてから**違う軸で**出し直す（相手 / 売り方 / 分野そのもの。すぐ3つ並べ替えても、社長には同じものに見える）／ **②答えが新しいゴールになっていた** — 「どこが違いますか」に答えた往復から `propose_work` が返り、**質問文がそのまま Work の題**になった（実際に見つけた）→ 探索の途中（まだ Work が無い候補の道）は、答えの往復からの提案を取らない（`noGoalFromAnswer`）＋ 会話の方針にも「答えは答えであって、新しいゴールではない」を足した ／ **③計画の吹き出しが社長の言葉ではなかった** — 候補のねらいと集めた条件を全部つないで1つのゴールにしていたので、「◯◯を立ち上げたい 終わり: … 背景: … 分野: … 使える時間: …」が**社長が書いた言葉として**吹き出しに出ていた → `startWork(goal, ctx)` に割り、**吹き出しは社長が決めたことだけ**・条件と背景は統括AIにだけ渡す（そのぶん「使える時間を超える週を作らない / やりたくないことを含む道は引かない」を明示して渡す）／ **④選んだ道が残っていなかった** — どの案を採ったかは `candidates.adopted_work_id` にしか無く、**選ばなかった2つは画面のどこにも出なかった** → `addDecided`（双子ストア）で **いちばん最初の「決めたこと」**として台帳に残す（`question='どの道で進めるか'`・選ばなかった道は理由つきで options に）。この決定はそのあとの実行の依頼文にも載る（Phase 9 の受け渡し）／ **⑤ロードマップの引き方**を憲法に足した（終わりの形から逆に引く / 最初のフェーズは承認したその日から動けるものに / 社長が使える時間の中で回る形に）。E2E に5項目（どれも違う→1問 / 答えをゴールにしない / 違う軸で出し直す / 吹き出しは社長の言葉だけ / 選んだ道が決定事項に残る） |
| 入口とセキュリティを整える（2026-08-25） | 社長の指示「新規登録なのかログインなのかちゃんと区分したい／パスワードを忘れたときも／パスワードは大文字小文字記号が必要に」。**入口は1つの姿しか無く、はじめての人と2回目の人が同じ画面**にいた。**3つの姿に分けた** — `login` / `signup` / `forgot`（`?mode=` で URL に持つ。切り替えは `history.replaceState` でサーバーに行かない）。**メールのリンク（マジックリンク）は畳んだ** — 「忘れたときに届くメール」が再設定で足りるので、メールで入る道が2本あった。**忘れたときの姿だけ Google を出さない**（Google の人にパスワードは無い）。**パスワードの決まりは `lib/auth/password.ts` の1か所**（10文字・大文字・小文字・数字・記号。満たしたところから緑、満たすまで送るボタンは灰色）で、**記号の並びは Supabase が数えるものと同じ** — 片方だけ厳しいと、画面で断られたものが API では通る。**ただし画面の決まりはUXであって、セキュリティそのものではない**（匿名鍵はブラウザに配られているので、画面を通さず認証API を叩けば弱いパスワードでも作れる）。**本当に止めるのはダッシュボードの Minimum password length ＋ Required characters** で、手順は `docs/RUNNING.md`。**画面だけ直して「守った」と言わない**。セキュリティで直したもの: **メールの総当たりを助けない**（「そのメールは登録されていません」と言わない。ログイン失敗はいつも同じ1行）／ **オープンリダイレクト**（`/auth/callback` の `?next=` をそのまま渡すと `//evil.example` で他所へ送れた → `/` で始まる素の path だけ通す）／ **入っている人に入口を見せない**（`/login` → `/home`。ただし `/auth/*` は素通しのまま — 再設定はセッションを持ったまま開く画面なので、弾くと自分のパスワードを変えられなくなる）／ **再設定は2回打たせる**（見えない文字を1回で決めさせると、打ち間違えたまま締め出される）／ **リンクを踏んでいない `/auth/reset` は正直に断る**。**確認メールが要るかどうかは Supabase が決める**ので、画面はどちらでも正しく振る舞う（`/api/health` の `signup.confirm` で見える）。検査は `tools/check/login.mjs`（12項目・3つの姿を1つずつ押す）＋ layout に3画面 |
| 実キーで最初の会社が動いた（2026-08-25） | 社長が Google で入り、探索 → 候補3つ → 採用 → 計画 → 承認まで通した。**そこで最初の実行が2本とも失敗した**（「成果物が書かれないまま終わりました」）。DB を見て原因まで辿った: **統括AIに名簿を一度も渡していなかった**。道具の説明には「agency-agents のカタログ ID」と書いてあるのに、その一覧はプロンプトに載っていない。だから `propose_hires` は `hires: []` を返し、担当には「商品設計担当」「デザイン制作担当」という**この会社に居ない名前**が書かれ、承認しても**誰も採用されず**、担当のいないタスクが2本走って失敗した。直したもの: ①**名簿を渡す**（`rosterBlock` を計画とフェーズ送りの依頼文に。道具の説明も「この一覧から選ぶ」に）②**`hires` だけを見ない**（`crewFor` — タスクの担当名からも採り、名簿に無い名前は落とし、1人も残らなければ調査担当）③**担当のいないタスクをポンプが拾わない**（誰の頭も載らないまま走らせると、成果物を書かずに終わって blocked になる＝お金だけ減る）④**担当名は控えに入る前に名簿へ寄せる**（承認のときだけ寄せると、計画の画面が「商品設計担当」と言い実際は調査担当が動く＝画面がその場で嘘をつく）⑤ついでに**前からの穴**: 差し戻しの直しタスクが担当なしで生まれていた（Work 画面が `taskId` を `DelActions` に渡していなかった）。⑥**決め打ちのプロバイダを本番と同じ壊れ方にした** — 小さい Work の計画は `hires: []` ＋ 居ない担当名を返す。行儀よく書くと、この穴は検査に永久に出てこない。合わせて、**Supabase の書き込み経路を実キーのDBに1本通した**（`authenticated` の役で全表に insert → 引き金（進捗の写し・原価の記帳）まで確かめて、最後に例外でロールバック）。**会話ログは次の返事が出るところへ移した**（社長の指示。前は入力欄のすぐ上）。E2E に「社員を提案しない計画でも担当が付いて動く」「居ない担当名を計画に残さない」を追加 |
| ワークフロー図を成果物にする（2026-08-25・archify） | 社長の指示「work のワークフローチャート作成する時 archify の skill を適応するようにしたい」。**そのままは持ち込めない** — archify は `archify.mjs validate → deliver → visual-check` という Node の CLI が前提で、AI社員は1タスク＝1往復・道具4つ、シェルもファイルシステムも無い。**持ち込んだのは「形」と「作法」**: 器（`schema_version` / `diagram_type` / `meta` / `lanes` / `phases` / `mainPath` / `nodes` / `edges`）は**名前ごと同じ**にして、**検証は OneFound の中で回す**（`lib/diagram/check.ts` の9つ。CLI が要らない）。**落としたもの**は飾り（`brand` / `visual_preset` / `animation` / `cards` / `views`）・**凡例**（置かない決まり）・`groups`（レーンとフェーズで足りる）・`route` / `via` / `channelX`（格子が位置を決めるので要らない）。**変えたもの**は `node.type` — archify の `frontend / backend / database` は一人社長のワークフローでは意味を持たないので `work / decision / deliverable / wait / end` に。色が付くのは判断＝橙と終わり＝緑だけ。**絵が壊れる診断と読みにくいだけの診断を分けた**: 壊れるほうは出さずに指したところだけ直してもらい（道具を1つに絞って1回だけ）、それでも壊れていれば通らなかったと正直に言う。読みにくいだけなら出して、直らなかったことを成果物に残す（archify の「未解決の診断は正直に報告する」）。**描き方はホームの地図と同じ語彙**（格子・直角の線・左3pxの色帯・ドット背景）で、違いは1つだけ — **動かさない**（成果物は読むもので探すものではない）。**文字は潰さない** — 440px のペインで 0.4倍になって読めなくなったので、縮めるのは 0.8 までにして横に送る。一覧の書き出しは**主線**（JSON をそのまま出すと記号の山）。作法は標準スキル `workflow-diagram.md` に。**決め打ちのプロバイダは1回目をわざと壊す**（線の先に居ないノードを指す）— 本物と同じ壊れ方を通さないと、直す仕掛けが動いているか分からない。E2E に3項目（図が成果物になる / 絵として描かれる / 壊れた線が残っていない） |
| ホームのワークフローに archify を当てる（2026-08-25） | 社長の指示「このスキル使って欲しいのは ホームのワークフローの画面組み立てる時です」。当ててみて、**残したのは2つ・外したのは2つ**。いちばん効いたのは作法を当てたことではなく、**作法を物差しにして元の実装が自分のルールを破っているのが見つかったこと**だった。**残した①「枝は最寄りの主線ノードから」** — チップ（判断・要確認）の列を `Math.max(nowSeq, 1)` で決めていたので**畳んだあとの列とずれ**、しかも単独チップの線が**2点しかなく、列がずれた瞬間に斜めの線**になっていた（「直角に曲がる線」は archify より前から決まっている）。畳みを組み立て側（`lib/live/flow.ts`）に移して**列を決める場所を1つ**にし、チップは親のフェーズの真下、線は4点の直角に。`MapPhase.span` で畳んでも元のフェーズ番号を失わない。**残した②「通らないものは描かない」** — 成果物の図と同じ9つの検査に掛け、通らなければ正直に言う。**外した①「主線を1本にする」** — ①放っておけない順に並べ替えると**地図の行が動く**（判断に答えた瞬間に Work が下がって別のが上がる。地図でいちばんやってはいけない）②「いま何が放っておけないか」は**進捗の答えの1行・タスクの判断待ちの帯・通知**がもう言っている（同じことを2回書かない）③線を 1.3→1.9 にしただけでは**ほとんど見えない**（見えない印は印ではない）。そもそもこの画面が答える問いは「どれが急ぎか」ではなく「これはどの Work か」で、その答えは**押すと1本だけ残る**仕掛けがすでに持っている。**外した②「ノードは12まで」** — これは1枚の図の作法で、地図は12個に収まらないから**拡大縮小とミニマップ**を持っている。予算で畳むと社長が見たいものを隠す側に倒れる。**skill は「当てるもの」ではなく「照らすもの」**として効いた（成果物の図のほうは1枚の図なので、作法がそのまま効く） |
| 盤面の見た目を4案から選ぶ（2026-08-25） | 社長が「見た目全体を見直したい・いくつか作って見せて」。同じデータ・同じ画面で4枚撮って見比べた — **A いまの形**（鎖は横・枝は真下。静かだが鎖どうしが無関係な島に見える）／**B 帯**（Work ごとに薄い面。どこまでが1つの Work か分かるが「横に区切らない」に触る）／**C 縦に流す**（フェーズが上から下、Work が横。縦の空きが埋まるが、Work が増えると横に伸びる）／**D 幹から枝分かれ**（会社の幹を1本左に通す。地下鉄の路線図）。**選ばれたのは D。** 幹は格子の上（列0のまん中）に置き、鎖は列1から。**Work が1本しか無い会社では引かない**（どこから来てどこへ行く線か言えないので、鎖を左端から始めて余白も作らない）。`?fx=` の足場は消して幹だけ残した。姿を作って初めて出た3つも直した（どの案でも正しい）: **社員の球の座標を組み立て側から渡す**（前は描く側が置き直していて、格子が変わると別のノードの上に載った）／**`bounds` に線も入れる**（幹のように節の外を通る線が箱から出て切れていた）／**`labels`（枝ラベル）を消した**（`MapWork.from` の系譜はまだ誰も入れていないので、描く側が空を回していた。系譜が入るときに枝の文法ごと戻す） |
| **見ていなくても会社が進む**（2026-08-25） | 社長が「1時間ごと・上限つき」を選んだ。**ポンプを Work 画面から器（Shell）へ移した** — どの画面を開いていても会社ぜんぶが進む（動いているときは3秒・静かなときは15秒。裏タブは何もしない）。Work 画面は読み直すだけにした（**同じ会社に2つのポンプを立てない**）／ **閉じているあいだは Cron**（`/api/cron`・毎時0分・`vercel.json`）。進め方は画面と同じ `pumpCompany` で、**2つ目の実行系を作らない**。誰として読み書きするかは **runner ＝ 会社にもう1人いる利用者**（`lib/run/runner.ts`。service role も専用ロールも使わない — ポリシー28本も既定値22表も store も1行も変わらない）／ **1日の上限**（`DAILY_CAP_CENTS`・既定 200セント）。当たったら**その日は止まるだけで、あすまた動く**（残高が尽きたときの `paused` とは別の線）。通知は1日1通（0026）。きょう使ったぶんと上限は `/billing` に出る ／ **鍵が3つそろわないうちは `/api/cron` は 503**（動いているふりをしない。`/api/health` の `cron` で見える） |
| **通しで見直した（2026-08-25）** | 社長の指示「onefound 全体でもっと改善すべき部分 … 完成度を高めてください」。**① AI社員が前のフェーズの成果物を1文字も見ずに始めていた** — 渡す成果物を「同じフェーズ」に絞っていたので、戦略フェーズの担当には調査の中身がどこからも届かなかった（承認済みの索引はタイトルだけ）。この Work の済んだタスクの成果物を新しい順に3件渡す（差し戻されたものは渡さない）／ **② 引き直しに空の計画の守りが無かった** — 立てるときは止めていたのに、「直したい」の往復が空で返ると**ちゃんとしていた計画が 0フェーズに置き換わった**。直しは元より悪くならない、が要る ／ **③ Work が立ったことが台帳に1行も残っていなかった**（0008 は `after update` だけ）→ 0027 で `work.created` ＋ `work.paused` / `work.done`（見ていないあいだに動くようになったので、台帳だけで読めることの重みが変わった）／ **④ ホームが止まった写真だった** — 会社は動いているのに1回読んで固まる。8秒ごとに読み直し、**中身が本当に変わったときだけ**入れ替える ／ **⑤「セキュリティ警告0件」が実際は1件だった**（`Leaked password protection`＝Supabase の設定・Pro プラン）。**コードで直せないものを「直した」と言わない** |
| **つないだ道具（MCP・2026-08-25）** | 社長の指示「将来的にはMCP接続もできるようにしたいけど、MCP構築時間かかりますか？できそうならしてください」→ **半日**（→ `docs/design/13-mcp.md`）。大きいのは protocol ではなく**誰として読み書きするか**で、そこは Cron の runner で一度通った道だった。**向きは2つあって、作ったのは client のほう**（AI社員が社長の道具を読む・書く。server の側は鍵の配り方が別設計なので分けた）／ **相手ごとの実装を持たない**（MCP は道具の側が自分で名乗る）／ **ライブラリを入れない**（JSON-RPC を POST するだけ）／ **本番は https だけ**・localhost はデモと検査のみ（`DEMO_MODE`。`NODE_ENV` では分けられない — `next start` は検査でも production）／ **書ける道具は社長が許した相手のぶんだけ**（`readOnlyHint` は鵜呑みにしない）／ **道具の名前に日本語を入れない**（`mcp________list_items` になった）／ **1タスク＝1往復の初めての例外**（上限3往復。つないでいない会社は1往復のまま）／ **鍵は型に置かない**（列の権限で止めると `public` の SECURITY DEFINER が要り、警告が1件増える）／ 話す相手は repo の中（`tools/mcp-test/server.mjs`）。**まだ: OAuth / 社員ごとの割り当て / server の側** |
| **ロードマップを引く力を上げた（2026-08-26）** | 社長の指示「目的伝えた際にAIがロードマップを完璧に作れるようにして欲しい」。**理由が1行も書かれていなかった** — 右ペインは「なぜこの順番か / 前提 / 見送った案」を出す作りなのに `draft_plan` がそれを出させておらず、**どの Work でも同じ決まり文句**が出ていた（社長は根拠ゼロで承認していた）→ `why` / `assumes` / `dropped` / `time_note` を道具と憲法に足した ／ **引くときに会社のことを知らなかった**（承認済みの成果物も決めたことも渡していなかった。実行には渡していたのに）／ **引いたあとの検査が2つだけ**だった → `lib/exec/plan-check.ts`（図の validate → 描き直しと同じ作法。辻褄を見て、指したところだけ1回だけ引き直させる。**元より悪くしない**）／ **deep を Luna → Luna Pro に**（Luna は 5.6 一家で知能指数 最下位の 52.3。Luna Pro は**同じ単価**で `reasoning.mode = pro`。タダで上がる）／ **一覧の単価が3枚まちがっていた**（Sonnet 5 / Sol / Terra。最大3倍高く記帳 → 残高がずれる）／ 決め打ちは**1回目をわざと週数を合わせない** |
| **進捗を畳んで3ビューに（2026-08-26）** | 社長の「これマジで見にくい」→ Mobbin の8アプリ（Wrike / Airtable / Asana / Jira / ClickUp / Coda / Programa / Height）を見て**行の固定・明るさ・答えの1行**を直した。**そのあと社長の「てかこの画面いる？」で畳んだ** — 直したら見やすくなった＝要る、ではない。見やすくなって初めて「これ、オフィスと同じことを言っている」が見えた。**ここにしか無かったのは日付だけ**で、進み具合も判断待ちも予定との差も**オフィスの輪が言っている** ／ 会社ぜんぶのカレンダーは、誰も守っていない見積り（計画の週数）に1画面ぶんの重みを載せていた（「遅れ N日」の1語ぶんなら載る）／ **日付は Work 画面のフェーズの行へ**（あの列は最初からあって、ずっと空だった）／ **答えの1行はビューの外へ**（判断待ち / 要確認 / 遅れ。タブを選ばないと見えないのがおかしかった。出すのは放っておけないときだけ）／ 消したもの: `Progress.tsx`・`ticks`・`todayX`・`done`・`over`・`restDays`・`endDate`・`Phase.x/w/from/to` ほか（**ガントの座標を組む計算がまるごと消えた**） |
| **成果物ができたら、社長が見るまで進まない（2026-08-25）** | 社長の言い直し「完全自動で動くというよりかは、**成果物ができたら確認してもらって、それで進めていいのか確認してもらう**」。**社長を待つのは2つ** — 計画の ◆ と、**まだ見ていない成果物**（`closePhaseIfDone` が毎回両方を測る）。**承認そのものが「進んでいい」の合図**で、押されるまで次のフェーズは始まらない ／ **閉じた直後だけでなく毎回測り直す** — 最後の1件を承認した瞬間に `ready` が立ち、ポンプが次を引く（押すのは承認の1回だけ）／ **ポンプは起こすタスクが無くても関所を見る**（idle の道）／ **引けなかったら引き直さない** — 「引けませんでした」の1通がそのまま試した印（`noticed`）／ 通知は3つ（要確認「成果物 N件 を見て」／判断待ち「決めて」／要確認「次に進みます」。**成果物のほうが先**） |
| **出す形は社長の手もとで使える形に（2026-08-25）** | 社長の指示「文章だけでなく、LPやPDFや様々な出力できるようにしたい、CSVとかもそうだし」（→ `docs/design/12-outputs.md`）。**形は1か所**（`lib/deliver/format.ts`）で `report / doc / copy / table / csv / page / code / diagram`。見るのは道具・画面・持ち出し・書き出しの4か所 ／ **記号のまま出さない**（`lib/deliver/md.ts` で読んで見出し・箇条書き・表に組む。ウェイトは 400 のまま）／ **ページ（LP）はアプリの中で走らせない**（`sandbox=""` ＋ `srcdoc`）／ **PDF は刷って作る**（見えない iframe。新しい窓は開かない。**紙は白い**）／ 決め打ちのプロバイダも csv と page を返す（本番の壊れ方を検査に通す） |
| **聞く回数を先に決めない（2026-08-25）** | 社長の指示「あなたに聞くのは2回とか決めなくていいよ、AI が判断して適切な提案ができるまでしていい」。**数の上限を5か所から外した**（憲法の「最大2〜4問」、計画の「関門は多くても2つ」、`ask` / `draft_plan` の説明、チャットの方針、引き直しの頼み文）。残したのは**1度に出す数**だけ（1〜4問。板は1問ずつ出るので多すぎると読めない）／ **◆ は「社長でないと決められないところ」にだけ。0 でもいい** — そのかわり**◆ を置かなかったフェーズは会社が自分で進む**。置かなかったところで止まるのは統括AIの判断を無かったことにするのと同じで、見ていないあいだは次の1時間がまるごと待ちになる ／ 通知も分ける（◆ あり＝判断待ち「見て、次に進めてください」／無し＝要確認「次に進みます」）。計画の画面も「あなたが決めるのは ◆ の N か所。ほかは会社が進めます」 |
| **チャット → Work → 完了を通しで歩く（2026-08-25）** | 社長の指示「完璧に作り上げてください。チャットしてから work 作ってそれを完了させるための流れまでも」。**最後まで走らせて終わった画面を見る**まで見つからないものばかりだった（`tools/check/_walk.mjs`）— **① 後のフェーズの仕事を全部 調査担当がやっていた**（計画で提案されるのは最初のフェーズの担当だけ。居ない担当は先頭の社員に落ちる）→ 要る人はその場で採用 ／ **② `draft_phase_tasks` の担当が任意**だった → 必須に ／ **③ 終わった Work が「進行中」「フェーズ 1 / 4」** → 完了 / 4 / 4 ／ **④「成果物がすべて揃っています」は確かめていなかった**（9件とも要確認）→ 事実に ／ **⑤ 終わりの報告が1行だけ**だった → 通知に中身＋会話に統括AIの報告（`lib/exec/finish.ts` の1か所・モデルは呼ばない）／ **⑥ ゴール行に「終わり:」「見込み:」が混ざっていた** → ctx へ ／ **⑦「作るもの」のフェーズを画面が推測していた** → 統括AIに書かせる ／ **⑧ オフィスのログが常に「稼働中」** → 実行中がいるときだけ ／ **⑨ もう作ってある会話が「この Work を作る」に戻っていた**（作ったことがカードの state にしか無かった）→ スレッドの Work を親から渡す |
| **会話の考え方を一本に（2026-08-25）** | 社長の指示「chat の考え方とかも固めて最適化して欲しい」。**元になっている1つの考えを最初に書いた** — 社長が書くのは「やりたいこと」か「困っていること」で、どちらもそのままでは仕事にならない。会話の仕事はそれを**押せる形**に変えること。だから**統括AIの番は、次に押せるものか答えそのもので終わる**（文章だけでいいのは尋ねられたときだけ）／ **いまどこにいるかを毎回そのまま言う**（`stageLine`。前は断片から毎回推測させていた。探索中はまだ聞けていない条件の名前まで出す）／ **答えの往復は1回で写して次の一手まで**（前は1つの発言にモデルを3回呼んでいた）／ **社長の最初の一言を history の先頭に固定**（探索が長引くと始まりが消えていた）／ **統括AIが「社長を待っているもの」を知る**（判断待ち・要確認の件数を snapshot に。聞かれなくても一言添える）／ 重複した決まりを1つに |
| **使われるための3つ（2026-08-25）** | 社長の指示「もっと需要のあるもの、他のものに負けないものに」。足りない機能を数えるのではなく、**「これが無いと使えない」を3つ** — **① 社長が Work を止められる**（見ていないあいだも動く会社にしたのに、止める手がどこにも無かった。`pauseWork` は残高が尽きたとき仕組みが呼ぶだけ）／ **② 作ったものを持ち出せる**（コピーと ⬇。**持ち出せない成果物は、無いのと同じ**。SKILL.md には最初から ⬇ が付いていたのに、いちばん大事なものに無かった。図は .json・文章は .md）／ **③ はじめての画面に押せる例を3つ**（入力欄1つだけでは、やりたいことが言葉になっている人しか先へ進めない。説明ではなく提案 — 押すとその言葉で会話が始まる） |
| **待たないで、先に作って動き出す（2026-08-26）** | 社長の指示「**受注が来てから制作っていうようにしたくない**。例えばlp制作事業だとすると、まずはマーケティングのための自分のサイト、ポートフォリオを制作してから、公開してそこから営業やマーケティングをする」「**まず必要な社員全員採用して**、調査社員が調査完了したらその結果をユーザーに投げて**わからない部分は統括AIがユーザーに質問投げて他のAIが全員動き出す**」。**4つとも「待つのをやめる」話**だった — **① 他人が動かないと終わらないフェーズを作らない**（「受注が3件入っている」はAI社員には終わらせられず、フェーズは全タスクが done で初めて閉じるので**会社が本当にそこで止まる**。憲法 ＋ `checkPlan` の `waiting` 検査。売る仕事は **見せられるものを作る → 公開する → そこから来る** の順に引く）／ **② 要る人は最初に全員そろえる**（`PlanPhase.owner` を必須に。前は最初のフェーズの担当しか提案されず、あとの人は `advancePhase` で採られていた＝画面には「担当は未定」。名簿に無い名前は**そのフェーズを回す人**に寄せる）／ **③ 起こせる人は全員起こす**（`nextQueued` が配列を返す・ポンプは `allSettled`。前は「走っているタスクが1つでもあれば null」で、**4人採用しても動くのは常に1人**だった。同じ人に2本は持たせない。取り合いは `startRun` の atomic な置き換えが捌く）／ **④ 分からないところは統括AIが聞き、そのタスクだけが待つ**（`draft_phase_tasks` にタスクごとの `ask`。統括AIは前のフェーズの成果物を読んでから引くので、そこで「社長にしか決められない」と分かったものに ◆ を立てられる。**ほかのタスクは queued のまま動き出す**。計画の ◆ は**フェーズの終わりの関門**、こちらは**タスクの入口の関門**）／ **⑤ 承認してから動き出すまで実測14秒**（行を0.5秒ごとに書き出して初めて見えた。「押しても効かない」ではなく「押した15秒後に効く」は通しの検査では気づけない）— **ポンプの静かなときの15秒を、押した直後にも待っていた**（→ `lib/pump.ts` の `wakePump()`。画面が移ったときと、移らない操作で起こす。2つ目のポンプは立てない）／ **サーバーアクションは同じ画面のぶんが順番待ちになる**ので、ポンプが返るまで `getWork` も `homeData` も後ろで待たされ、**動いているのに画面が固まっていた**（→ `app/api/pump/route.ts`。中身は同じ `pumpCompany` で、変えたのは呼び方だけ）。直した後の実測は **0.5秒で15% → 3秒で75% → 5秒で要確認** ／ ついでに **`.sx` は「横に送る」という設計側の宣言**として検査から外した（数えていると、社員がひとり増えるたびに検査が赤くなる） |
| **会社が育つ（2026-08-26）** | 社長の指示「**hermes agent を全社員に導入したい**」。元は **NousResearch/hermes-agent（MIT）** の閉じた学習の輪（→ `docs/design/14-learning.md`）。**そのままは持ち込めない**（Python ＋ Node ＋ ripgrep ＋ ffmpeg ＋ Git ＋ `~/.hermes/` が前提で Workers に載らない）ので、archify と同じく**輪だけ**を写した — **⓪ AI社員の憲法**（統括AIには94行あるのに社員には1枚も無く、「分かったふりで書かない」を**社員は一度も読んでいなかった**。7人ぜんぶの定義の前に載せ、重複する定義側のルールは消した。Web検索が既定オフなので「**記憶から書いたことには『未確認』と印を付ける**」を1行入れた）／ **① 社員が手順書を書く・使いながら直す**（`write_skill` / `improve_skill`。渡した手順書だけ直せる・1タスク1枚まで・ついでに「社員に付けたスキルが実行から一度も読まれていない」穴も塞いだ）／ **② 通すのは統括AI**（社長が3案から選択。Hermes は社員が自分で書き換えるが、ここには「勝手に増えると社長が知らないうちに社員が変わる」という先の判断がある。落としたものは理由つきで残り、**トグル1つで戻せる**）／ **③ 記憶の手入れ**（上限であふれると「古いもの」が落ちる＝「要らないもの」ではない → 満杯前に**畳んで濃くする**。元より悪くしない）／ **④ 思い出す**（承認済み成果物の**タイトルしか**渡していなかったので、別 Work の中身は届いていなかった。`to_tsvector` は日本語を切れず、pgroonga は `public` の関数が要って警告が増えるので、**`ilike` ＋ pg_trgm の GIN**（0030）。**往復は増やさず、こちらが先に引いて依頼文に載せる**）／ **⑤ 社長のこと**（決めた・差し戻した、という**起きた事実**だけを1行に。モデルは呼ばない。置き場は学びと同じ `agent_skills` の1行 — **表を増やさない**。見える・消せる） |
| **候補を「需要があって1人でできる仕事」にする（2026-08-26）** | 社長の指示「**実際に需要があって個人1人でもできるような仕事、ゴールの提案をして欲しい**」。読み直したら**その2つがどちらも入っていなかった** — `fit` は 速さ / 安さ / 得意との相性で、**誰かが欲しがっているかは一度も問われず**、**1人で回せるか**も無かった（だから「小さな実用品の販売所」のような抽象的な案に寄っていた）。**軸を 需要 / 1人で回せる / 最初の1件まで に入れ替え**（cost は「1人で回せる」に吸収）、並びも需要の高い順へ ／ **抽象に逃げられなくした** — `who`（誰が買うのか）と `first_one`（最初の1人をどこで見つけるか）を必須に。「個人」「中小企業」は書いたことにならない、と道具に書いた。`hours_per_week` を社長の使える時間と突き合わせる ／ **確かめていないことを候補の顔に出す**（`unsure`。統括AIは Web を見ていないので、需要は記憶から言っているだけ） ／ **需要は、作ったものを公開したときに確かめる**（憲法 ＋ `checkPlan`）— **一度この順番を間違えた**。最初は「最初のフェーズで外に出して確かめる（相手に直接聞く / 先行の申し込みを取る）」と書いたが、社長の指示は逆で「**マーケティングを最後の方に。最初に制作、準備をきちんと**」（前の LP の例と合わせて2回言われている）→ **順番は「作る → 整える → 公開する → そこから広げる」** に直した。`market-first`（集客・営業・宣伝を最初のフェーズにしない）＋ `publish`（それでも公開のフェーズは必ず入れる。`unproven` のときだけ）。どちらも fatal にしない。**リスクは言った上で、社長の順番で通した** ／ ついでに **`PlanLimits` を誰も埋めていなかった**（`checkPlan(plan)` を引数なしで呼んでいたので、「使える時間の中で回る形にする」の検査は一度も動いていなかった）。0031 |
| **改善に改善を繰り返す（2026-08-26 · 5〜6周め）** | **⑤ オフィスの輪が、絵で言うはずのことを言っていなかった** — 社員の球がいつも1つしか無く（描く側は何人でも置ける形なのに、組み立て側が1人しか渡していなかった）、引き継いでいないところに矢羽根が立っていた（判定が `k > 0` ＝ フェーズが変わっただけ）。区間ごとに球を置き、同じ人の区間は組み立て側で1本に畳む。`Work.crew` は誰も読んでいなかったので削除。**⑥ ログアウトが死んでいた** — 左下の「わたし」のメニューに行はあるのに `onClick` が無く、**会社から出る道が画面のどこにも無かった**。「設定」も死んでいたので外した（「わたし」の設定という画面は無い）。どのアカウントで入っているかも出していなかった（`Y あなた` の決め打ちだけ）→ メールを1行め。**「7日間」のトライアルは実装されていなかった**ので、言わないことにした。**検査そのものの穴**: `dead.mjs` は押せるものを画面を開いた時点で集めるので、**開いてから出る板の中は一度も押していなかった** → 板を出すものを覚えて中も押す（104個ぶん増えて、新しく死んでいたものは0）。E2E 108件 / layout 18画面 / reach 14画面 / 生 322 / 死 12 |
| **改善に改善を繰り返す（2026-08-26 · 2〜4周め）** | 社長の「改善に改善を繰り返して」。**② 言っていることが見出しと合っていない**を直した（「なぜこの順番か」の1行めが仕組みの説明だった／メンバーの「できること」が Critical Rules だった → ロスターに `can` を足し統括AIにも渡す／デスクとオフィスの「経過」がずっと空だった）。**③ 行き先が、数えたものを指していなかった**（ホームの「成果物 N件 を見る」が全件の画面へ／通知の「開く」がいちばん多い用件で違う画面を開いていた／⌘K のタスクが Work の頭に落ちる／⌘K は `?open=` を付けているのに決定事項の画面が読んでいなかった）。**④ 「新しく書く」が行き止まりだった** — スキルのペインは読むだけで、ひな形を作っても書き込む場所が無かった（✏編集だけが設計から抜けていた）→ `editSkill`（直せるのは消せるものと同じ範囲・保存ボタンは置かない・閉じ際にも書く）。**「引き継ぎもログに出る」と書いてあるのに、そんな行を作っているところが無かった** → フェーズが変わって回す人が変わったときだけを拾う（でっち上げない）。E2E 106件 / layout 18画面 / reach 14画面 / 生 218 / 死 12 |
| **一覧の画面が、持っている情報を出していなかった（2026-08-26）** | 社長の「機能が完璧じゃない、そしてその機能にあった画面も完璧じゃない」。画面を1枚ずつ実物で読んで、器はあるのに中身が入っていないところを直した。**① 成果物のサムネイルがどれも題を2回書いていた**（`previewFor` が markdown の見出しを取る＝カードのタイトルと同じ。さらに Work 名がサムネイルとカード下で2回）→ 見出しを飛ばして本文か箇条書きから取る。**② 「要確認 N」と出しているのにそこへ行けなかった** → その数字自体を押せる絞り込みに（`?only=review`・列は auto-fill）。**③ 決定事項が、決めた瞬間に理由と選ばなかった道を失っていた**（台帳の存在意義そのもの）→ 理由を残し、選ばなかった道も暗く並べ、どの Work の判断かも出す。**④ Work 画面のフェーズ4行がどれも同じ `/tasks` へ飛んでいた** → ワークフローの地図と同じ作法（`?ph=`／選んだ1つだけ残る／下も絞られる）。**⑤ 型にあるだけで誰も埋めていない節**を埋めた／消した（`rest`/`endDate`/`late` ＝ 帯の「残り —」が永久／`decs` ＝ 右ペインの「決めたこと」が常に空／`crew[].dim` ＝ 手が空いた社員が「0タスク」と7人並ぶ／`mine`・`leadWhen` は消した）。**⑥ `when` の持ち方を1つに**（`lib/when.ts`。前はメモリ版だけが「たった今」という文字を入れており、**本物の会社では日付がどこにも出ていなかった**）。検査も一段強く（押せるものがまだそこに居るかまで見る → 生 218 / 死 12）。E2E 101件 / layout 18画面 / reach 14画面 |
| **止まらない会社にする（2026-08-26）** | 社長の「**お金をサブスクで継続したくなって払ってもいい価値のあるもの**まで作り上げて」。機能を足すのではなく「これがあると解約する」を探したら、全部**会社が黙って止まる**話だった。**① 止まったタスクから戻る道が1本も無かった** — `blocked` が1つ残るとフェーズは永久に閉じず、Work は二度と進まない。通知は「途中で止まりました」と言い、押した先には行動が1つも無かった（`runs.error` はずっと書かれていたのに画面のどこにも出ていない）→ `retryTask` / `skipTask` / `taskWhy` ＋ `StuckActions`（もう一度やる / これは飛ばす）。**② 止まっていることがどこにも出ていなかった** → `HomeData.stuck`（ピルの左、いちばん先。遅れとは別のもの）＋ 朝の報告。**③ 通知の画面が自分の約束を守っていなかった**（「この画面から出ずに終わる」と書いてあるのに、判断も承認もできなかった）→ `inboxAct` ＋ Work 画面と同じ部品を並べる（判断のペインは `DecisionPick` に出して1か所に）。**④ 閉じているあいだ進むかどうかが画面に無かった** → `/billing` に点＋1行（`awake`）。決め打ちのプロバイダは**1回だけ道具を使わずに終える**ので、E2E はこの戻り道を通らないと先へ進めない。E2E 93件 / layout 18画面 / reach 14画面 |
| **不足を5つ潰す（2026-08-26）** | 社長の「あとは何が不足してる？」→「全てお願いします」。**① 順番と時間の検査が半分の道にしか効いていなかった**（`chatMakeWork` が `PlanLimits` なしで `startWork` を呼んでいて、**チャットに書いて作る、いちばん使う道で発火していなかった**）／ **② 品質担当が一度も働いていなかった**（名簿にいるのに**呼び出しがコードに1か所も無い**社員だった → `lib/exec/qa.ts`。在籍しているときだけ・**1回だけ**・差し戻すのは社長と同じ道・細かい好みでは差し戻さない）／ **③ Web検索の栓を社長が押せる形に**（前は `OPENROUTER_WEB=1` が全部の往復に付いていた。いまは候補を出すときと調査担当・分析担当の実行だけ。既定はオフ・メンバー画面で入り切り。0032）／ **④ 学びを「ルールにする」操作**（画面には前から「社長が決める」と書いてあったのに操作が無かった。学びは30行で薄まる／ルールは残って毎回効く。**移す**。0033）／ **⑤ 社員が矛盾を上げる道**（`flag_conflict`。憲法に「黙って上書きしない」と書いてあるのに書き残す先が無かった）／ **⑥ 時間**（何と比べた「遅れ」かがどこにも書いていなかった。基準は**承認したときの見込み**で、フェーズが閉じたその場で `paceSay` が言う。**モデルは呼ばない**）。E2E 89件 |

---

## 機能候補の判断（2026-08-24・見送りの台帳）

出された候補12件のうち、**導入した5件**は上の横断の表にある（会社の記憶 / Agent Memory=学び＋差し戻し / 成果物の版 / runs.model / ⌘K）。
**見送った7件**は「悪い案だから」ではなく「順番が違うから」。理由ごと残す:

| 候補 | 判断 | 理由 |
|---|---|---|
| Task Dependencies（並列実行） | 見送り | `task_deps` の器は 0001 からある。いまの逐次ポンプは「依存＝直列」の正しい近似で、並列化は**開いている画面の課金が N 倍**になる話とセット。統括AIが依存を書くようになってから |
| Agent Activity / Run History | 既にある | runs / run_steps / decision_refs / audit_events が Action Log そのもの。画面はデスクとタスクの歩みが読む |
| Model Router 高度化（難易度で自動選択） | 見送り | 実モデルの品質・原価の実測が無いのにルーティングを作るのは早い。器はそろった — **2026-08-25 に「社員ごとにモデルと深さを選ぶ」が本物になった**（`lib/ai/catalog.ts` ＋ `agent_prefs`）ので、自動で選ぶ側を足すときは、この一覧と設定の上に乗る |
| AI Governance（ZDR モード） | 半分導入 | Run ごとの Model / Cost / Tier の記録は入れた（0018）。ZDR・provider 固定は OpenRouter の `provider` 指定で可能だが、**鍵が無いと検証できない** — Phase 11 の宿題のまま |
| Permissions / Approval 階層 | 原則だけ記録 | いまの道具は全部「書き残す」系で、外に出る道具（メール送信・公開・支払い）が無い。**外に出る道具を足すときは Approval 必須** — この原則を先に固定する（下の「守るルール」に追記） |
| Routines（定期業務） | 見送り | 「見ていないところで料金だけ増えない」（ポンプの決めごと）と真っ向からぶつかる。常時実行は Cloudflare（Cron / DO）に出るときに、専用ロールの設計と一緒に。朝の報告が最初の Routine の形 |
| Cloud Workspace / Sandbox | 見送り（MVP後） | 候補の文のとおり大型機能。Web検索（OpenRouter の web プラグイン）だけは既にあり、鍵と課金設定で有効化できる |
| **複数社（memberships）** | **見送り（2026-08-24・社長の判断）** | いまは**会社は1つだけ**。器（`accounts` と全ポリシーの `account_id = private.current_account_id()`）はそのままなので、入れるときポリシー28本は無変更で足せる。**押せる顔をやめた** — パンくずの根から ⌄ と「会社を追加」を外して素の文字にした（切り替える先も増やす道も無いのに押せるのは、「押しても何も起きないものを置かない」に反する）。入れるときは `memberships` ＋ `current_account_id()` の書き換えを認証と一緒に。そのとき **トライアル枠は人に1回・2社目からは有料**（会社を作り直すだけで枠が湧かないように） |

---

## 元の要件から変えたこと

| | 原文 | いま | 理由 |
|---|---|---|---|
| モデル調達 | OpenRouter | **OpenRouter**（2026-08 に原文へ戻した） | いったん「2社に直接」へ変えたが、鍵1本で全モデル・無料のテストモデル（Ox Alpha）・自動迂回を取って戻した。直つなぎも残す → `docs/design/05` の判断ログ |
| ホスティング | Next.js ＋ Supabase | **Cloudflare** ＋ Supabase（Postgres/Auth） | 1実行 = 1 Durable Object。二重起動が起きず、進捗を WebSocket で直接押せる |
| 思考量のUI | Fast / Balanced / Deep をユーザーに見せる | **見せない**（`deep`/`standard`/`fast` は内部だけ） | 「毎回考えなくていい」という製品思想と、選ばせるUIが矛盾していた |
| ナビゲーション | Office / Work / Tasks / Deliverables | ホーム / 通知 / Work / タスク / 成果物 / メンバー / 決定事項 | Office をホームに改名し可視化の場に。決定事項を独立させた。**採用はレールに置かない**（行き先ではなく操作なので） |
| 単発の依頼 | （定義なし） | **いまある Work の中のタスク**（用事は廃止 → 06 の判断ログ） | 入れ物を3つにすると、全画面とデータに例外が付く |
| Work の粒度 | （定義なし） | 判定条件3つ＋昇格・降格 | 「LPが目標の Work も過程の Work もある」問題への答え |
| 課金の単位 | Credits（将来） | **トークン**（1 = $0.00001） | 画面に出す数字を実感のある桁にした |

## まだ設計に落ちていない要件

原文にあって、いまの23画面・7文書でまだ埋まっていないもの。**10件すべて MVP スコープに入れます。後回しにしません。**

（追記 2026-08-24: **Case B / D は設計・画面・統括AI接続まで済んだ** — 上の横断の表「入口の接続」。
以下の表は宿題を出した時点の記録として残す。）

### A. コードを書く前に設計する（構造が増えるもの）

**新しい実体・状態・画面が増えるもの。** あとから足すとデータの移行が要るので、先にやります。

| # | 宿題 | 増えるもの |
|---|---|---|
| 1 | **Discovery Mode**（Case B。条件 → 市場調査 → 3〜5候補 → 比較 → 選ぶ） | 候補と評価軸のテーブル、比較の画面、選択＝決定事項 |
| 2 | **既存事業の取り込み**（Case D。テキスト・URL・ファイル → Business Context → 診断） | Business Context のテーブル、取り込んだ資料、診断結果 |
| 6 | **質問**（選択肢 / 推奨 / 自由入力）— **画面ではなく、入力欄の上にくっつく板**にした | 質問と回答の保存、Work 作成フローの分岐 |
| 7 | **はじめての画面**（登録直後 → 2日以内に成果物）。**空の状態はここに含まれる** | 空状態は例外ではなく、**新規ユーザーが最初に見る画面** |

**画面としては5枚増えました。** ⓪はじめての画面 ⓪-a条件を集める（チャット）⓪-b候補をくらべる ⓪-c事業の取り込み ⓪-d診断結果。
質問は独立した画面をやめ、**どの画面でも入力欄の上に出る板**（`shell.ask`）にしています。

### B. コードに入ってから決める（器の振る舞い）

**データに影響しない、既存の器の状態違い。** 静止画では判断できないので、**動くものの上でやったほうが速くて正確です。**

| # | 宿題 | いつ |
|---|---|---|
| 3 | 統括AIの3状態（Dock / Expanded / Chat Focus） | Phase 4 |
| 4 | 右ペインの3状態（Normal / Split / Focus）とリサイズ | Phase 4 |
| 5 | エラーの画面（※空の状態は A-7 に移動） | Phase 4 |
| 8 | 会話履歴を Work ごとに分ける画面 | Phase 4 |

### C. 中身（あとで足せる）

| # | 宿題 | いつ |
|---|---|---|
| 9 | 初期ロスター6体の定義文（日本語） | Phase 7 |
| 10 | 多言語の土台（日本語ファースト、英語・韓国語を後から） | Phase 3 で骨だけ |

---

## Phase 3 に入る前にやること

**A の4件（画面6枚＋データモデルの追記）を1ラウンドで設計します。**
これは Phase 1 の続きなので、新しいフェーズ番号は振りません。

終わったら、その先の見た目の磨き込みは **Phase 4（ダミーデータで全画面）でやります。**
静止画で判断できることには限りがあるので、**Phase 4 が本当のUI/UXレビューの場**です。

### 任意 — 早めにやると安いこと

**画面を作らずに、1タスクだけ API で走らせて成果物を見る。**

この製品の一番大きな未知は「AI社員が出してくる成果物の質」です。
**それはUIをいくら磨いても分かりません。** 逆に、スクリプト1本（半日）で分かります。

分かること: 市場調査レポートが実用に足るか ／ 1タスクの実トークンと実時間 ／
憲法とAI社員の定義文をどう直すべきか ／ 原価の見積もりが合っているか

Phase 3 の前でも後でも構いませんが、**早いほど得です。**

## MVP でやらないこと（原文のまま）

3D Office ／ 複雑なアバター ／ 社員のレベル ／ マーケットプレイス ／ Gmail 自動送信 ／ Slack 自動化 ／
Stripe 操作の代行 ／ GitHub 自動マージ ／ 複雑な権限 ／ 完全自律の長期エージェント ／ 高度な長期記憶 ／
モバイルネイティブ ／ 複数人での共同編集
（複数会社はここから外した — 会社の切り替えをパンくずの根に置いた以上、本物にする。Phase 11 の残り）

**あとから拡張できる形にはしておく。いま作らない。**
