-- 候補に「何ができたら完了か」を持たせる。
--
-- 前は候補を採用したあとに **別の質問**（ask_end）で聞き返していた。
-- 社長から見ると「選んだのに、もう一度似たことを聞かれる」形になっていた。
-- 完了の定義は**候補の一部**（それが決まらないと候補として比べられない）なので、
-- 出すときに一緒に書かせて、社長は**見てから選ぶ**。
--
-- 既にある行は空文字。読み側は空を許す（無いものは無いと出す）。
alter table public.discovery_candidates
  add column if not exists ending text not null default '';
